---
phase: 02-web-sdk
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - packages/server/public/index.html
  - packages/server/public/viewer.js
  - packages/web-sdk/e2e/viewer.spec.ts
  - packages/web-sdk/README.md
  - package.json
  - DEPLOY.md
autonomous: false
requirements: [WEB-01, WEB-02, WEB-05]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "demo 查看器页本身即 SC1 证明：它只用 script src=/pushhub.js 与 new PushHub(serverUrl, channelKey) 两行接入（D-23，不另建 blank 页），E2E 经 URL 参数注入服务端地址与频道后实时收到消息并渲染（WEB-01/WEB-02）"
    - "查看器攻击样本按钮（D-22）渲染后 DOM 无 script 元素、无 on 开头属性、无 javascript:/data: href，链接带 rel=noopener noreferrer（SC3 真浏览器终验/WEB-05）"
    - "查看器排障细节（D-24）：接入配置存 localStorage 重开免填；oldest_kept_seq 语义可视化——比已见最早 seq 更老的批次到达时渲染一条\"更早消息已清理\"分隔线（D-10）；状态指示实时反映 status 事件"
    - "pushhub.js 多次请求后 wrangler tail 无对应 invocation（SC4：静态资产命中不触发 Worker，dashboard 计费不增）；对照 /api/send 请求在 tail 中可见"
    - "生产查看器保持连接期间执行一次重新部署（0.1.8），页面自动重连续补拉断连期间消息（SC2 生产维度：每次部署即免费混沌测试）"
    - "packages/web-sdk/README.md 完整记载 API 表面：四事件语义（含 D-16×D-17 交集措辞\"history 的 messages 永远只含宿主未见消息\"）、三生命周期方法、renderMarkdown、心跳/退避参数、两行接入示例——三端移植的接口契约文档"
  artifacts:
    - "packages/server/public/index.html（demo 查看器页，替换占位页；含 CSP meta 纵深防御）"
    - "packages/server/public/viewer.js（查看器逻辑，独立静态文件以满足 script-src 'self' CSP）"
    - "packages/web-sdk/e2e/viewer.spec.ts（SC1/SC3 + localStorage + 分隔线 E2E）"
    - "packages/web-sdk/README.md（SDK API 文档——参考实现契约载体）"
    - "DEPLOY.md 部署记录新增 0.1.7 与 0.1.8 行"
  key_links:
    - "index.html script 引用 /pushhub.js 加 ?v= 缓存规避参数 ↔ 构建产物更新节奏（stale SDK 缓存是部署后\"改了没生效\"的经典来源）"
    - "viewer.js click_url 跳转 ↔ scheme 白名单（仅 http/https）——消息内 click_url 不可信，D-21\"click_url 跳转同理\"的落点"
    - "oldest_kept_seq 分隔线 ↔ @pushhub/shared HistoryFrame.oldest_kept_seq 语义（D-10：诚实缺口不报错）"
    - "查看器 window.__pushhub 调试句柄 ↔ E2E 断连手段兜底与 D-24 排障定位"
  prohibitions:
    - statement: "查看器页消息内容进入 DOM 必经 PushHub.renderMarkdown（与 SDK 同一消毒管道，无双路径漂移）"
      status: "flagged-unverified"
      verification: "viewer.js 中对消息 text 的 DOM 写入唯一入口是 renderMarkdown 返回值"
    - statement: "查看器不做消息构造与回复（D-22 边界：发消息用 curl/smoke，回复属 Phase 4）"
      status: "flagged-unverified"
      verification: "viewer.js 与 index.html 无对 /api/send 或任何回复端点的调用"
    - statement: "public/ 内静态文件路径不以 api 前缀开头（wrangler.jsonc asset-first 遮蔽禁令）"
      status: "flagged-unverified"
      verification: "packages/server/public/ 目录清单仅 index.html、viewer.js、pushhub.js（产物）"
    - statement: "CSP meta 保持 script-src 'self'（禁 unsafe-inline）——消毒层失守时的纵深防线"
      status: "flagged-unverified"
      verification: "index.html 的 CSP meta 含 script-src 'self' 且页面全部脚本经外链文件加载"
---

<objective>
交付 demo 查看器页（D-22/D-23/D-24）与 SDK API 文档（README），并在生产完成 Phase 2 全部 4 条成功标准的最终验证：SC1 两行接入（查看器自身即证明）、SC3 攻击样本渲染（真浏览器 + 生产）、SC4 资产命中不计 Worker 请求（wrangler tail 对照）、SC2 生产部署混沌（查看器保持连接时再部署一次，观察自动重连续补拉）。

Purpose: 查看器是用户可自助打开的端到端验证入口与后续每次部署的免费混沌观察点；README 是 Phase 5/6 移植时照抄的接口契约文档（CONTEXT specifics：事件 API 表面即三端参考契约）。
Output: packages/server/public/{index.html, viewer.js}、packages/web-sdk/README.md、viewer.spec.ts、生产部署 0.1.7 + 0.1.8（混沌）与 DEPLOY.md 记录。
</objective>

<execution_context>
@C:/Users/31243/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/31243/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-web-sdk/02-CONTEXT.md

# 前序产物（API 表面与韧性行为——查看器消费）
@.planning/phases/02-web-sdk/02-01-SUMMARY.md
@.planning/phases/02-web-sdk/02-02-SUMMARY.md
@packages/web-sdk/src/pushhub.ts
@packages/web-sdk/src/render/render-markdown.ts

# 部署与集成锚点
@packages/server/public/index.html
@packages/server/wrangler.jsonc
@packages/server/src/index.ts
@DEPLOY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demo 查看器页（index.html + viewer.js + CSP 纵深）</name>
  <files>packages/server/public/index.html, packages/server/public/viewer.js</files>
  <read_first>
    - D:/AIworkspaces/PushHub/packages/server/public/index.html（占位页——保留 zh-CN/utf-8/viewport 头三行惯例）
    - D:/AIworkspaces/PushHub/packages/web-sdk/src/pushhub.ts（API 表面：四事件载荷形态、status 枚举、生命周期方法——查看器是第一个真实消费者）
    - D:/AIworkspaces/PushHub/packages/web-sdk/src/render/render-markdown.ts（renderMarkdown——消息渲染唯一管道）
    - D:/AIworkspaces/PushHub/packages/shared/src/index.ts（HistoryFrame.oldest_kept_seq 与 MessageFrame.click_url 字段——分隔线与跳转的依据）
    - D:/AIworkspaces/PushHub/packages/server/wrangler.jsonc（L33-34 注释：public/ 文件路径不得以 api 前缀开头；assets asset-first）
    - D:/AIworkspaces/PushHub/.planning/phases/02-web-sdk/02-CONTEXT.md（D-22/D-23/D-24 查看器三决策全文）
  </read_first>
  <action>
    1. index.html（替换占位页）：保留原头三行 meta；新增 CSP meta——default-src 'self'、script-src 'self'（禁 inline 脚本，纵深防御：消毒层若失守内联脚本仍被 CSP 拦截）、style-src 'self' 'unsafe-inline'、connect-src 'self' ws: wss: http: https:（查看器允许指向任意服务端地址）、img-src * data:。结构：接入表单（serverUrl 输入框默认 location.origin、channelKey 输入框、连接按钮）、状态指示（色点 + 文本绑定 status 事件）、消息流列表容器、攻击样本区（D-22：若干预设按钮，本地经 PushHub.renderMarkdown 渲染展示，不从服务端发送）。script 标签两条：/pushhub.js 带 ?v= 缓存规避查询参数（值取当前部署版本号字面量，README 注明升级 SDK 时同步改）与 /viewer.js。文件名避开 api 前缀（wrangler.jsonc 禁令）。
    2. viewer.js：读取 URL 参数 server 与 key 预填表单（E2E 注入路径，研究 A5）；提交时写 localStorage（键 pushhub.server 与 pushhub.key，D-24 下次免填；页面注明 key 存本机浏览器的取舍）并 new PushHub(serverUrl, channelKey)（D-23：查看器自身即两行接入证明）。on("status") 驱动指示灯四态（02-01 定稿枚举）。on("history") 与 on("message") 统一进 appendMessage：每条消息渲染时间戳、title 加粗、text 经 PushHub.renderMarkdown 后写入 li 的 innerHTML（消毒管道唯一入口）；click_url 处理——点击消息卡片时先做 scheme 白名单校验（仅 http 与 https 放行，其余 scheme 丢弃并 console 提示）再 window.open 带 noopener（D-21 click_url 同理条款）。on("history") 载荷 oldest_kept_seq 大于零且大于当前已见最早 seq 减一时，在列表顶部渲染一条静态分隔线"更早的消息已被清理"（D-10/D-24，只渲染一次）。on("error") 在页面错误条展示 message 与 fatal 标记。连接前先 destroy 旧实例（防重复连接）。暴露 window.__pushhub = 当前实例（调试句柄：E2E 断连兜底与 D-24 排障定位）。攻击样本按钮：内置字符串数组（script 注入、img onerror、javascript: 链接各一），点击后经 renderMarkdown 渲染进展示区——验证的就是消毒本身。
    3. 布局为极简单列（内联 style 或 style 标签，style-src 已允许 unsafe-inline）；不做消息构造、不做回复（D-22 边界——发消息用 curl/smoke.mjs，回复属 Phase 4）。
  </action>
  <verify>
    <automated>
      cd D:/AIworkspaces/PushHub：
      1. pnpm --filter @pushhub/web-sdk run build 后 node -e 断言 packages/server/public/ 下 index.html、viewer.js、pushhub.js 三文件存在且文件名不以 api 开头
      2. node 解析 index.html 断言 CSP meta 含 script-src 'self' 且 script 标签 src 为 /pushhub.js 与 /viewer.js（含 ?v= 参数）
      3. grep 断言 viewer.js 含 localStorage 键 pushhub.server 与 window.__pushhub 赋值
    </automated>
  </verify>
  <acceptance_criteria>
    - packages/server/public/index.html 为查看器页（非占位文案），含 CSP meta（script-src 'self'）与 /pushhub.js、/viewer.js 两个外链 script（D-22/D-23）
    - viewer.js 中消息 text 的 DOM 写入唯一来源为 PushHub.renderMarkdown 返回值；click_url 跳转前有 http/https scheme 白名单判定（D-21）
    - URL 参数 server/key 预填 + localStorage 持久化（键 pushhub.server、pushhub.key）+ window.__pushhub 调试句柄（D-24）
    - oldest_kept_seq 分隔线逻辑存在且单次渲染（D-10 可视化）
    - viewer.js 与 index.html 无对 /api/send 或任何回复端点的调用（D-22）
  </acceptance_criteria>
  <done>查看器页在 wrangler dev 下可打开、接入、实时收消息（E2E 在 Task 2 验证）；CSP 与消毒双防线就位。</done>
</task>

<task type="auto">
  <name>Task 2: viewer E2E（SC1/SC3）+ SDK API 文档（README）</name>
  <files>packages/web-sdk/e2e/viewer.spec.ts, packages/web-sdk/README.md</files>
  <read_first>
    - D:/AIworkspaces/PushHub/packages/web-sdk/e2e/tracer.spec.ts（setup 建频道与断言风格——本任务扩展）
    - D:/AIworkspaces/PushHub/packages/web-sdk/e2e/reconnect.spec.ts（02-02 产物——风格与调试句柄用法参照）
    - D:/AIworkspaces/PushHub/packages/web-sdk/src/pushhub.ts（README 文档化的 API 事实源）
    - D:/AIworkspaces/PushHub/.planning/phases/02-web-sdk/02-CONTEXT.md（D-16~D-21 决策措辞——README 语义段落逐条对应）
    - D:/AIworkspaces/PushHub/packages/shared/README.md（协议演进三规则文档风格参照）
  </read_first>
  <action>
    1. e2e/viewer.spec.ts：setup 建频道（同 tracer 模式）→ goto /?server=http://127.0.0.1:4911&key=<channelKey>（URL 参数注入，A5）→ 断言状态指示到 online → Node 侧 fetch 发一条含 Markdown 强调语法的消息（如 text 含双星号包围词）→ 消息流出现该条且 strong 元素存在（Markdown 渲染真路径）→ 点击攻击样本按钮 → 展示区 querySelector("script") 为 null、无 on 开头属性、锚元素带 rel 含 noopener noreferrer（SC3 终验）→ reload 页面 → 断言表单值从 localStorage 恢复（D-24）→ on("history") 场景：断言分隔线逻辑（可经预填充消息的频道 + 构造 oldest_kept_seq 观察，或经 __pushhub 句柄单测式驱动）。
    2. packages/web-sdk/README.md：API 参考文档，段落含——两行接入示例（script 标签 + new PushHub(serverUrl, channelKey)，WEB-01 形态）；四事件语义表（message 实时逐条 / history 批次，逐字写明"on(\"history\") 载荷的 messages 永远只含宿主未见消息；oldest_kept_seq 与 has_more 原样透传"——D-16×D-17 交集契约措辞 / status 枚举 / error 载荷）；生命周期三方法（构造即连、disconnect 可再 connect、destroy 释放）；静态 PushHub.renderMarkdown(text) 用法与"宿主可只用原始数据自行渲染"说明（WEB-05 双形态）；心跳与退避参数表（30s ping、10s pong 死线、5s 探活死线、full jitter cap 60s、DEDUP_WINDOW=1000、SYNC_PAGE_MAX=100）；D-21 链接强制行为说明；频道 key 存 localStorage 的安全取舍说明；三端移植注意（事件 API 表面即 Phase 5/6 接口契约、render-markdown.ts 可移植模块直接 import 路径）；?v= 缓存参数维护约定。文档措辞以 pushhub.ts 实现为准，不写未实现的行为。
    3. 全套回归：build + test + e2e（tracer + reconnect + viewer 三 spec）。
  </action>
  <verify>
    <automated>
      cd D:/AIworkspaces/PushHub：
      1. pnpm --filter @pushhub/web-sdk run e2e（三个 spec 文件全绿）
      2. grep 断言 README.md 含字符串 "messages 永远只含宿主未见消息" 与 renderMarkdown 与 noopener（文档契约措辞落位）
    </automated>
  </verify>
  <acceptance_criteria>
    - viewer.spec.ts 绿：URL 参数接入 → online → 实时消息 Markdown 渲染（strong 元素）→ 攻击样本 DOM 无害断言 → localStorage 恢复 → 分隔线
    - packages/web-sdk/README.md 存在且含四事件表、生命周期、renderMarkdown、参数表、交集语义措辞（"messages 永远只含宿主未见消息"原文出现）
    - e2e 全套（tracer/reconnect/viewer）一次通过
  </acceptance_criteria>
  <done>SC1/SC3 在真实查看器页面 E2E 定格；SDK 对外契约文档就位（Phase 5/6 移植依据）。</done>
</task>

<task type="auto">
  <name>Task 3: 部署 0.1.7 + 生产四标准终验（SC4 资产命中 + SC2 部署混沌）</name>
  <files>package.json, DEPLOY.md</files>
  <precondition>Task 2 全绿；wrangler 已登录；.claude/admin.key 存在；https://pushhub.dyun.org 可达；本机可运行 Playwright（Chromium 已装）。</precondition>
  <read_first>
    - D:/AIworkspaces/PushHub/DEPLOY.md（版本 +1 规约、部署记录表、D-15 checklist）
    - D:/AIworkspaces/PushHub/.planning/STATE.md（workers.dev SNI 阻断记录——一律走 pushhub.dyun.org）
    - D:/AIworkspaces/PushHub/packages/server/wrangler.jsonc（assets binding——SC4 验证对象）
    - D:/AIworkspaces/PushHub/scripts/smoke.mjs（生产冒烟命令形态）
  </read_first>
  <action>
    1. 根 package.json version 0.1.6 改 0.1.7；pnpm run deploy（链式构建 + 部署，查看器与 SDK 同时上线）。
    2. 生产冒烟：PH_SMOKE_URL=https://pushhub.dyun.org PH_ADMIN_KEY=$(cat .claude/admin.key) node scripts/smoke.mjs → SMOKE OK。
    3. SC4 验证（wrangler tail 对照法）：后台启动 pnpm --filter @pushhub/server exec wrangler tail --format json（run_in_background）；对 https://pushhub.dyun.org/pushhub.js 连续 fetch 5 次均 200；等待数秒后检查 tail 输出——这 5 次资产请求零 invocation 事件；随后 POST /api/send 一次（冒烟频道即可），tail 中出现且仅出现该次 Worker invocation。结论写入 DEPLOY.md 0.1.7 行（静态资产命中不触发 Worker，免费额度不受 SDK 分发影响）。若 tail 因网络/权限不可用，降级为 checkpoint 人工核对 dashboard 请求曲线。
    4. SC1/SC3 生产快验：node fetch https://pushhub.dyun.org/ 断言 200 且 HTML 含 /pushhub.js 与 /viewer.js 引用。
    5. SC2 生产混沌（每次部署即免费混沌测试的正式演练）：用 Playwright 脚本（一次性，不入测试套件——生产域名不进自动化，D-26）打开 https://pushhub.dyun.org/?server=https://pushhub.dyun.org&key=<冒烟频道 channelKey>（admin API 再建一个专用频道更佳）→ 等 status online → 根 package.json version 0.1.7 改 0.1.8 并 pnpm run deploy（部署必然断开全量 WS）→ 部署执行期间与完成后 Node 侧 fetch 发 2 条消息 → 轮询页面（最长 90s，覆盖 60s 退避上限）断言：status 回 online、2 条消息出现、零重复。DEPLOY.md 记录 0.1.8 行（注明为 SC2 混沌验证部署）。
    6. D-15 ④ dashboard Duration 人工核对（见 human-check）。
  </action>
  <verify>
    <automated>
      1. PH_SMOKE_URL=https://pushhub.dyun.org PH_ADMIN_KEY=$(cat .claude/admin.key) node scripts/smoke.mjs 输出 SMOKE OK
      2. node -e 断言 https://pushhub.dyun.org/ 与 /pushhub.js 均 200，前者 HTML 含 viewer 引用
      3. SC4 tail 对照：/pushhub.js×5 零 invocation、/api/send 1 次 invocation（或降级人工核对记录）
      4. SC2 混沌脚本断言重连 + 恰补 2 条零重复（Playwright 输出 PASS）
    </automated>
    <human-check>Cloudflare dashboard：① Workers & Pages → pushhub → Durable Objects → Duration 空闲平直（D-15 ④）；② 部署断连重连尖峰后回落（预期现象，DEPLOY.md 已述）；③ 确认 /pushhub.js 请求不进 Workers 请求计数曲线（SC4 的 dashboard 视角终验，~1 分钟）。</human-check>
  </verify>
  <acceptance_criteria>
    - 生产 0.1.7：SMOKE OK；/ 与 /pushhub.js 均 200；查看器页 HTML 含两个 script 引用
    - SC4：资产请求零 Worker invocation 有 tail 或 dashboard 证据（写入 DEPLOY.md 0.1.7 行）
    - SC2 生产混沌：0.1.8 部署后查看器自动重连、恰补 2 条、零重复（脚本输出 PASS，记录进 DEPLOY.md 0.1.8 行）
    - DEPLOY.md 新增 0.1.7 与 0.1.8 两行（各含 Worker Version ID 与验证结果）
  </acceptance_criteria>
  <done>Phase 2 四条成功标准在生产全部有证据：SC1（查看器两行接入 + E2E）、SC2（本地三形态 E2E + 生产部署混沌）、SC3（jsdom 回归 + 真浏览器 + 生产攻击样本）、SC4（tail/dashboard 资产命中零计费）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 不可信消息内容 → 查看器 DOM | 查看器是消毒管道的第一个真实宿主；click_url 是消息内不可信跳转指令 |
| 查看器 localStorage | Channel Key 明文存本机浏览器（D-24 用户自担取舍，需文档明示） |
| 生产部署 → 已连接客户端 | 部署断连是 SC2 的混沌注入源（也是攻击面外的可用性事件） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | viewer.js click_url 跳转（javascript:/data: scheme 滥用） | medium | mitigate | 跳转前 scheme 白名单（仅 http/https），其余丢弃并提示（D-21 "click_url 跳转同理"条款落地） |
| T-02-09 | Tampering + Elevation | 查看器消息渲染（消毒层若失守的最后防线） | critical | mitigate | 消息 text 唯一入口 renderMarkdown + CSP meta script-src 'self' 禁 inline 脚本（纵深防御，双层独立） |
| T-02-05 | Information Disclosure | 查看器 localStorage 明文 Channel Key | medium | accept | D-24 锁定的用户自担取舍（下次免填的便利）；页面与 README 明示风险与清除方法；接受理由：本机浏览器存储的读取门槛已高于密钥经 URL 传输的面 |
| T-02-10 | Spoofing | 部署窗口期重连到的旧版本边缘节点 | low | accept | Cloudflare 部署原子性 + SDK 重连后 sync 补拉消解消息面差异；观察点已入 D-15 checklist（重连尖峰后回落） |
</threat_model>

<verification>
- pnpm --filter @pushhub/web-sdk run e2e：tracer + reconnect + viewer 三 spec 全绿（phase gate 前置）
- 生产 0.1.7：SMOKE OK + 查看器页可达 + SC4 资产命中零计费证据
- 生产 0.1.8 混沌：查看器自动重连续补拉恰 2 条零重复
- README 契约措辞落位（交集语义原文可 grep）
</verification>

<success_criteria>
- SC1：查看器两行接入即实时收消息（本地 E2E + 生产页面可达）
- SC2：本地三种断连形态 E2E（02-02）+ 生产部署混沌（本计划）双重证明
- SC3：攻击样本三层验证（jsdom fixtures 回归 + 真浏览器 E2E + 生产查看器按钮）
- SC4：/pushhub.js 由静态资产分发零 Worker 计费（tail/dashboard 证据）
- SDK API 文档（README）成为 Phase 5/6 移植契约
</success_criteria>

## Artifacts this phase produces

（02-03 增补部分——至此 Phase 2 全部产物闭合）
- 静态页面：packages/server/public/index.html（demo 查看器，CSP 纵深）与 packages/server/public/viewer.js（查看器逻辑；window.__pushhub 调试句柄；localStorage 键 pushhub.server / pushhub.key）
- 文档：packages/web-sdk/README.md（SDK API 契约：四事件表含"messages 永远只含宿主未见消息"交集措辞、三生命周期、renderMarkdown、心跳/退避参数表、?v= 缓存约定、三端移植注意）
- E2E：e2e/viewer.spec.ts（SC1/SC3/localStorage/分隔线）
- 部署：0.1.7（查看器上线）与 0.1.8（SC2 混沌验证）+ DEPLOY.md 记录两行
- 阶段级符号总目（三计划合并）：window.PushHub 类、on 四事件、connect/disconnect/destroy、静态 renderMarkdown、PING 常量、退避/心跳/探活/去重/翻页七组常量、createMachine 纯状态机、parseServerFrame、SeqDedup、@pushhub/web-sdk 四个 npm scripts、根 deploy/test 链式脚本、三个静态资产文件

<output>
Create .planning/phases/02-web-sdk/02-03-SUMMARY.md when done
</output>
