---
phase: 02-web-sdk
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web-sdk/src/render/render-markdown.ts
  - packages/web-sdk/test/fixtures/attack-samples.json
  - packages/web-sdk/src/connection-machine.ts
  - packages/web-sdk/src/pushhub.ts
  - packages/web-sdk/test/machine-fatal.test.ts
  - packages/web-sdk/test/adapter-lifecycle.test.ts
autonomous: true
gap_closure: true
gap_ids: [G-02-2, G-02-4]
requirements: [WEB-05, WEB-01]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SVG 命名空间锚点（tagName 小写 'a'）与 HTML 锚点同等携带 target=_blank + rel=noopener noreferrer——D-21 tabnabbing 加固不可绕过（G-02-2 truth）"
    - "style/form/input/button/select/textarea/label/option 标签经消毒后从渲染输出消失——DOMPurify FORBID_TAGS 收敛放行面（G-02-4 WR-02）"
    - "畸形 serverUrl 构造 PushHub 不再同步抛异常：宿主收到 error(fatal, code=connect_failed) + status=offline，不再卡在 connecting（G-02-4 WR-04）"
    - "web-sdk 测试套件全绿，既有 68 例基线零回退"
  artifacts:
    - packages/web-sdk/src/render/render-markdown.ts（tagName 两分支判定 + FORBID_TAGS 配置）
    - packages/web-sdk/test/fixtures/attack-samples.json（SVG 锚点 + 禁用标签新样本）
    - packages/web-sdk/src/connection-machine.ts（WS_FAIL 输入事件）
    - packages/web-sdk/src/pushhub.ts（openSocket try/catch + 延迟 dispatch）
  key_links:
    - "afterSanitizeAttributes 两分支判定 ↔ attack-samples.json SVG 锚点样本断言（fixture 回归锁定 G-02-2 修复）"
    - "openSocket try/catch → setTimeout(0) 延迟 dispatch(WS_FAIL) → machine connecting→offline + emitError(fatal) → 宿主 on(\"error\") 监听（延迟一跳保证 error 不在构造期、宿主监听注册前丢失）"
---

<objective>
闭合 UAT gap G-02-2（major）与 G-02-4 的 WR-02/WR-04 两项（SDK 层修复）：
render-markdown.ts 的 afterSanitizeAttributes 钩子 tagName 大小写两分支判定 + DOMPurify FORBID_TAGS 放行面收敛；pushhub.ts 对畸形 serverUrl 的构造容错（emitError 而非同步抛异常）。

Purpose: SVG 命名空间锚点绕过 D-21 tabnabbing 加固是已实证的安全缺口（02-UAT.md Test 2 用户裁决"修复"）；WR-02/WR-04 为用户裁决的批次加固项。三者全部改变 pushhub.js 产物字节——本计划只改源码与测试，重建与部署统一归 02-06。
Output: 修复后的 render-markdown.ts / connection-machine.ts / pushhub.ts、增补的 attack-samples.json 样本、machine/adapter 层新测试用例。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-web-sdk/02-UAT.md
@packages/web-sdk/src/render/render-markdown.ts
@packages/web-sdk/src/connection-machine.ts
@packages/web-sdk/src/pushhub.ts

Gap 来源：02-UAT.md ## Gaps 的 G-02-2（root_cause: afterSanitizeAttributes 判定 `node.tagName === 'A'` 大小写敏感，SVG 命名空间 tagName 为小写 'a' 落入 else 分支不设 target/rel）与 G-02-4（WR-02/WR-04）。根因已诊断完毕，直接修复，无需再调查。

关键代码事实（已核实）：
- render-markdown.ts:54 `if (node.tagName === "A")`——HTML 命名空间 tagName 大写，SVG 命名空间小写；hook 注册在 purify 惰性单例创建分支内。
- 真实的同步抛出点不在构造函数的字符串拼接，而在 pushhub.ts openSocket() 内 `new WebSocket(this.wsUrl)`——畸形 URL 使 WebSocket 构造器同步抛 SyntaxError，经 apply→dispatch 传回构造函数（构造即连 D-18 时序）。
- render.test.ts 的 audit() 用 `querySelectorAll("a")` 做结构断言（HTML 文档中按 local name 匹配，同样选中 SVG `<a>`）——新 fixture 样本自动获得"所有锚点 target=_blank / rel 含 noopener noreferrer"的结构审计，无需改测试代码。
- 攻击样本表是"实证预期输出表"（RESEARCH Pattern 2）：expected 值先观察实际输出、核验安全语义后固化。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SVG 锚点两分支修复 + DOMPurify FORBID_TAGS 收敛（G-02-2 + WR-02）</name>
  <files>packages/web-sdk/test/fixtures/attack-samples.json, packages/web-sdk/src/render/render-markdown.ts</files>
  <behavior>
    - Test 1（svg-anchor，G-02-2 RED）：输入含 SVG 命名空间锚点的样本（如 `<svg><a xlink:href="https://ok.example"><text x="8" y="16">链接</text></a></svg>`），渲染输出的 SVG 锚点必须携带 target="_blank" 且 rel 含 noopener noreferrer——当前代码下该断言失败（SVG 锚点无 target/rel）
    - Test 2（forbidden-tags，WR-02）：`<style>`、`<form><input><button>` 等样本消毒后这些标签从输出消失
    - Test 3（task-list-tradeoff）：GFM 任务列表 `- [x] 已完成` 在 FORBID input 后退化为无复选框的 li（input 消失、文本保留）——fixture 固化这一有意取舍
  </behavior>
  <action>
    第一步（RED）：向 test/fixtures/attack-samples.json 增补四条样本——svg-anchor（SVG 命名空间锚点）、style-tag（`<style>body{}</style>` 类）、form-controls（form/input/button 组合）、task-list（`- [x]` GFM 任务列表）。expected 值按实证流程固化：先临时跑 renderMarkdown 观察实际输出，核验其安全语义后写入 fixture（svg-anchor 样本在修复前输出中 SVG 锚点不带 target/rel——这就是 RED 证据；其余三条在 FORBID_TAGS 生效前后输出不同，expected 固化为收敛后的形态）。注意 attack-samples.json 仅被 render-markdown.ts 头注释与 render.test.ts 消费，不影响 E2E 计数。
    提交：test(02-04): 增补 SVG 锚点 + 禁用标签攻击样本（RED）。
    第二步（GREEN）：
    (a) G-02-2 根因修复：render-markdown.ts afterSanitizeAttributes 钩子内判定改为 tagName === "A" || tagName === "a" 两分支（HTML 命名空间大写、SVG 命名空间小写，两者同等设 target=_blank + rel=noopener noreferrer，D-21 加固不可绕过）。
    (b) WR-02 放行面收敛：purify.sanitize 调用增加 FORBID_TAGS 选项，清单为 style、form、input、button、select、textarea、label、option（UAT 裁决点名 style/form/input/button，"等"字覆盖同族表单控件）；hook 注册位置不变（仍在惰性单例分支）。
    (c) 已知取舍必须在 SUMMARY 记录：FORBID input 使 GFM 任务列表复选框字形消失（文本保留）——这是 UAT Test 4 用户裁决的明知取舍，task-list 样本即为固化证据；若后续开发者想保留复选框，从清单去掉 input 即有 fixture 护航。
    跑 pnpm --filter @pushhub/web-sdk test 全绿后提交：feat(02-04): SVG 锚点两分支判定 + DOMPurify FORBID_TAGS 收敛（G-02-2/WR-02）。
    禁止事项：不得改动 marked 配置（gfm/breaks 冻结）；不得在 FORBID_TAGS 之外引入 ALLOWED_TAGS 白名单反转（改动面失控）；不得改 render.test.ts 的 audit 逻辑（结构断言本就覆盖 SVG 锚点）。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/web-sdk test</automated>
  </verify>
  <done>SVG 命名空间锚点输出携带 target=_blank + rel=noopener noreferrer；style/form/input/button/select/textarea/label/option 从输出消失；任务列表文本保留复选框消失形态已固化进 fixture；全套测试绿（原 68 例零回退）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 畸形 serverUrl 构造容错——WS_FAIL 事件 + adapter 延迟派发（WR-04）</name>
  <files>packages/web-sdk/test/machine-fatal.test.ts, packages/web-sdk/test/adapter-lifecycle.test.ts, packages/web-sdk/src/connection-machine.ts, packages/web-sdk/src/pushhub.ts</files>
  <behavior>
    - Test 1（machine 层）：新输入事件 { kind: "WS_FAIL"; message: string } 在 connecting 态 → cancelAllTimers + emitError({ message, code: "connect_failed", fatal: true }) + emitStatus("offline")，且此后零动作（不武装重连定时器）；在 idle/offline/destroyed/online 态 → 零动作（防御性忽略）
    - Test 2（adapter 层）：stub 一个构造即抛 SyntaxError 的 WebSocket 类后，new PushHub("not a url", "phc_" + 32 字符) 构造不抛异常；vi.advanceTimersByTime(0) 后 on("error") 监听收到 fatal:true + code:"connect_failed"，on("status") 监听收到 "offline"，且不再创建新 socket（fatal 不重连）
    - Test 3（密钥纪律）：错误载荷 message 不含 Channel Key 子串（沿用"密钥即身份不进日志/错误"纪律——错误文案不得内嵌 wsUrl 全文，其路径段含密钥）
  </behavior>
  <action>
    第一步（RED）：machine-fatal.test.ts 增 describe（WS_FAIL 与既有 v!==1 fatal 同族语义：报错 + 停止 + 不复活）；adapter-lifecycle.test.ts 增畸形 URL 用例——在用例内 vi.stubGlobal 覆盖 beforeEach 的 Recording stub 为构造即 throw new SyntaxError 的类，断言构造不抛、延迟一跳后 error/status 事件到达（fake timers 下用 vi.advanceTimersByTime(0) 触发 setTimeout(...,0)）。测试密钥格式沿用 openHub 的 "phc_" + "k".repeat(32)。
    提交：test(02-04): WS_FAIL 事件与畸形 serverUrl 容错用例（RED）。
    第二步（GREEN）：
    (a) connection-machine.ts：MachineEvent 联合增 { kind: "WS_FAIL"; message: string }；input switch 增 case——仅 state === "connecting" 时 cancelAllTimers + emitError({ message: event.message, code: "connect_failed", fatal: true }) + enter("offline")，不武装任何定时器（畸形 URL 是确定性配置错误，重试无意义，fatal 语义与 D-07 一致）；其余态零动作。模块保持零平台 API（prohibition）。
    (b) pushhub.ts：openSocket() 内 `new WebSocket(this.wsUrl)` 包 try/catch；catch 内不得同步 dispatch（构造即连 D-18 时序下，构造函数内的同步 emitError 会在宿主 on() 注册前丢失——这正是查看器卡"连接中"的另一半根因），改为 setTimeout(() => this.dispatch({ kind: "WS_FAIL", message: "..." }), 0) 延迟一跳派发，保证构造已返回、宿主监听已挂。错误文案用英文静态描述（如 "failed to construct WebSocket for serverUrl"），不内嵌 wsUrl 或密钥。this.ws 保持 null。
    (c) 既有 adapter 测试不回归：正常路径 openHub() 行为不变（FakeWebSocket 构造不抛）。
    跑 pnpm --filter @pushhub/web-sdk test 全绿后提交：feat(02-04): 畸形 serverUrl 容错——WS_FAIL 事件 + 延迟派发（WR-04）。
    禁止事项：不得把修复做在 viewer.js 侧兜底（UAT 明确要求 SDK 层 emitError 路径）；不得给构造函数加 URL 预校验双路径（唯一抛出源是 WebSocket 构造器，包裹它即可）；不得改变 connect()/disconnect() 的同步语义。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/web-sdk test</automated>
  </verify>
  <done>畸形 serverUrl 构造不抛异常，宿主可观察 error(fatal, code=connect_failed) + status offline；机器层 WS_FAIL 语义单测覆盖；全套测试绿零回退。查看器端到端的"呈现 error 态不卡连接中"验证归 02-06（避免与 02-05 同文件不同 wave 冲突）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webhook 消息文本 → renderMarkdown 管道 | 任意外部发送方的不可信输入，消毒后直达所有客户端 DOM |
| 宿主提供的 serverUrl → SDK WebSocket 构造 | 本地用户输入（查看器表单/URL 参数），畸形值此前以同步异常炸穿构造函数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | render-markdown.ts afterSanitizeAttributes | critical | mitigate | tagName 两分支判定堵住 SVG 命名空间锚点绕过 D-21 强制新窗口（G-02-2），attack-samples.json 增样本固化回归 |
| T-02-02 | Tampering | DOMPurify 默认 profile | medium | mitigate | FORBID_TAGS 收敛 style/form/input/button 等非聊天语义标签，压缩表单钓鱼与 UI 伪装攻击面（WR-02） |
| T-02-03 | DoS | pushhub.ts 构造函数/openSocket | low | mitigate | 畸形 serverUrl 由同步 throw 改为 error(fatal)+offline 事件路径，宿主可呈现错误不卡 UI（WR-04） |
| T-02-04 | Information Disclosure | WS_FAIL 错误载荷 | low | mitigate | 错误消息为静态英文文案，不内嵌 wsUrl 全文（路径段含 Channel Key），沿用既有密钥不外泄纪律 |
</threat_model>

<verification>
- pnpm --filter @pushhub/web-sdk test 全绿：新增 SVG 锚点/禁用标签/任务列表样本断言 + WS_FAIL 机器语义 + 畸形 URL adapter 用例全过，既有 68 例零回退。
- pnpm --filter @pushhub/web-sdk run typecheck 通过（MachineEvent 联合扩展后 switch 穷尽性由编译器保证）。
- 产物字节变更（本计划必然产生）不在本计划部署——统一归 02-06 的 0.1.9。
</verification>

<success_criteria>
- G-02-2 truth 成立：SVG 命名空间锚点与 HTML 锚点同等携带 target=_blank + rel=noopener noreferrer，且由 fixture 回归锁定。
- G-02-4 WR-02/WR-04 成立：FORBID_TAGS 收敛落地并有断言；畸形 serverUrl 走 error 事件路径不抛异常。
- 两个任务各自完成 test commit → feat commit 的 TDD 提交对。
</success_criteria>

<output>
Create .planning/phases/02-web-sdk/02-04-SUMMARY.md when done
</output>
