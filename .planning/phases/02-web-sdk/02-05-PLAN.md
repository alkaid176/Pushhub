---
phase: 02-web-sdk
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web-sdk/build.mjs
  - packages/web-sdk/test/cache-bust-sync.test.ts
  - packages/web-sdk/scripts/chaos-sc2.mjs
  - packages/server/public/index.html
  - packages/server/public/viewer.js
  - packages/web-sdk/e2e/viewer.spec.ts
autonomous: true
gap_closure: true
gap_ids: [G-02-3, G-02-4]
requirements: [WEB-01]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "index.html 的 /pushhub.js?v= 缓存参数与根 package.json version 恒一致——构建机制保证，非人工纪律（G-02-3 truth）"
    - "chaos-sc2.mjs 日志不再含硬编码版本号字符串（G-02-3 次要项）"
    - "localStorage 被全禁环境（隐私模式/存储策略）查看器正常加载：server 输入框回退 window.location.origin 缺省、脚本无未捕获异常（G-02-4 WR-03）"
    - "web-sdk 单测与 viewer E2E 全绿零回退"
  artifacts:
    - packages/web-sdk/build.mjs（构建期版本注入步骤）
    - packages/web-sdk/test/cache-bust-sync.test.ts（恒一致断言）
    - packages/web-sdk/scripts/chaos-sc2.mjs（日志改为 EXPECT_VERSION 插值）
    - packages/server/public/viewer.js（localStorage 读取侧 try/catch）
  key_links:
    - "根 package.json version → build.mjs 构建期注入 → index.html ?v= → cache-bust-sync.test.ts 断言（三点恒一致的机制闭环）"
    - "viewer.js localStorage 读取 try/catch ↔ viewer.spec.ts 存储禁用 E2E 用例（真实浏览器验证防护生效）"
---

<objective>
闭合 UAT gap G-02-3（major）与 G-02-4 的 WR-03（viewer 加固）：
build.mjs 构建期自动注入根版本号到 index.html 的 ?v= 缓存参数（机制化取代人工同步）+ chaos-sc2.mjs 硬编码日志修正；viewer.js localStorage 读取路径补 try/catch（与写入侧对齐）。

Purpose: ?v= 手工硬编码已在 0.1.8 出现脱钩（0.1.7 残留），未来 SDK 字节变更时会命中 stale 缓存——02-UAT.md Test 3 用户裁决"机制化"；WR-03 为批次加固项。本计划与 02-04 零文件重叠，可并行执行。
Output: build.mjs 注入机制 + 恒一致测试 + chaos 日志修正 + viewer.js 读取防护 + viewer E2E 新用例。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-web-sdk/02-UAT.md
@packages/web-sdk/build.mjs
@packages/server/public/viewer.js

Gap 来源：02-UAT.md ## Gaps 的 G-02-3（root_cause: ?v= 为 index.html 手工硬编码与根 package.json version 无机制关联）与 G-02-4 的 WR-03。根因已诊断完毕，直接修复。

关键代码事实（已核实）：
- index.html 引用行为 `<script src="/pushhub.js?v=0.1.7"></script>`，其上一行有约定注释（值=当前部署版本号，升级时同步改——本计划将这句人工纪律作废为机制保证，注释一并更新）。
- build.mjs 已有结构：esbuild 构建产物 copy 到 packages/server/public/pushhub.js 后做字节报表 + vm 沙箱冒烟；注入步骤加在 copy 之后。
- chaos-sc2.mjs 顶部已解析 `const EXPECT_VERSION = arg("expect-version")` 且断言 rootVersion === EXPECT_VERSION（不匹配即中止）——硬编码 '0.1.7' 的日志行改插值 EXPECT_VERSION 即语义准确，零新文件读取。
- viewer.js 写入侧（connect() 内 setItem）已有 try/catch；读取侧裸调用在表单预填两行（serverInput.value / keyInput.value 赋值），写入侧注释风格照抄即可。
- viewer.spec.ts 现有单条大用例（SC1+SC3+D-24+D-10）；E2E 经 playwright webServer 自动先 build（本计划改 build.mjs 后 E2E 自动走注入路径，幂等）。
- 根 package.json 当前 version 0.1.8；版本推进规则"部署前 +1"（01-05 决策），本计划不动版本号，0.1.9 归 02-06。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build.mjs 构建期版本注入 + chaos 日志修正（G-02-3）</name>
  <files>packages/web-sdk/test/cache-bust-sync.test.ts, packages/web-sdk/build.mjs, packages/web-sdk/scripts/chaos-sc2.mjs, packages/server/public/index.html</files>
  <behavior>
    - Test 1（恒一致断言，RED→GREEN 主断言）：packages/server/public/index.html 中 pushhub.js script 标签的 ?v= 值 === 根 package.json version；且该引用在文件中恰出现一次（多标签漂移即失败）
    - Test 2（机制生效）：执行一次构建后断言 1 仍然成立（注入幂等，重复构建不产生偏差）
  </behavior>
  <action>
    第一步（RED）：新建 packages/web-sdk/test/cache-bust-sync.test.ts（vitest 默认 node 环境，无需 DOM docblock；用 node:fs readFileSync + node:path/node:url 定位文件——测试文件出发 ../../package.json 为根、../../server/public/index.html 为页面；正则提取 pushhub.js\?v=([0-9A-Za-z.\-]+) 的值）。当前 index.html 为 ?v=0.1.7 而根版本 0.1.8，测试必失败——RED 证据。
    提交：test(02-05): index.html ?v= 与根 version 恒一致断言（RED）。
    第二步（GREEN）：
    (a) build.mjs 在 copy 产物到 server/public 之后增加注入步骤：读根 package.json（resolve(pkgRoot, "../../package.json")）的 version 字段 → 读 packages/server/public/index.html → 将 pushhub.js?v=… 的参数值替换为根版本 → 写回。正则替换必须断言恰命中一次：命中 0 次即 console.error + process.exit(1)（防未来 index.html 重构静默丢失机制——构建失败比静默漂移安全）。注入成功输出一行日志（如 injected ?v=0.1.8 into server/public/index.html）。
    (b) 同步更新 index.html 中 ?v= 约定注释：由"升级 SDK 时同步改此值"的人工纪律措辞改为"构建期由 build.mjs 自动注入根 package.json version，勿手改"。
    (c) chaos-sc2.mjs viewer-online 日志行的硬编码 0.1.7 改为 ${EXPECT_VERSION} 插值（变量已在模块作用域，且脚本自身断言 rootVersion === EXPECT_VERSION，日志语义比原硬编码更准确）。
    (d) 执行 pnpm --filter @pushhub/web-sdk run build 一次：index.html 被注入为 ?v=0.1.8（当前根版本），cache-bust-sync.test.ts 转绿。构建产生的 pushhub.js 产物字节若因并行的 02-04 源码修复而变化，产物变更不在本任务提交——统一由 02-06 重建并提交（本任务 git 提交仅含 files 所列四个源文件）。
    跑 pnpm --filter @pushhub/web-sdk test 全绿后提交：feat(02-05): build.mjs 构建期注入根版本号 + chaos 日志去硬编码（G-02-3）。
    禁止事项：不得改用 CI 断言替代注入（UAT 裁决选机制化注入而非仅断言；断言测试是机制的双保险不是替代品）；不得在 build.mjs 引入新依赖（node 内置模块足够）；注入不得触碰 index.html 其它内容（scoped 正则替换，非全文件重写）。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/web-sdk run build && pnpm --filter @pushhub/web-sdk test</automated>
  </verify>
  <done>构建后 index.html 引用 ?v=0.1.8 与根 version 一致；替换恰命中一次否则构建失败；chaos-sc2.mjs 日志无硬编码版本；测试全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: viewer.js localStorage 读取侧防护（WR-03）</name>
  <files>packages/web-sdk/e2e/viewer.spec.ts, packages/server/public/viewer.js</files>
  <behavior>
    - Test 1（E2E，存储全禁场景，RED）：page.addInitScript 将 window.localStorage 属性重定义为抛 SecurityError 的 getter（模拟浏览器存储策略全禁）→ 打开查看器页无未捕获页面错误；#server-url 值回退为页面 origin（缺省值 window.location.origin，即 http://127.0.0.1:4911）；#channel-key 为空；#status-text 为"未连接"（无参数不自动连接，D-24 既有语义不变）
  </behavior>
  <action>
    第一步（RED）：viewer.spec.ts 新增独立 test 块（不动既有大用例）——addInitScript 在页面任何脚本之前运行（Playwright 语义天然满足"读取先于 viewer.js 执行被拦截"）；用 Object.defineProperty(window, "localStorage", { get() { throw new DOMException("denied", "SecurityError"); } }) 覆盖。断言用 expect(page.locator("#server-url")).toHaveValue(...) 与页面错误收集（page.on("pageerror") 记录后断言为空）。当前 viewer.js 读取侧无防护——脚本在预填行夭折，RED。
    提交：test(02-05): localStorage 全禁环境查看器加载 E2E（RED）。
    第二步（GREEN）：viewer.js 表单预填两行（serverInput.value / keyInput.value 赋值）包 try/catch——读取 window.localStorage.getItem 异常时回退：server 取 window.location.origin、key 取空串（与既有的 URL 参数优先级链一致：url 参数 || localStorage || origin 缺省）。注释风格对齐写入侧既有 try/catch（"localStorage 不可用（隐私模式等）——免填功能降级"）。
    跑 pnpm --filter @pushhub/web-sdk e2e 全绿（既有 6 例 + 新用例；E2E webServer 自动先 build，build.mjs 注入已随 Task 1 生效）后提交：feat(02-05): viewer localStorage 读取侧 try/catch（WR-03）。
    禁止事项：不得把预填逻辑抽成新函数重构（最小改动包裹即可）；不得吞掉其它异常源（try/catch 范围仅限两行读取赋值）。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/web-sdk e2e</automated>
  </verify>
  <done>存储被禁环境查看器正常加载：输入框回退缺省、无未捕获异常；E2E 全绿零回退。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器缓存 → 宿主页面 | stale 缓存可能在 SDK 升级后继续投放旧字节（旧消毒逻辑/旧协议行为） |
| 浏览器存储策略 → viewer.js | 存储全禁环境使 localStorage 访问抛异常（隐私模式、cookie 策略） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | 缓存参数 ?v= 机制 | medium | mitigate | 构建期自动注入根版本 + cache-bust-sync.test.ts 恒一致断言，消除 stale 缓存投放旧版 SDK 的机制性风险（G-02-3） |
| T-02-06 | DoS | viewer.js localStorage 读取 | low | mitigate | 读取侧 try/catch 对齐写入侧，存储全禁环境查看器不夭折、攻击样本等安全演示功能仍可用（WR-03） |
</threat_model>

<verification>
- pnpm --filter @pushhub/web-sdk run build 后：index.html ?v= 与根 version 一致（cache-bust-sync.test.ts 断言），注入日志可见。
- pnpm --filter @pushhub/web-sdk test 全绿（新增 cache-bust-sync 用例 + 既有零回退）。
- pnpm --filter @pushhub/web-sdk e2e 全绿（存储禁用新用例 + 既有 6 例零回退）。
</verification>

<success_criteria>
- G-02-3 truth 成立：?v= 与根 version 恒一致由机制保证，人工同步纪律作废；chaos 日志无硬编码版本。
- G-02-4 WR-03 成立：localStorage 读取防护落地并经真实浏览器 E2E 验证。
- 两个任务各自完成 test commit → feat commit 的 TDD 提交对。
</success_criteria>

<output>
Create .planning/phases/02-web-sdk/02-05-SUMMARY.md when done
</output>
