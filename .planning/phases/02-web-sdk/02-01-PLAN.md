---
phase: 02-web-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web-sdk/package.json
  - packages/web-sdk/tsconfig.json
  - packages/web-sdk/vitest.config.ts
  - packages/web-sdk/playwright.config.ts
  - packages/web-sdk/build.mjs
  - packages/web-sdk/src/pushhub.ts
  - packages/web-sdk/src/frames.ts
  - packages/web-sdk/src/dedup.ts
  - packages/web-sdk/src/render/render-markdown.ts
  - packages/web-sdk/src/entry-iife.cts
  - packages/web-sdk/test/render.test.ts
  - packages/web-sdk/test/frames.test.ts
  - packages/web-sdk/test/dedup.test.ts
  - packages/web-sdk/test/fixtures/attack-samples.json
  - packages/web-sdk/e2e/tracer.spec.ts
  - packages/server/public/pushhub.js
  - .gitignore
  - package.json
  - DEPLOY.md
autonomous: false
requirements: [WEB-01, WEB-02, WEB-04, WEB-05]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Playwright 真浏览器经 wrangler dev 静态资产加载 /pushhub.js，页面以两行接入（script src=/pushhub.js + new PushHub(serverUrl, channelKey)）后，POST /api/send 发出的消息在 2 秒内触发 on(\"message\") 回调且 text 逐字一致（SC1/WEB-01/WEB-02）"
    - "test/fixtures/attack-samples.json 全部攻击样本经 PushHub.renderMarkdown 输出：无 script 元素、无 on* 属性、无 javascript:/data: href；存活链接一律带 target=_blank 与 rel=\"noopener noreferrer\"（SC3/WEB-05，jsdom 单测 + 真浏览器 E2E 双层验证）"
    - "WS 意外关闭后 SDK 以 full jitter 指数退避（上限 60_000ms）自动重连，重连后消化 accept 即推的首拉 history（seq 去重）并发送 sync since=last_seq，宿主不会重复收到已见消息（SC2 基础路径/WEB-04；退避曲线与心跳死线的完整单测在 02-02）"
    - "生产 https://pushhub.dyun.org/pushhub.js 返回 200 且响应体与本地 dist/pushhub.js 字节一致（SC4 分发路径；资产命中不触发 Worker 的计费验证在 02-03）"
    - "心跳出站帧为字符串常量 PING = '{\"v\":1,\"type\":\"ping\"}'，逐字节等于 packages/server/src/chat-room.ts:44 服务端 setWebSocketAutoResponse 匹配串"
  artifacts:
    - "packages/web-sdk/package.json（@pushhub/web-sdk，type:module，scripts: build/test/typecheck/e2e）"
    - "packages/web-sdk/src/pushhub.ts（PushHub 类：四事件 + connect/disconnect/destroy + 静态 renderMarkdown）"
    - "packages/web-sdk/src/frames.ts（parseServerFrame 接收侧 guard）"
    - "packages/web-sdk/src/dedup.ts（shouldDeliver seq 去重窗口）"
    - "packages/web-sdk/src/render/render-markdown.ts（D-20 可移植纯 TS 渲染消毒模块）"
    - "packages/web-sdk/src/entry-iife.cts（esbuild IIFE 打包入口）"
    - "packages/web-sdk/build.mjs（打包 + 复制到 packages/server/public/pushhub.js + 体积报表 + 构建冒烟）"
    - "packages/web-sdk/test/{render,frames,dedup}.test.ts + test/fixtures/attack-samples.json"
    - "packages/web-sdk/e2e/tracer.spec.ts（真浏览器 × 真 wrangler dev 端到端 tracer 验证）"
    - "packages/server/public/pushhub.js（构建产物，gitignored）"
    - "DEPLOY.md 部署记录新增 0.1.5 行"
  key_links:
    - "build.mjs esbuild 产物 → 复制到 packages/server/public/pushhub.js → wrangler assets 直接命中 → E2E 页面 script 标签引用（此链断裂即 SC1/SC4 同时失效）"
    - "src/pushhub.ts PING 字符串常量 ↔ packages/server/src/chat-room.ts:44 PING_FRAME 字节级匹配（失配则心跳降级为唤醒 DO 的普通入站消息，烧请求额度）"
    - "src/frames.ts parseServerFrame ↔ @pushhub/shared 的 ServerFrame 类型与 PROTOCOL_VERSION 常量（任何复制定义都是协议漂移，违反 01-02 冻结契约）"
    - "根 package.json deploy 脚本链（web-sdk build → server deploy）↔ DEPLOY.md 版本 +1 流程（命令契约 pnpm run deploy 不变）"
  prohibitions:
    - statement: "SDK 运行时依赖恰为 marked、dompurify、@pushhub/shared 三项，不引入重连库与渲染框架（CLAUDE.md §Web SDK Do NOT use）"
      status: "flagged-unverified"
      verification: "pnpm --filter @pushhub/web-sdk list --depth 0 的 dependencies 段仅含上述三项"
    - statement: "产物仅 IIFE 单文件一种格式，不做 UMD/ESM 双格式打包"
      status: "flagged-unverified"
      verification: "build.mjs 恰含一次 esbuild 调用且 --format=iife，dist 目录仅 pushhub.js 一个产物"
    - statement: "src/render/ 模块仅依赖 marked 与 dompurify，不触碰 Node 专有模块（D-20 可移植约束，Phase 5 Tauri 前端直接 import 的前提）"
      status: "flagged-unverified"
      verification: "src/render/render-markdown.ts 的 import 语句恰为 marked 与 dompurify 两条"
    - statement: "消毒断言只跑 jsdom 与真浏览器两层，轻量 DOM 测试宿主不承载 DOMPurify 断言（RESEARCH Pitfall 3 实证双向失真）"
      status: "flagged-unverified"
      verification: "test/render.test.ts 首行 docblock 为 @vitest-environment jsdom，web-sdk 包 devDependencies 无 happy-dom"
    - statement: "消息内容进入 DOM 前必经 renderMarkdown 消毒管道（存储型 XSS 防线，CLAUDE.md 最高危项）"
      status: "flagged-unverified"
      verification: "SDK 源码中对 innerHTML 的赋值仅出现在测试与文档示例中，src/ 内赋值目标必为 renderMarkdown 返回值"
    - statement: "ping 帧以字符串常量直发，杜绝运行时对象序列化构造（键序反转即与服务端匹配串失配）"
      status: "flagged-unverified"
      verification: "src/pushhub.ts 含 const PING 字符串常量且 ws.send(PING) 直发"
    - statement: "帧类型与协议常量一律 import @pushhub/shared 单一来源，SDK 包内零复制定义"
      status: "flagged-unverified"
      verification: "grep -v '^//' 后在 packages/web-sdk/src 内搜 PROTOCOL_VERSION 的赋值形式（= 1）零匹配，仅 import 出现"
---

<objective>
Tracer 切片：交付端到端最小但生产可用的 pushhub.js——从冻结协议包到真浏览器真服务端收真消息并安全渲染的一条完整竖切片。

用户故事（自 ROADMAP Goal 直译）：**As a** 网页开发者，**I want to** 在任意页面引入单个 script 标签并 new PushHub(serverUrl, channelKey)，**so that** 零依赖零构建获得频道消息的实时接收与安全渲染能力。

Purpose: 这一条切片同时证明 4 条成功标准的路径（connect / receive / recover 基础 / sanitize / serve），并定型 D-16/D-18 锁定的 SDK 公开 API 表面（三端移植的接口契约，one-way 决策经 Task 2 checkpoint 定稿）。SDK 作为 @pushhub/shared 的第二个消费者（服务端之后），协议包身份模型不变，记录 decision: no-change。
Output: packages/web-sdk 包（源码 + 单测 + Playwright E2E）、构建产物挂载到 packages/server/public/pushhub.js、生产部署 0.1.5 + 冒烟。

研究开放问题落点（本计划内解决）：
1. D-25 单测 DOM 宿主偏差：采用 jsdom（非 happy-dom）承载消毒断言——研究实证 happy-dom 20.11.6 下 DOMPurify 双向失真（假阴性 + 假阳性），jsdom 全对；D-25 自标 reversible（测试栈组织可调），此偏差在授权范围内，据此落地。
2. Playwright Chromium 中国网络下载：安装命令带 PLAYWRIGHT_DOWNLOAD_HOST 镜像兜底。
3. setOffline 是否关闭已建立 WS（A1）：Task 3 E2E 内置 spike 用例，结论记入 SUMMARY 供 02-02 断连混沌用例消费。
4. D-16×D-17 交集语义定稿（按研究推荐，02-02 实现完整测试）：on("history") 载荷的 messages 永远只含宿主未见消息（帧结构 oldest_kept_seq/has_more 原样透传）；实时 message 帧经去重后逐条触发 on("message")。本计划 tracer 实现该语义的基础路径。
</objective>

<execution_context>
@C:/Users/31243/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/31243/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-web-sdk/02-CONTEXT.md
@.planning/phases/02-web-sdk/02-RESEARCH.md
@.planning/phases/02-web-sdk/02-PATTERNS.md

# 冻结协议唯一事实源（帧类型/常量必须 import，禁复制）
@packages/shared/src/index.ts
@packages/shared/src/validators.ts

# 服务端行为锚点（SDK 必须对齐的实现）
@packages/server/src/chat-room.ts
@packages/server/src/index.ts
@packages/server/wrangler.jsonc
@packages/server/public/index.html

# 工程环境
@packages/shared/package.json
@tsconfig.base.json
@scripts/smoke.mjs
@DEPLOY.md
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: 包合法性人工核验（[SUS] 全集，install 前置门）</name>
  <what-built>尚未安装任何包。RESEARCH.md 包合法性审计将本计划全部 5 个新包装态标为 [SUS]（too-new：最近 30 天内有发布）。判读为高频发布主流包的典型假阳性（周下载 1500 万~2.7 亿、canonical repo），但按协议仍须人工过目后才允许 install。</what-built>
  <how-to-verify>
    逐一访问 npmjs.com/package/{marked, dompurify, esbuild, jsdom, @playwright/test}，核对四点：
    1. Source repository 指向 canonical repo（markedjs/marked、cure53/DOMPurify、evanw/esbuild、jsdom/jsdom、microsoft/playwright）
    2. 周下载量级与研究记录一致（千万级）
    3. 最近发布为 patch/minor 前进而非可疑换主
    4. 无异常 install scripts（研究已审：esbuild 为标准平台二进制脚本且已在根 onlyBuiltDependencies 白名单；@playwright/test scripts 为空对象；其余三个无 postinstall）
    安装版本：marked@18.0.11、dompurify@3.4.14、esbuild@0.28.2、jsdom@30.0.1、@playwright/test@1.62.1（研究 registry 实查；marked/dompurify 为 CLAUDE.md §Web SDK 锁定选型）。vitest@4.1.11 与 typescript@7.0.2 已在 workspace 使用，随包 devDependencies 声明即可。
  </how-to-verify>
  <resume-signal>批准全部 5 个包输入 "approved"；对任一包有疑义则指出包名与疑义</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: SDK 公开 API 表面定稿（D-16/D-18 one-way 门）</name>
  <decision>冻结 pushhub.js 对外 API 表面。事件枚举与生命周期方法一经发布即为公开契约（Phase 5/6 按此移植、宿主页面按此编程），变更即破坏性。</decision>
  <context>
    D-16 锁定四事件、D-18 锁定构造即连与生命周期方法；status 枚举值与 error 载荷结构是 CONTEXT.md 留给规划阶段的 Discretion 项（研究 A4 建议：一旦发布即对外契约，定稿前值得用户过目）。
  </context>
  <options>
    <option id="approve-recommended">
      <name>按推荐值定稿</name>
      <pros>与冻结协议帧一一对应（D-16 无 SDK 私有加工）；构造签名与 WEB-01 文字完全一致（恰两参）；error 载荷三字段覆盖 fatal 语义（v!==1 断连不重连的载体）；status 四值覆盖 tracer 到 02-02 全部状态迁移</pros>
      <cons>无 off(name, cb) 精细解绑——SPA 细粒度场景需 destroy 重建实例</cons>
    </option>
    <option id="adjust-values">
      <name>调整枚举值或字段名</name>
      <pros>用户可改 status 枚举拼写（如 disconnected 代替 offline）或 error 载荷字段</pros>
      <cons>偏离研究 A4 建议值需同步改三端移植预期；拖延定稿时间</cons>
    </option>
    <option id="add-off-method">
      <name>增补 off(name, cb) 方法</name>
      <pros>细粒度解绑能力</pros>
      <cons>扩大 one-way 表面；当前场景 destroy() 已覆盖卸载需求，YAGNI</cons>
    </option>
  </options>
  <resume-signal>输入 approve-recommended / adjust-values（附具体值）/ add-off-method</resume-signal>
</task>

<task type="tracer">
  <name>Task 3: Tracer——packages/web-sdk 端到端最小切片（实现→构建→静态分发→真浏览器验证）</name>
  <files>
    packages/web-sdk/package.json, packages/web-sdk/tsconfig.json, packages/web-sdk/vitest.config.ts,
    packages/web-sdk/playwright.config.ts, packages/web-sdk/build.mjs,
    packages/web-sdk/src/pushhub.ts, packages/web-sdk/src/frames.ts, packages/web-sdk/src/dedup.ts,
    packages/web-sdk/src/render/render-markdown.ts, packages/web-sdk/src/entry-iife.cts,
    packages/web-sdk/test/render.test.ts, packages/web-sdk/test/frames.test.ts, packages/web-sdk/test/dedup.test.ts,
    packages/web-sdk/test/fixtures/attack-samples.json, packages/web-sdk/e2e/tracer.spec.ts,
    packages/server/public/pushhub.js, .gitignore
  </files>
  <precondition>Task 1 包合法性 checkpoint 已批准且 Task 2 已选定 API 表面；pnpm registry 可达（Phase 1 已多次成功安装，失败时换镜像源重试）。</precondition>
  <reversibility rating="one-way">公开 API 表面（D-16 事件枚举 / D-18 生命周期形态）随本任务进入产物；one-way 决策已由 Task 2 的 checkpoint:decision 前置把关。</reversibility>
  <read_first>
    - D:/AIworkspaces/PushHub/.planning/phases/02-web-sdk/02-RESEARCH.md（Pattern 1 esbuild .cts 实证、Pattern 2 渲染管道实证代码与攻击样本预期输出表、Pattern 3 重连序列、Pattern 4 Playwright 编排、Pitfalls 1-10）
    - D:/AIworkspaces/PushHub/.planning/phases/02-web-sdk/02-PATTERNS.md（每个文件的 analog 与行号锚点）
    - D:/AIworkspaces/PushHub/packages/shared/src/index.ts（ServerFrame 全帧类型 + PROTOCOL_VERSION/INITIAL_FETCH/SYNC_LIMIT_DEFAULT 常量，import 单一来源）
    - D:/AIworkspaces/PushHub/packages/shared/src/validators.ts（判别联合结果类型范式：ok/code/message）
    - D:/AIworkspaces/PushHub/packages/server/src/chat-room.ts（L44-45 PING_FRAME/PONG_FRAME 字面量、L192 auto-response 注册、L377 accept 即推首拉 sendHistory(server, null, undefined)、L425-462 sync keyset 语义）
    - D:/AIworkspaces/PushHub/packages/server/src/index.ts（L105-113 WS 路由与逐段 decodeURIComponent——SDK 侧必须 encodeURIComponent）
    - D:/AIworkspaces/PushHub/packages/shared/package.json（exports 映射 ./fixtures/*）
    - D:/AIworkspaces/PushHub/packages/shared/tsconfig.json 与 tsconfig.base.json（继承方式与 types:[] 写法）
    - D:/AIworkspaces/PushHub/packages/server/wrangler.jsonc（assets asset-first 配置；public/ 文件路径不得以 api 前缀开头）
    - D:/AIworkspaces/PushHub/scripts/smoke.mjs（L60-85 admin 建频道编排、L104-113 预挂监听时序、L51-57 Node 侧 fetch 发消息）
    - D:/AIworkspaces/PushHub/.gitignore（现状已含 dist/，需补产物条目）
  </read_first>
  <action>
    1. 安装（Task 1 已批准）：pnpm --filter 方式不可用于尚不存在的包——先建 package.json 再 pnpm install。pnpm --filter @pushhub/web-sdk add marked dompurify 与 add -D esbuild jsdom vitest typescript @playwright/test（版本按 Task 1 核验值）。Chromium 二进制：pnpm --filter @pushhub/web-sdk exec playwright install chromium；下载失败时以 PLAYWRIGHT_DOWNLOAD_HOST=https://npmmirror.com/mirrors/playwright/ 环境变量重试（研究开放问题 4 兜底）。
    2. 包骨架：package.json name @pushhub/web-sdk、private true、type module、dependencies 含 @pushhub/shared workspace:*、scripts 对齐 server 命名（typecheck: tsc -p tsconfig.json、test: vitest run、build: node build.mjs、e2e: playwright test）。tsconfig 继承 ../../tsconfig.base.json，compilerOptions types 设空数组防 Node 类型泄漏进 SDK 源码（可移植性），include 覆盖 src/**/*.ts、src/**/*.cts、test/**/*.ts。vitest.config.ts 默认 node 环境（状态机类测试快），render 测试文件首行 docblock 切 jsdom——本条即 D-25 偏差落地：消毒断言宿主用 jsdom，理由见 objective 开放问题 1。
    3. src/frames.ts：parseServerFrame(raw) 接收侧 guard，返回判别联合（ok:true 带 ServerFrame / ok:false 带 fatal 布尔与 message）。规则：JSON.parse 失败非致命丢弃；帧对象 v 字段 !== PROTOCOL_VERSION（import 自 @pushhub/shared）为 fatal（D-07 客户端侧职责：断连报错不重连）；type 字段匹配 message/history/pong/error 四值放行（未知字段一律忽略，D-07 前瞻兼容）；未知 type 非致命丢弃。实现照抄 RESEARCH Code Examples 骨架，结构深校验按 validators.ts 手写 guard 风格补 message/history 分支的字段类型检查。文件头注释声明决策编号与职责边界（仓库文档惯例）。
    4. src/dedup.ts：SeqDedup 类（或 shouldDeliver 函数 + 内部状态）：seen 为 Set、lastSeq 取 max；重复 seq 返回 false 不投递；每次投递后把小于 lastSeq 减去 1000（DEDUP_WINDOW，对齐服务端 RETENTION_KEEP=500 + 首拉 50 的交叠上界再留冗余）的旧 seq 从 Set 裁剪，内存有界（D-17）。
    5. src/render/render-markdown.ts（D-20 可移植纯 TS 模块，文件头注释写明"pushhub.js 与 Phase 5 Tauri 前端共用，环境假设仅 window/document"）：marked.use 配 gfm 与 breaks（聊天语义）；DOMPurify 以默认导出工厂创建（惰性单例，全局 window 缺失时跳过初始化并直接返回转义降级）；注册 afterSanitizeAttributes hook——A 元素一律 setAttribute target=_blank 与 rel=noopener noreferrer（D-21，防反向 tabnabbing）；返回 purify.sanitize(marked.parse(text))。实现体照抄 RESEARCH Pattern 2 实证代码。导出 renderMarkdown(text: string): string 纯函数。
    6. src/pushhub.ts：PushHub 类实现 Task 2 定稿的 API 表面。构造函数恰两参 (serverUrl: string, channelKey: string)，构造即发起连接（D-18）。on(name, cb) 支持四事件（D-16）：message（实时帧逐条，载荷为 MessageFrame 语义对象）、history（载荷为 HistoryFrame，messages 已按去重窗口过滤——D-16×D-17 交集语义基础路径）、status（Task 2 定稿枚举）、error（Task 2 定稿载荷）。connect/disconnect/destroy：disconnect 主动关闭并停重连可再 connect；destroy 等于 disconnect 加清空全部监听与定时器。内部：WS URL 以 serverUrl 替换前缀 http 为 ws、去尾部斜杠、拼 /api/ws/ 加 encodeURIComponent(channelKey)（Pitfall 7）；出站心跳用字符串常量 const PING 逐字节等于 '{"v":1,"type":"ping"}' 并以 30 秒间隔直发（Pitfall 4：运行时对象序列化会键序反转导致 auto-response 失配）；WS_OPEN 后收首拉 history（服务端 L377 无条件推送）→ messages 过滤去重 → 随后发送 {"v":1,"type":"sync","since":lastSeq,"limit":200}（SYNC_LIMIT_DEFAULT import 自 shared；Pitfall 5：首拉只覆盖 50 条，sync 是必须步骤）；意外 close（非用户主动）→ full jitter 退避重连：delay 取随机数乘 min(60_000, 500 乘 2 的 attempt 次方)（cap 60s 已锁，base 500ms 为 Discretion 建议值）；v!==1 fatal 帧 → error(fatal) + 断连且不再重连（Pitfall 10 方向：服务端宽容客户端严格）。错误事件载荷绝不包含 channelKey 子串（密钥不进日志惯例，chat-room.ts/index.ts 同款纪律）。静态方法 static renderMarkdown = renderMarkdown（D-19：纯函数辅助，SDK 无 UI 无 DOM 所有权）。
    7. src/entry-iife.cts：恰两行——import { PushHub } 后 module.exports = PushHub（Pitfall 1/2 实证：ESM 默认导出会产出 {default:X} 全局、type:module 包内 .ts 写 module.exports 运行时抛错；.cts 是唯一正确形态）。
    8. build.mjs：esbuild exec 调用 src/entry-iife.cts，参数 --bundle --minify --format=iife --global-name=PushHub --outfile=dist/pushhub.js；复制产物到 packages/server/public/pushhub.js；打印 min/gzip 字节数（zlib gzipSync）；构建冒烟：产物末尾加载断言（node vm 或 Function 构造执行）typeof PushHub 为 function，min 体积超 120KB 打报警。.gitignore 追加一行 packages/server/public/pushhub.js（dist/ 已覆盖包内产物目录）。
    9. test/fixtures/attack-samples.json：固化 RESEARCH Pattern 2 实证预期输出表全部 8 行（script 标签残留、img onerror、javascript: 链接、data: URL 链接、带 onclick 的合法锚、svg onload、iframe、正常 Markdown 对照组）为输入/期望输出对数组。
    10. test/render.test.ts（首行 docblock 切 jsdom）：逐条喂 attack-samples.json，断言输出经 DOMParser 或 container innerHTML 解析后：无 script 元素节点、无以 on 开头的属性、无 javascript: 或 data: 协议 href、A 元素均带 rel 含 noopener noreferrer 与 target _blank（SC3 断言基线）。
    11. test/frames.test.ts（node 环境）：静态 import @pushhub/shared/fixtures/ 下正反例 JSON（history-frame.positive/message-frame.positive/pong-frame.positive/ws-error-frame 等全部 12 个），逐条喂 parseServerFrame：正例 ok true 且未知字段（如 _note）被忽略、反例按 fatal/丢弃两档正确分流。
    12. test/dedup.test.ts（node 环境）：首次 seq 投递 true、重复 seq false、乱序到达 lastSeq 取 max、超窗口旧 seq 被裁剪后 Set 尺寸有界。
    13. playwright.config.ts：testDir e2e、baseURL http://127.0.0.1:4911（Pitfall 6：禁 localhost 防 IPv6 绕道）、webServer.command 前置构建再起服务（pnpm --filter @pushhub/web-sdk run build 然后 pnpm --filter @pushhub/server exec wrangler dev --port 4911 --ip 127.0.0.1 --var ADMIN_KEY:e2e-admin-key）、url 轮询 http://127.0.0.1:4911/、reuseExistingServer true、timeout 60_000（D-26：本地真 DO/真 KV/真 WS，不触生产额度；--var 只存在于本地 dev 进程）。
    14. e2e/tracer.spec.ts：setup 用 playwright request POST /api/admin/channels（Bearer e2e-admin-key，body { name: 含时间戳唯一串 }）拿 channelKey/sendKey（正则 phc_/phs_ 断言，对齐 smoke.mjs:80-81）。测试体 page.setContent 两行接入——HTML 含 script src=http://127.0.0.1:4911/pushhub.js 与 new PushHub("http://127.0.0.1:4911", key)，页面内 window 收集事件数组；断言：status 到达 online（waitForFunction 轮询）；Node 侧 fetch POST /api/send（Bearer sendKey，body 含 title 与 text）后页面 message 事件在 2 秒内出现且 text 逐字一致（SC1 端到端，延迟对齐验收线）；页面内调用 PushHub.renderMarkdown 攻击样本并将返回值经 innerHTML 写入容器 div，断言容器 querySelector script 为 null、无 on* 属性、锚元素带 rel noopener noreferrer（SC3 真浏览器层）。附 spike 用例：context.setOffline(true) 后观察页面 status 是否在数秒内迁移到重连态（记录 WS 是否被 Chromium 关闭——研究 A1 结论写入 SUMMARY，02-02 断连混沌消费；此用例只观察不断言成败）。所有断言注意 attach-before-trigger 时序（smoke.mjs:104-113 教训：即发即弃首帧必须预挂监听）。
  </action>
  <verify>
    <automated>
      cd D:/AIworkspaces/PushHub 后依次执行并全部退出 0：
      1. pnpm --filter @pushhub/web-sdk run typecheck
      2. pnpm --filter @pushhub/web-sdk run build（输出含 min/gzip 体积报表与 PushHub 构建冒烟通过；packages/server/public/pushhub.js 存在）
      3. pnpm --filter @pushhub/web-sdk test（render/frames/dedup 全绿）
      4. pnpm --filter @pushhub/web-sdk run e2e（tracer.spec 全绿，spike 用例输出结论）
      5. pnpm --filter @pushhub/server test（服务端回归不受影响，60 例基线）
    </automated>
  </verify>
  <acceptance_criteria>
    - packages/web-sdk/package.json 存在且 dependencies 恰含 marked、dompurify、@pushhub/shared；devDependencies 含 esbuild、jsdom、vitest、typescript、@playwright/test（D-25 偏差：jsdom 在列，轻量 DOM 宿主不在列）
    - packages/web-sdk/src/entry-iife.cts 扩展名为 .cts 且内容为 import PushHub 与 module.exports = PushHub（RESEARCH Pitfall 1/2 实证唯一正确形态）
    - src/pushhub.ts 含 const PING 字符串常量，值逐字节为 {"v":1,"type":"ping"}（与 packages/server/src/chat-room.ts:44 PING_FRAME 相同）
    - src/frames.ts 从 @pushhub/shared import PROTOCOL_VERSION 与 ServerFrame 类型（包内零复制定义）
    - src/render/render-markdown.ts 的 import 恰为 marked 与 dompurify 两条（D-20），且注册 afterSanitizeAttributes hook 写入 target=_blank 与 rel=noopener noreferrer（D-21）
    - pnpm --filter @pushhub/web-sdk run build 后 packages/server/public/pushhub.js 存在，node 加载产物断言 typeof PushHub 为 function，min 体积小于 120000 字节
    - test/render.test.ts 首行 docblock 为 @vitest-environment jsdom；attack-samples.json 覆盖 RESEARCH 实证表全部 8 类样本
    - pnpm --filter @pushhub/web-sdk run e2e 通过：真浏览器两行接入后 on("message") 在 POST /api/send 后 2000ms 内收到 text 一致的消息；攻击样本渲染容器无 script 元素与 on* 属性
    - .gitignore 含 packages/server/public/pushhub.js 条目
    - SUMMARY 记录 setOffline spike 结论（关闭/不关闭已建立 WS）与 min/gzip 实测体积
  </acceptance_criteria>
  <done>单测 + 构建 + E2E 全绿；构建产物经 wrangler dev 静态资产在真浏览器两行接入收真消息且攻击样本渲染无害；spike 结论与体积数据记入 SUMMARY。</done>
</task>

<task type="auto">
  <name>Task 4: 构建流水线接线 + 部署 0.1.5 + 生产冒烟</name>
  <files>package.json, DEPLOY.md</files>
  <precondition>Task 3 全绿；wrangler 已登录（OAuth，Phase 1 前置已做）；.claude/admin.key 存在（PH_ADMIN_KEY 来源，勿入库）；https://pushhub.dyun.org 可达（自定义域名——workers.dev 存在 SNI 阻断与 DNS 污染，STATE.md 已记录）。</precondition>
  <read_first>
    - D:/AIworkspaces/PushHub/DEPLOY.md（版本 +1 规约：部署前补丁位 +1、部署记录表格式、smoke 命令形态）
    - D:/AIworkspaces/PushHub/package.json（root scripts 现状与 version 0.1.4）
    - D:/AIworkspaces/PushHub/scripts/smoke.mjs（smoke 覆盖范围参照）
  </read_first>
  <action>
    1. 根 package.json：version 0.1.4 改 0.1.5（部署前 +1 规约）；deploy 脚本改为先构建后部署的链式（pnpm --filter @pushhub/web-sdk run build 后接 pnpm --filter @pushhub/server run deploy）——pnpm run deploy 命令契约不变（DEPLOY.md 流程不受影响，RESEARCH Pattern 5 方案 A：产物不进 git，链式构建消灭旧产物部署类 bug）；test 脚本改为 server 与 web-sdk 两包串联。
    2. pnpm run deploy（必须带 run——pnpm 拦截裸 deploy；01-05 既定纪律）。
    3. 生产冒烟：PH_SMOKE_URL=https://pushhub.dyun.org 与 PH_ADMIN_KEY 取 .claude/admin.key 内容，node scripts/smoke.mjs，预期输出 SMOKE OK（覆盖建频道/发消息/WS 实收/断连补拉/401 与 413 反例）。
    4. SC4 生产前半验证：node fetch https://pushhub.dyun.org/pushhub.js 断言 200、content-type 为 JavaScript、响应字节数与本地 dist/pushhub.js 一致（node fs 比对）。
    5. DEPLOY.md 部署记录表新增 0.1.5 行（版本、时间、URL、Worker Version ID、冒烟结果与 pushhub.js 体积）。
  </action>
  <verify>
    <automated>
      cd D:/AIworkspaces/PushHub 后：
      1. node -e 断言 https://pushhub.dyun.org/pushhub.js 返回 200 且 body 与 dist/pushhub.js 字节一致
      2. PH_SMOKE_URL=https://pushhub.dyun.org PH_ADMIN_KEY=$(cat .claude/admin.key) node scripts/smoke.mjs 输出 SMOKE OK
      3. git diff 检查 DEPLOY.md 新增 0.1.5 行
    </automated>
    <human-check>Cloudflare dashboard → Workers & Pages → pushhub → Durable Objects → Duration：冒烟频道空闲挂 WS 数分钟后 duration 平直不增（D-15 ④ 固定人工项，Hibernation 生效回归）。</human-check>
  </verify>
  <acceptance_criteria>
    - 根 package.json version 为 0.1.5；deploy 脚本为先 web-sdk build 后 server deploy 的链式；test 脚本串联两包
    - pnpm run deploy 退出 0，wrangler 输出新 Worker Version ID
    - https://pushhub.dyun.org/pushhub.js 可访问（200）且字节与本地构建产物一致（SC4 分发路径打通）
    - smoke.mjs 输出 SMOKE OK 且端到端延迟低于 2000ms
    - DEPLOY.md 部署记录含 0.1.5 行
  </acceptance_criteria>
  <done>pushhub.js 在生产域名可用，服务端回归冒烟通过，版本 0.1.5 登记在案；SC1/SC3 的生产可复现基础就绪。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook 发送方 → 服务端 → SDK | 不可信 Markdown 消息内容最终进入 SDK 渲染管道与宿主页面 DOM（本阶段主战场） |
| npm registry → 构建链 | marked/dompurify/esbuild/jsdom/@playwright/test 五个新包进入供应链 |
| SDK → 宿主页面 | SDK 代码在宿主页面权限内运行，错误载荷与日志是信息披露面 |
| 服务端 → SDK | 伪造/混淆协议帧（v/type 篡改）经 WS 入站 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm 安装（5 个 [SUS] too-new 包） | high | mitigate | Task 1 blocking-human 包合法性核验（npmjs.com canonical repo/下载量/install scripts 四点核对）后才允许 install；esbuild 平台脚本已在根 onlyBuiltDependencies 白名单 |
| T-02-01 | Tampering + Elevation of Privilege | render-markdown.ts / 消息 Markdown 渲染（存储型 XSS：script 注入、img onerror、javascript:/data: href） | critical | mitigate | marked→DOMPurify 消毒管道（D-20）+ attack-samples.json fixtures jsdom 回归 + 真浏览器 E2E DOM 断言（SC3 双层）；消毒断言宿主用 jsdom（轻量宿主实证失真）；ASVS V5 输入校验同源 LIMITS 常量 |
| T-02-02 | Spoofing | render-markdown.ts 链接输出（反向 tabnabbing：target=_blank 窃取 window.opener） | medium | mitigate | D-21 afterSanitizeAttributes hook 强制 rel=noopener noreferrer + target=_blank；attack-samples 断言锚元素属性（实证生效） |
| T-02-04 | Tampering | src/frames.ts（协议混淆帧：伪造 v/type） | medium | mitigate | parseServerFrame guard：v !== PROTOCOL_VERSION 判 fatal 断连不重连（D-07 客户端侧职责）、未知 type 丢弃、未知字段忽略；完整 fatal 路径单测在 02-02 |
| T-02-05 | Information Disclosure | pushhub.ts error 事件载荷（Channel Key 泄进错误消息） | medium | mitigate | 错误载荷仅 message/code/fatal 三字段，实现与测试双断言 key 子串不出现；demo 页 localStorage 存 key 是 D-24 用户自担取舍（02-03 文档注明） |
</threat_model>

<verification>
- pnpm --filter @pushhub/web-sdk run typecheck && run build && test 全绿（本计划提交前）
- pnpm --filter @pushhub/web-sdk run e2e：真浏览器 × 真 wrangler dev（真 DO/真 KV/真 WS，D-26）两行接入收真消息 + 攻击样本渲染无害
- pnpm --filter @pushhub/server test 回归 60 例基线不回退
- 生产 0.1.5：smoke.mjs SMOKE OK + /pushhub.js 200 且字节一致
- setOffline spike 结论与 min/gzip 体积写入 02-01-SUMMARY.md（02-02 消费）
</verification>

<success_criteria>
- SC1 路径证明：真浏览器两行接入即收实时消息（E2E 绿）
- SC3 路径证明：全部攻击样本双层（jsdom + 真浏览器）消毒断言绿
- SC2 基础路径证明：意外断连自动重连 + 去重 + sync 补拉（E2E 可观察）
- SC4 分发路径证明：生产 /pushhub.js 200 且与本地构建字节一致
- SDK 公开 API 表面（D-16/D-18）定稿并进入产物
</success_criteria>

## Artifacts this phase produces

（本计划创建的部分；02-02/02-03 增补）

- 全局导出：window.PushHub 类（IIFE 单文件 dist/pushhub.js）
- 公开 API：constructor(serverUrl, channelKey) 构造即连（D-18）；on("message"|"history"|"status"|"error", cb)（D-16）；connect()/disconnect()/destroy()；静态 PushHub.renderMarkdown(text): string（D-19）
- 事件载荷：message → MessageFrame 语义对象；history → HistoryFrame（messages 只含未见消息，oldest_kept_seq/has_more 原样）；status → Task 2 定稿枚举；error → { message, code?, fatal? }
- 内部模块：parseServerFrame（frames.ts）、SeqDedup/shouldDeliver（dedup.ts）、renderMarkdown（render/render-markdown.ts，D-20 可移植）
- 常量：PING 字符串 '{"v":1,"type":"ping"}'、BACKOFF_CAP_MS=60_000、BACKOFF_BASE_MS=500、DEDUP_WINDOW=1000、心跳间隔 30_000ms
- 文件：packages/web-sdk/{package.json, tsconfig.json, vitest.config.ts, playwright.config.ts, build.mjs, src/*, test/*, e2e/tracer.spec.ts}
- npm scripts：@pushhub/web-sdk 的 build/test/typecheck/e2e；根 test（两包串联）与 deploy（先 build 后 deploy 链式）
- 静态资产：packages/server/public/pushhub.js（构建产物，gitignored）
- 部署：0.1.5 + DEPLOY.md 记录行

<output>
Create .planning/phases/02-web-sdk/02-01-SUMMARY.md when done
</output>
