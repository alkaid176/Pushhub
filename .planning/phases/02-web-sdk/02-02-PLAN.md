---
phase: 02-web-sdk
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/web-sdk/src/connection-machine.ts
  - packages/web-sdk/src/pushhub.ts
  - packages/web-sdk/src/frames.ts
  - packages/web-sdk/test/machine-backoff.test.ts
  - packages/web-sdk/test/machine-fatal.test.ts
  - packages/web-sdk/test/machine-events.test.ts
  - packages/web-sdk/test/history-filter.test.ts
  - packages/web-sdk/test/dedup.test.ts
  - packages/web-sdk/e2e/reconnect.spec.ts
  - package.json
  - DEPLOY.md
autonomous: true
requirements: [WEB-04, WEB-02]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "fake timers 单测证明连续 30 次重连尝试的全部退避 delay 位于 [0, min(60_000, 500*2^attempt)] 区间（full jitter，cap 60s 锁定——SC2/WEB-04）"
    - "收到 v !== 1 的帧后：on(\"error\") 收到 fatal 载荷、status 迁移 offline、之后任何定时器触发都不再创建 socket（D-07 客户端严格方向）"
    - "心跳死线与探活：每 30s 发 PING、pong 10s 死线超时强制重连；visibilitychange 到 visible 立即探活（5s 死线），超时判死线重连续补拉（D-27，iOS 冻结恢复路径——逻辑完备不依赖真机）"
    - "大于 50 条的离线缺口经 has_more 翻页全部补齐：E2E 断连期间发 55+5 条，恢复后 60 条全到、零重复（Pitfall 5：只吃首拉不翻页会静默丢中间段）"
    - "on(\"history\") 载荷的 messages 永远只含宿主未见消息，oldest_kept_seq 与 has_more 原样透传（D-16×D-17 交集语义，02-01 开放问题 2 定稿）——单测喂交叠批次断言"
    - "setOffline（或 02-01 spike 选定的断连手段）混沌后 SDK 自动重连续补拉，宿主页面无感知（SC2 E2E）"
    - "生产 0.1.6 部署后 smoke.mjs 输出 SMOKE OK"
  artifacts:
    - "packages/web-sdk/src/connection-machine.ts（纯状态机：输入事件流 → 输出动作流，无 DOM 无 WebSocket——Phase 5 Tauri 移植同构参考）"
    - "packages/web-sdk/test/machine-backoff.test.ts / machine-fatal.test.ts / machine-events.test.ts / history-filter.test.ts（node 环境 + fake timers）"
    - "packages/web-sdk/test/dedup.test.ts 增补窗口裁剪与交叠用例"
    - "packages/web-sdk/e2e/reconnect.spec.ts（断连混沌 + 大缺口翻页补拉）"
    - "DEPLOY.md 部署记录新增 0.1.6 行"
  key_links:
    - "connection-machine 状态迁移（idle→connecting→online⇄reconnecting→offline）↔ pushhub.ts adapter 的 WS/定时器/DOM 事件接线（状态机纯逻辑脱离 DOM 全覆盖，adapter 薄）"
    - "history 过滤 ↔ SeqDedup.shouldDeliver（D-16×D-17 交集的唯一实现点——三端移植照此）"
    - "visibilitychange 监听 ↔ 探活 ping 死线（D-27：页面回前台 → 立即 ping → 5s 无 pong → 强制重连 + sync 补拉）"
    - "has_more 翻页循环 ↔ 服务端 chat-room.ts L425-462 sync keyset 语义（多取 1 条判 has_more；本批最大 seq 为新 since；连续 100 次上限防异常死循环）"
  prohibitions:
    - statement: "状态机模块不 import WebSocket/DOM/Node 类型——纯输入输出流（可移植与全覆盖的前提）"
      status: "flagged-unverified"
      verification: "connection-machine.ts 的 import 段为空或仅类型 import，且函数签名只出现自定义事件/动作类型"
    - statement: "补拉循环有硬上限（SYNC_PAGE_MAX=100），服务端异常返回循环 has_more 时不会无限翻页"
      status: "flagged-unverified"
      verification: "machine 或 pushhub 源码含上限常量且超限路径 emit error 事件（单测覆盖）"
    - statement: "宿主回调在重连/补拉任何路径下不会收到重复 seq 的 message 或 history.messages 项（D-17 宿主永不见重复）"
      status: "flagged-unverified"
      verification: "history-filter.test.ts 与 e2e/reconnect.spec.ts 断言收到的 seq 序列无重复元素"
---

<objective>
重连韧性与协议纵深完整化：把 02-01 tracer 的基础恢复路径扩成完整 SC2 能力——full jitter 退避曲线、心跳 pong 死线、visibilitychange 探活（D-27）、v!==1 fatal 断连不重连、has_more 翻页补拉、D-16×D-17 交集语义的完整实现与测试，并以断连混沌 E2E 与生产部署 0.1.6 收口。

Purpose: SC2（部署断连后宿主无感自动重连续补拉）是四条成功标准中最依赖边界行为的一条；服务端每次部署即全量断连，SDK 必须把这个事件变成宿主页面无感知的例行恢复。纯状态机抽取同时是 Phase 5 Tauri 移植的同构参考（CLAUDE.md §Testing Desktop 既定思路）。
Output: connection-machine.ts 纯状态机 + 四组单测 + reconnect.spec.ts 混沌 E2E + 0.1.6 部署冒烟。

D-16×D-17 交集语义（02-01 开放问题 2 已定稿，本计划实现并测试）：history 帧照原样触发 on("history")（帧类型/字段/oldest_kept_seq/has_more 不做 SDK 私有加工，D-16），其中 messages 数组按去重窗口过滤——"on(\"history\") 载荷的 messages 永远只含宿主未见消息"；实时 message 帧经 shouldDeliver 过滤后逐条触发 on("message")。这是两决策唯一自洽的交集，02-03 的 README 将把该措辞写进 API 文档。
</objective>

<execution_context>
@C:/Users/31243/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/31243/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-web-sdk/02-CONTEXT.md
@.planning/phases/02-web-sdk/02-RESEARCH.md

# 02-01 产物（API 表面与既有实现——本计划在其上扩展）
@.planning/phases/02-web-sdk/02-01-SUMMARY.md
@packages/web-sdk/src/pushhub.ts
@packages/web-sdk/src/frames.ts
@packages/web-sdk/src/dedup.ts

# 服务端 sync 语义锚点
@packages/server/src/chat-room.ts
@packages/shared/src/index.ts
@DEPLOY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 纯状态机抽取 + 退避/fatal/事件映射单测（fake timers）</name>
  <files>packages/web-sdk/src/connection-machine.ts, packages/web-sdk/src/pushhub.ts, packages/web-sdk/test/machine-backoff.test.ts, packages/web-sdk/test/machine-fatal.test.ts, packages/web-sdk/test/machine-events.test.ts</files>
  <precondition>02-01 已完成（pushhub.ts 基础实现与测试基线在位）。</precondition>
  <reversibility rating="costly">去重与恢复语义进 SDK 行为契约（D-17 标 costly），三端移植需一致实现——本任务把语义固化进可测纯逻辑。</reversibility>
  <read_first>
    - D:/AIworkspaces/PushHub/packages/web-sdk/src/pushhub.ts（现有内联重连/去重逻辑——抽取对象）
    - D:/AIworkspaces/PushHub/packages/web-sdk/src/frames.ts（parseServerFrame 三态结果——FRAME 输入的来源）
    - D:/AIworkspaces/PushHub/packages/web-sdk/src/dedup.ts（shouldDeliver——状态机输出 emit 动作的判定依赖）
    - D:/AIworkspaces/PushHub/.planning/phases/02-web-sdk/02-RESEARCH.md（Pattern 3：输入/输出事件全集设计、full jitter 公式、状态迁移图）
    - D:/AIworkspaces/PushHub/packages/server/src/chat-room.ts（L44-45 心跳字面量、L377 accept 即推首拉——重连确定序列的服务端依据）
    - D:/AIworkspaces/PushHub/packages/server/test/ws-fanout.test.ts 或 sync-catchup.test.ts（fake 时序测试的仓库风格参照）
  </read_first>
  <behavior>
    - machine-backoff.test.ts：createMachine 后连续喂 30 次 WS_CLOSE 事件，收集全部 schedule 动作的 delayMs，断言每个都大于等于 0 且小于等于 min(60_000, 500 * 2^attempt)；cap 锁定 60_000（SC2）
    - machine-fatal.test.ts：喂 FRAME(v=2) 后，断言输出含 emitError(fatal=true) 与 emitStatus("offline")；随后喂任意 TIMER 事件，断言不再输出 createSocket（不重连）
    - machine-events.test.ts：WS_OPEN 后喂 history 帧（messages 数组）→ 输出 emitHistory（messages 过滤后）+ sendSync(since=lastSeq)；喂 message 帧 → 输出 emitMessage（未见 seq）；重复 seq 的 message 帧 → 无 emitMessage 输出；喂 pong 帧 → 心跳死线定时器重置
  </behavior>
  <action>
    1. 先写三个测试文件（RED：connection-machine 尚不存在，typecheck/test 失败即红）。
    2. 新建 src/connection-machine.ts：导出 createMachine()，内部状态覆盖 idle/connecting/online/reconnecting/offline（status 枚举与 02-01 Task 2 定稿一致）。输入事件：CONNECT/DISCONNECT/DESTROY/WS_OPEN/WS_CLOSE/FRAME(kind,payload)/VISIBILITY(visible|hidden)/TIMER(kind)。输出动作：createSocket(url)/closeSocket()/sendPing()/sendSync(since,limit)/schedule(kind,delayMs)/cancel(kind)/emitStatus(s)/emitMessage(m)/emitHistory(batch)/emitError(e)。退避用 full jitter：delay = Math.random() * Math.min(60_000, 500 * 2 ** attempt)。模块保持纯逻辑：零 WebSocket/DOM/Node 依赖，随机数与时间经由可注入参数（默认 Math.random）以便测试确定性（D-17 语义载体）。
    3. 重构 src/pushhub.ts 为 adapter：WS onopen/onclose/onmessage、setInterval 心跳、visibilitychange 监听全部翻译为 machine.input 事件；machine 输出动作映射到真实 WebSocket/定时器/DOM 操作与 on 回调发射。02-01 既有公开 API 与事件语义不变（行为等价重构，02-01 测试保持绿）。重连确定序列固化：WS_OPEN → 吃首拉 history（messages 经 SeqDedup.shouldDeliver 过滤进 emitHistory）→ 无条件 sendSync(since=lastSeq, limit=200)（SYNC_LIMIT_DEFAULT）→ 收 history 且 has_more 为 true 则以本批最大 seq 续翻，连续翻页达 100 次（SYNC_PAGE_MAX）放弃并 emitError（防服务端异常死循环）。
    4. 跑单测至全绿（GREEN），commit。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/web-sdk test 与 pnpm --filter @pushhub/web-sdk run typecheck 全部退出 0（含 02-01 既有 render/frames/dedup 基线不回退）</automated>
  </verify>
  <acceptance_criteria>
    - packages/web-sdk/src/connection-machine.ts 存在且导出 createMachine；模块内零 WebSocket/window/document 引用（纯逻辑，Phase 5 移植参考）
    - machine-backoff.test.ts 断言 30 次退避 delay 全部落在 [0, min(60_000, 500*2^attempt)] 且上限恰为 60_000
    - machine-fatal.test.ts 断言 v!==1 → fatal error + offline + 后续 TIMER 不再 createSocket（D-07 客户端严格方向，Pitfall 10）
    - machine-events.test.ts 覆盖 history 过滤投递、sendSync 触发、message 去重、pong 重置死线四类映射
    - pushhub.ts 重构后 02-01 的 e2e/tracer.spec.ts 仍绿（pnpm --filter @pushhub/web-sdk run e2e -- e2e/tracer.spec.ts）
  </acceptance_criteria>
  <done>连接生命周期全部语义（退避曲线、fatal、事件映射）在纯状态机层单测全覆盖，adapter 重构行为等价。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 心跳死线 + visibilitychange 探活（D-27）+ 交集语义完整测试</name>
  <files>packages/web-sdk/src/pushhub.ts, packages/web-sdk/src/connection-machine.ts, packages/web-sdk/test/history-filter.test.ts, packages/web-sdk/test/dedup.test.ts</files>
  <read_first>
    - D:/AIworkspaces/PushHub/packages/web-sdk/src/connection-machine.ts（Task 1 产物——本任务扩展 TIMER/VISIBILITY 语义）
    - D:/AIworkspaces/PushHub/packages/web-sdk/src/pushhub.ts（adapter 接线点：定时器与 document.visibilitychange）
    - D:/AIworkspaces/PushHub/packages/shared/src/index.ts（HistoryFrame/oldest_kept_seq 字段定义——交集语义断言对象）
    - D:/AIworkspaces/PushHub/packages/server/src/chat-room.ts（L192 auto-response：pong 由服务端零唤醒自动回——SDK 死线判定的对端行为）
  </read_first>
  <behavior>
    - 心跳死线：online 状态下每 30_000ms 输出 sendPing；喂 PONG 后 pong 死线定时器重置；连续喂 TIMER(pong_deadline) 而无 pong → 输出 closeSocket + 进入 reconnecting
    - 探活（D-27）：喂 VISIBILITY(visible) → 立即输出 sendPing + schedule(probe, 5_000)；5s 内收到 pong → 取消探活无重连；超时 → 强制重连续补拉（closeSocket + reconnecting）
    - 交集语义（history-filter.test.ts）：预置已见 seq {1..30}，喂 history 批次含 seq {20..50} → emitHistory 载荷 messages 恰为 {31..50}，且 oldest_kept_seq 与 has_more 与输入帧完全一致（"messages 永远只含宿主未见消息"）
    - dedup 窗口（dedup.test.ts 增补）：投递到 seq=2000 后，Set 中 seq < 1000 的条目被裁剪（内存有界），且后续 seq 重放判定不受裁剪影响
  </behavior>
  <action>
    1. 先写 history-filter.test.ts 与 dedup.test.ts 增补用例、machine 侧 TIMER/VISIBILITY 行为用例（并入 Task 1 的测试文件或新文件，一场景一文件惯例）。
    2. connection-machine.ts 扩展：常量 HEARTBEAT_INTERVAL_MS=30_000、PONG_DEADLINE_MS=10_000、PROBE_DEADLINE_MS=5_000（研究 A2 Discretion 建议值）；online 态周期 sendPing + pong 死线调度；VISIBILITY(visible) → sendPing + probe 调度，pong 到达取消、超时走强制重连路径；VISIBILITY(hidden) → 取消心跳周期（页面冻结时省额度，恢复时探活接管）。
    3. pushhub.ts adapter：document.addEventListener("visibilitychange") 翻译 VISIBILITY 事件；心跳与死线定时器随 machine schedule/cancel 动作创建清除。destroy() 移除 visibilitychange 监听（D-18 资源释放完备）。
    4. history 过滤实现点确认：emitHistory 前对 batch.messages 逐条过 SeqDedup.shouldDeliver，oldest_kept_seq/has_more 原样透传（D-16×D-17 交集唯一实现点）。
    5. 单测全绿后 commit。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/web-sdk test 全绿（新增 history-filter/machine 用例 + 既有基线）；pnpm --filter @pushhub/web-sdk run typecheck 退出 0</automated>
  </verify>
  <acceptance_criteria>
    - fake timers 下心跳/死线/探活三组时序断言全绿（30s ping、10s pong 死线、5s 探活死线，D-27）
    - history-filter.test.ts 证明交叠批次过滤后 "messages 只含未见消息" 且 oldest_kept_seq/has_more 原样（D-16×D-17 交集）
    - dedup.test.ts 证明窗口裁剪后 Set 尺寸有界且判定正确（D-17）
    - destroy() 后无残留定时器与 visibilitychange 监听（D-18）
  </acceptance_criteria>
  <done>SC2 的全部边界行为（退避、心跳、探活、fatal、翻页、去重）在单测层全覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: 断连混沌 E2E（SC2）+ 部署 0.1.6 生产冒烟</name>
  <files>packages/web-sdk/e2e/reconnect.spec.ts, package.json, DEPLOY.md</files>
  <precondition>02-01 SUMMARY 的 setOffline spike 结论可查（断连手段按结论选定）；wrangler 已登录；.claude/admin.key 存在；https://pushhub.dyun.org 可达。</precondition>
  <read_first>
    - D:/AIworkspaces/PushHub/.planning/phases/02-web-sdk/02-01-SUMMARY.md（setOffline spike 结论 + 02-01 实测数据）
    - D:/AIworkspaces/PushHub/packages/web-sdk/e2e/tracer.spec.ts（02-01 的 setup 编排与页面注入模式——本任务复用）
    - D:/AIworkspaces/PushHub/packages/web-sdk/playwright.config.ts（webServer 配置）
    - D:/AIworkspaces/PushHub/scripts/smoke.mjs（L155-210 断连补拉断言范式：恰补 N 条且 seq 连续零重复）
    - D:/AIworkspaces/PushHub/DEPLOY.md（版本 +1 与部署记录）
  </read_first>
  <action>
    1. e2e/reconnect.spec.ts 用例一（SC2 核心）：setup 建频道 → 页面两行接入（复用 tracer.spec 的 setContent 模式）→ 等 status online → 按 spike 结论断连：结论为 setOffline 会关 WS 则 context.setOffline(true)，否则用页面内调试路径直接关闭底层 socket（备选：CDP Network.emulateNetworkConditions）→ 断言 status 迁移 reconnecting → Node 侧 fetch（不受浏览器离线影响，smoke.mjs:51-57 同款）发 2 条消息 → 恢复网络/连接 → status 回 online → 断言恰补 2 条、seq 连续、全部收到的 seq 无重复（SC2：宿主无感续收）。
    2. 用例二（大缺口翻页，Pitfall 5 回归）：新建频道先发 55 条（循环 fetch）→ 页面接入吃首拉 50 条 → 断连 → 再发 5 条 → 恢复 → 断言总收到 60 条、零重复（验证 has_more 翻页路径真实走通）。
    3. 用例三（被动断连恢复）：页面接入后由 Node 侧重启视角不可行——改为验证部署等价场景：页面接入 → 向频道发 1 条确认在线 → 页面内主动 socket close 模拟意外断连（非 disconnect()——验证不经用户调用的断线也走重连）→ 自动恢复后新消息照常到达。
    4. 根 package.json version 0.1.5 改 0.1.6；pnpm run deploy（链式自动重建 SDK 产物——Pattern 5 防 stale 产物）。
    5. 生产冒烟：PH_SMOKE_URL=https://pushhub.dyun.org PH_ADMIN_KEY=$(cat .claude/admin.key) node scripts/smoke.mjs → SMOKE OK；node fetch /pushhub.js 断言 200 且与本地 dist 字节一致。DEPLOY.md 记录 0.1.6 行（注明 SDK 重连韧性完整化）。
  </action>
  <verify>
    <automated>
      cd D:/AIworkspaces/PushHub 后：
      1. pnpm --filter @pushhub/web-sdk run e2e（tracer.spec + reconnect.spec 全绿）
      2. PH_SMOKE_URL=https://pushhub.dyun.org PH_ADMIN_KEY=$(cat .claude/admin.key) node scripts/smoke.mjs 输出 SMOKE OK
      3. node -e 断言生产 /pushhub.js 200 且与 dist/pushhub.js 字节一致
    </automated>
    <human-check>dashboard Durable Objects Duration：空闲频道挂 WS 平直不增（D-15 ④ 例行）。</human-check>
  </verify>
  <acceptance_criteria>
    - e2e/reconnect.spec.ts 三用例绿：断连后自动重连续补拉恰 2 条零重复；55+5 大缺口 60 条全到零重复；意外 socket close 自动恢复
    - 根 package.json version 0.1.6，DEPLOY.md 新增 0.1.6 行
    - 生产 SMOKE OK 且 /pushhub.js 与本地构建字节一致
  </acceptance_criteria>
  <done>SC2 在本地 E2E（三种断连形态）与生产冒烟双验证通过；SDK 重连韧性行为进入 0.1.6 产物。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 服务端 → SDK | 混淆/未来版本帧经 WS 入站（fatal 路径本计划完整化）；异常 has_more 循环是潜在 DoS 面 |
| 页面可见性事件 → SDK | visibilitychange 探活路径（D-27）在 iOS 冻结恢复场景下的行为正确性 |
| SDK → 宿主 | 重复消息若漏过去重即破坏宿主状态一致性（D-17 是宿主契约不是优化） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | connection-machine fatal 路径（伪造 v 的帧诱导 SDK 死循环或静默失配） | medium | mitigate | v !== PROTOCOL_VERSION → fatal error + offline + 不再 createSocket（machine-fatal.test.ts 锁定；Pitfall 10 方向记忆：服务端宽容、客户端严格） |
| T-02-06 | DoS | has_more 翻页循环（服务端异常/被构造的无限 has_more） | low | mitigate | SYNC_PAGE_MAX=100 硬上限，超限 emitError 放弃（单测覆盖超限路径） |
| T-02-07 | Tampering | 去重窗口绕过（构造交叠批次使重复消息进入宿主） | medium | mitigate | shouldDeliver 是 message 与 history 双路径唯一闸门（交集语义单点实现）；history-filter + e2e 断言 seq 零重复 |
| T-02-08 | DoS | 心跳缺失导致的静默死线（NAT/中间设备超时后连接假活） | low | mitigate | 30s ping + 10s pong 死线强制重连；visibilitychange 探活 5s 死线（D-27） |
</threat_model>

<verification>
- pnpm --filter @pushhub/web-sdk test：退避/fatal/事件映射/心跳/探活/交集/去重七组单测全绿（fake timers）
- pnpm --filter @pushhub/web-sdk run e2e：tracer（02-01 回归）+ reconnect（断连混沌三用例）全绿
- 生产 0.1.6：SMOKE OK + /pushhub.js 字节一致
</verification>

<success_criteria>
- SC2 完整证明：本地三种断连形态（网络离线、大缺口、被动 socket close）全部自动恢复且零丢失零重复；退避曲线单测锁定 cap 60s
- D-27 探活逻辑实现且时序单测覆盖（iOS 真机验证按 D-27 明确不追踪）
- D-16×D-17 交集语义有专属测试与唯一实现点
</success_criteria>

## Artifacts this phase produces

（02-02 增补部分）
- 模块：connection-machine.ts 的 createMachine()（纯状态机，输入/输出事件流——Phase 5 Rust 移植同构参考）
- 常量：BACKOFF_CAP_MS=60_000、BACKOFF_BASE_MS=500、HEARTBEAT_INTERVAL_MS=30_000、PONG_DEADLINE_MS=10_000、PROBE_DEADLINE_MS=5_000、SYNC_PAGE_MAX=100、DEDUP_WINDOW=1000
- 行为契约：重连确定序列（WS_OPEN → 首拉过滤 → sync since=lastSeq → has_more 翻页）；"on(\"history\") 的 messages 永远只含宿主未见消息"（02-03 写入 README）
- 测试：machine-backoff / machine-fatal / machine-events / history-filter / dedup（增补）/ e2e/reconnect.spec.ts（三断连形态）
- 部署：0.1.6 + DEPLOY.md 记录行

<output>
Create .planning/phases/02-web-sdk/02-02-SUMMARY.md when done
</output>
