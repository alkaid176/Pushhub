---
phase: 02-web-sdk
plan: 06
type: execute
wave: 2
depends_on: ["02-04", "02-05"]
files_modified:
  - package.json
  - packages/web-sdk/e2e/viewer.spec.ts
  - packages/server/public/pushhub.js
  - packages/server/public/index.html
  - DEPLOY.md
autonomous: true
gap_closure: true
gap_ids: [G-02-2, G-02-3, G-02-4]
requirements: [WEB-01, WEB-04, WEB-05]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "生产 https://pushhub.dyun.org 运行 0.1.9：/pushhub.js 与本地构建逐字节一致（SVG 锚点修复 + FORBID_TAGS + WS_FAIL 容错已上线）"
    - "生产 / 页面引用 /pushhub.js?v=0.1.9——版本注入机制首次生产生效（G-02-3 端到端实证）"
    - "畸形 serverUrl 接入的查看器呈现 error 态（致命错误 + 已断开），不卡'连接中'（WR-04 端到端实证）"
    - "server 60 例 + web-sdk 全量单测 + E2E 全绿零回退；DEPLOY.md 有 0.1.9 部署记录"
  artifacts:
    - package.json（version 0.1.9）
    - packages/server/public/pushhub.js（0.1.9 构建产物字节）
    - packages/server/public/index.html（?v=0.1.9，构建注入）
    - DEPLOY.md（0.1.9 部署记录行）
  key_links:
    - "本地构建字节 ↔ 生产 /pushhub.js 字节（cmp 逐字节核对——防产物与源不一致）"
    - "根 package.json 0.1.9 → build.mjs 注入 → 生产 / 的 ?v=0.1.9（G-02-3 机制生产闭环）"
---

<objective>
集成验证与生产上线：消费 02-04/02-05 的全部修复，完成畸形 serverUrl 的查看器端到端用例、版本推进 0.1.9、全量回归（server 60 例 + web-sdk 单测 + E2E）、部署与生产验证、DEPLOY.md 登记。

Purpose: 三个 gap 的修复代码只有上线生产才算闭合（UAT 的验证对象就是生产环境）；render-markdown.ts/pushhub.ts/connection-machine.ts 的字节变更必须经重建 + 版本 +1 部署（项目既定规则），且 ?v= 注入机制随本次部署首次生产生效。
Output: 生产 0.1.9 + 逐字节/标记头证据 + DEPLOY.md 记录 + 新 E2E 用例。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-web-sdk/02-UAT.md
@.planning/phases/02-web-sdk/02-04-SUMMARY.md
@.planning/phases/02-web-sdk/02-05-SUMMARY.md
@DEPLOY.md

Gap 来源：G-02-2/G-02-3/G-02-4 的生产交付（修复本体在 02-04/02-05）。

既定流程事实（已核实）：
- 版本规则"部署前 +1"（01-05 决策）：先改根 package.json version 0.1.8 → 0.1.9，部署记录版本即本次代码版本。
- 部署命令必须 `pnpm run deploy`（pnpm 拦截裸 deploy；该脚本链式 web-sdk build → server deploy，build.mjs 注入随链自动执行）。
- 生产访问一律走自定义域 https://pushhub.dyun.org（本机对 *.workers.dev 存在 SNI 阻断 + DNS 污染，STATE.md 既定事实；部署本身走 api.cloudflare.com 不受影响）。
- SC4 标记头对照法（0.1.8 先例）：静态资产响应无 x-ph-worker 头（资产命中不触发 Worker），Worker 路径响应带 x-ph-worker: 1。
- 冒烟：node packages/server/scripts/smoke.mjs 需 PH_SMOKE_URL 与 PH_ADMIN_KEY（用户密码库 secret 明文，勿入库）；网络阻断拿不到时按 0.1.3/0.1.4 先例降级（本地全绿为功能等价证据 + WINDOWS.md 登记 unrun-verify）。
- DEPLOY.md 部署记录表格式：版本 | 时间 (UTC) | URL | Worker Version ID | 冒烟结果（含变更说明）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 畸形 serverUrl 查看器 E2E + 版本 0.1.9 + 全量回归</name>
  <files>packages/web-sdk/e2e/viewer.spec.ts, package.json</files>
  <action>
    (a) viewer.spec.ts 新增独立 test 块（WR-04 端到端，SDK 修复已由 02-04 就位）：以 `?server=${encodeURIComponent("not a url")}&key=phc_${"k".repeat(32)}` 打开查看器 → 断言 #error-bar 可见且文本含"致命错误"，#status-text 最终为"已断开"（waitForFunction 轮询，error 事件经 0ms 延迟派发）。本用例归本计划而非 02-04，因 viewer.spec.ts 与 02-05 同文件必须错 wave。
    (b) 根 package.json version 0.1.8 → 0.1.9（部署前 +1 规则）。
    (c) 全量回归三连：pnpm test（根脚本：server 60 例 + web-sdk 全量）→ pnpm --filter @pushhub/web-sdk run typecheck → pnpm --filter @pushhub/web-sdk e2e（E2E webServer 自动先 build：pushhub.js 含全部修复字节、index.html 被注入 ?v=0.1.9、cache-bust-sync 断言随 version 变更同步转绿）。
    提交：test(02-06): 畸形 serverUrl 查看器 E2E + chore(02-06): 版本推进 0.1.9。
    禁止事项：不得跳过 (c) 直接部署（部署断连即生产混沌，回归必须先行）；不得手工编辑 index.html 的 ?v=（02-05 机制负责，手改即破坏被测机制）。
  </action>
  <verify>
    <automated>pnpm test && pnpm --filter @pushhub/web-sdk e2e</automated>
  </verify>
  <done>三连全绿（server 60 + web-sdk 单测 + E2E 含两条新用例：存储禁用、畸形 serverUrl）；根 version 0.1.9；E2E 构建链已产出 0.1.9 字节与 ?v=0.1.9。</done>
</task>

<task type="auto">
  <name>Task 2: 部署 0.1.9 + 生产验证 + DEPLOY.md 登记</name>
  <files>DEPLOY.md, packages/server/public/pushhub.js, packages/server/public/index.html</files>
  <precondition>部署需 wrangler 已认证（npx wrangler whoami 非 error）；生产 ADMIN_KEY secret 已就位（0.1.8 起既有，无需重设）。执行前断言，不满足即停。</precondition>
  <action>
    (a) 执行 pnpm run deploy（链式 build→deploy：build.mjs 重建产物 + 注入 index.html ?v=0.1.9 后由 wrangler 上线）。从部署输出记录 Worker Version ID。
    (b) 生产程序化验证（全部经 https://pushhub.dyun.org）：
        - 字节一致：curl -s https://pushhub.dyun.org/pushhub.js 与本地 packages/server/public/pushhub.js 逐字节比对（curl 落盘后 cmp），并记录 min/gzip 字节数（较 0.1.8 的 81,022 变化即为修复字节上线的证据）；
        - 机制生效：curl -s https://pushhub.dyun.org/ 的 pushhub.js 引用为 ?v=0.1.9（恰一处）；
        - 资产零计费标记头：curl -sI https://pushhub.dyun.org/pushhub.js 与 /viewer.js 响应头无 x-ph-worker（SC4 对照法，0.1.8 先例）。
    (c) D-15 冒烟（①建频道发消息 ②WS 实收 ③断线补拉）：PH_SMOKE_URL=https://pushhub.dyun.org PH_ADMIN_KEY=<用户密码库 secret> node packages/server/scripts/smoke.mjs。若本窗口拿不到 PH_ADMIN_KEY 或遇网络阻断（UND_ERR_CONNECT_TIMEOUT / DNS 污染为既定环境问题），按 0.1.3/0.1.4 先例降级：以 (b) 程序化证据集 + 本地真实 workerd 全绿收尾，在 WINDOWS.md 登记 unrun-verify 待网络窗口补跑——不烧重试。
    (d) DEPLOY.md 部署记录表增 0.1.9 行（沿用表格格式：版本 | 时间 UTC | URL https://pushhub.dyun.org | Worker Version ID | 冒烟结果），结果栏注明：gap closure（G-02-2 SVG 锚点两分支 + WR-02 FORBID_TAGS + WR-04 WS_FAIL 容错 + G-02-3 ?v= 构建期注入首次生产生效 + WR-03 viewer 存储防护）+ 字节数 + 标记头证据。
    提交：chore(02-06): 部署 0.1.9（gap closure：SVG 锚点 + ?v= 机制 + 三项加固）——含产物字节、index.html、DEPLOY.md。
    禁止事项：不得以 workers.dev 域名做任何验证（本机被污染，假阴性）；冒烟拿不到 key 时不反复试探（既定降级路径）；secret 绝不写入任何提交文件。
  </action>
  <verify>
    <automated>curl -s https://pushhub.dyun.org/pushhub.js -o /tmp/prod-pushhub.js && cmp /tmp/prod-pushhub.js packages/server/public/pushhub.js && curl -s https://pushhub.dyun.org/ | grep -o 'pushhub.js?v=[0-9.]*' | sort -u</automated>
  </verify>
  <done>生产 0.1.9：/pushhub.js 与本地构建 cmp 一致、/ 引用恰一处 ?v=0.1.9、资产响应无 x-ph-worker；SMOKE OK（或按先例降级并登记 WINDOWS.md）；DEPLOY.md 有 0.1.9 完整记录行。</done>
  <reversibility rating="reversible">部署可随时以 git revert + 重部署回滚；版本号推进为线性约定无破坏性。</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地构建产物 → 生产边缘 | 部署管线上传的资产字节必须与被测源码一致 |
| 生产边缘 → 验证出口 | 本机网络对 workers.dev 被污染，验证出口必须固定为自定义域 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | 部署管线产物一致性 | high | mitigate | pnpm run deploy 链式构建（源→产物→部署单一管线）+ 生产 /pushhub.js 与本地构建 cmp 逐字节核对 |
| T-02-08 | Spoofing | 生产验证出口 | medium | mitigate | 全部验证固定经 https://pushhub.dyun.org（workers.dev 本机 DNS 污染会产出假证据）；冒烟含错误 Admin Key 401 反例（smoke.mjs 既有步骤） |
</threat_model>

<verification>
- pnpm test + e2e 全绿（Task 1 三连）。
- 生产证据集：cmp 字节一致 + ?v=0.1.9 恰一处 + 资产无 x-ph-worker + SMOKE OK（或 WINDOWS.md 登记降级）。
- DEPLOY.md 0.1.9 行完整（含 Worker Version ID 与变更说明）。
</verification>

<success_criteria>
- 三个 gap 的修复在生产 0.1.9 可观察：SVG 锚点带防护字节上线、?v= 机制生产生效、查看器畸形 URL 呈现 error 态。
- 全量回归零回退；部署记录完整可追溯。
</success_criteria>

<output>
Create .planning/phases/02-web-sdk/02-06-SUMMARY.md when done
</output>
