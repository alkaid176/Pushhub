---
phase: 03-admin-keys
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - packages/web-sdk/e2e/admin.spec.ts
  - package.json
  - DEPLOY.md
  - WINDOWS.md
autonomous: false
requirements: [ADM-05, KEY-02, KEY-03, KEY-04, ADM-01, ADM-02, ADM-03]
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "D-41 全链路单 test 绿（真浏览器 × 真 wrangler dev）：登录→建频道→接入片段→建带标签 Send Key→发消息→历史可见→重置 Channel Key（viewer 被踢）→吊销 Send Key（401）→删除频道（列表消失）——核心用户旅程一次性串联（CONTEXT specifics 原话旅程的自动化）"
    - "全量回归绿：pnpm test（server 全部 + web-sdk 单测）与 pnpm --filter @pushhub/web-sdk e2e（全部 spec）零失败——回归数字记入 SUMMARY"
    - "生产部署 0.1.11（版本先 +1 再 deploy）：smoke.mjs SMOKE OK（含新 sendKeys 取值路径的生产实证）；GET /admin.html 200 且响应无 x-ph-worker 头；/api/send 401 反例响应带 x-ph-worker: 1（对照）；/ 与 /admin.html 的 pushhub.js?v= 引用值均为 0.1.11（构建注入生产实证）"
    - "生产 GET /api/admin/channels 列出旧格式冒烟频道（smoke- 前缀）且结构完整——normalize 兼容读取生产实证（migrate-on-write 策略零破坏证据）"
    - "用户人工验收完成：dashboard 请求计数核对（SC4——admin.html 浏览不产生 Worker 请求曲线）+ 浏览器走一遍核心旅程 + 经管理页删除 1 个旧冒烟频道（D-34 首次真实 dogfooding + one-way 操作由用户亲手执行）"
    - "DEPLOY.md 登记完整（版本/时间/URL/Version ID/冒烟结果/管理页上线说明）；WINDOWS.md 登记 dashboard 人工核对项与用户 dogfooding 项"
  artifacts:
    - "e2e/admin.spec.ts：D-41 journey test（单 test 全链路串联）"
    - "root package.json version 0.1.11"
    - "DEPLOY.md 0.1.11 部署记录行"
    - "WINDOWS.md 人工验收登记"
  key_links:
    - "部署链（既有规约）：root version +1 → pnpm run deploy（链式 web-sdk build → server deploy，构建期完成 admin.html ?v= 注入）→ smoke.mjs 生产冒烟 → DEPLOY.md 登记——版本号即本次代码版本，字节变更经重建+版本 +1 部署为既定规则"
    - "smoke.mjs 新取值路径（channel.sendKeys[0].key，03-01 联动）在生产首次实跑——冒烟绿即 schema 演进生产闭环证据"
    - "SC4 双证据闭环：程序化（x-ph-worker 标记头对照，本 plan Task 2）+ dashboard 人工（请求计数，Task 3 用户验收）"
---

<objective>
**As a** 管理员，**I want to** 在生产环境用浏览器完成频道与密钥全生命周期管理（真实冒烟频道删除即首次 dogfooding），**so that** 真实客户端大规模接入前的管理能力就位且经生产验证（阶段目标收口）。

本 plan 收口 Phase 3：D-41 全链路 E2E（单 test 串联完整旅程）+ 全量回归 + 生产部署 0.1.11（版本 +1 规约 + 冒烟 + /admin.html 资产对照 + DEPLOY.md 登记）+ 用户人工验收（SC4 dashboard + 管理页 dogfooding 删除）。

Purpose: 本地全绿不等于上线；gap 修复只有上线生产才算闭合（项目既定规则）。
Output: journey test、0.1.11 部署与登记、用户验收记录。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-keys/03-CONTEXT.md
@.planning/phases/03-admin-keys/03-RESEARCH.md
@.planning/phases/03-admin-keys/03-04-SUMMARY.md

@scripts/smoke.mjs
@DEPLOY.md
@WINDOWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-41 全链路 journey test + 全量回归</name>
  <files>packages/web-sdk/e2e/admin.spec.ts</files>
  <read_first>packages/web-sdk/e2e/admin.spec.ts（切片一至四全部既有 test 与 helper——journey 复用其断言原子）, .planning/phases/03-admin-keys/03-CONTEXT.md（§specifics 核心用户旅程原话——journey 步骤对照）, packages/web-sdk/e2e/viewer.spec.ts（waitViewerOnline——重置踢连观察复用）</read_first>
  <action>
    在 admin.spec.ts 追加 1 个串联 test（test 名含「D-41 全链路」）：
    登录（正确 key）→ 建频道（唯一名）→ 断言片段卡三块 → 建 Send Key（标签 journey-bot）→ 断言行与掩码 → 经该 Send Key request.post /api/send 发 1 条消息（200）→ 展开历史区断言该消息可见（倒序首条）与「未回复」徽标 → pageB viewer 以 URL 参数连接（waitViewerOnline）→ 走重置确认框确认 → 断言 pageB 离开 online + 新 Key 明文展示块出现 → pageB 以新 Key 重连 online（历史保留——首拉含前述消息）→ 吊销该 Send Key（确认框）→ request.post /api/send 该 Key 断言 401 → 打开删除确认框输入频道名前缀 → 确认 → 断言频道从列表消失与详情空态。
    步骤间不复用独立 test 的 beforeEach 频道——本 test 自建自删自证（删除即清理）。
    随后跑全量回归：pnpm test（server 全部集成测试 + web-sdk 全部单测——含 cache-bust-sync 双文件断言）与 pnpm --filter @pushhub/web-sdk e2e（admin + viewer + reconnect + tracer 全部 spec）；将各套件通过数（N/N）记入 SUMMARY 供 DEPLOY.md 登记。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/web-sdk e2e -- admin.spec.ts && pnpm test</automated>
  </verify>
  <done>journey test 绿（单 test 完整旅程）；全量回归零失败：server 集成 N/N + web-sdk 单测 N/N + E2E 全部 spec N/N（数字记入 SUMMARY）。</done>
  <acceptance_criteria>
    - admin.spec.ts 存在单个 test 覆盖九步旅程（登录/建频道/片段/建 Key/发消息/历史/重置踢连/吊销 401/删除），test 内含 viewer 第二 page 的 online→离开 online→新 Key 重连 online 三态断言
    - journey test 内频道经删除步骤清理（无残留频道依赖 beforeEach）
    - pnpm test 与 pnpm --filter @pushhub/web-sdk e2e 全部退出码 0，通过数记录在 SUMMARY
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 生产部署 0.1.11 + 冒烟 + /admin.html 资产对照 + normalize 生产实证</name>
  <files>package.json, DEPLOY.md, WINDOWS.md</files>
  <read_first>DEPLOY.md（版本号规则 + 部署步骤 + D-15 checklist + 部署记录表——登记格式对照）, scripts/smoke.mjs（03-01 后新取值路径——冒烟预期）, .planning/phases/03-admin-keys/03-RESEARCH.md（§Runtime State Inventory——生产旧格式频道清单与 A4 假设）, WINDOWS.md（既有 unrun-verify 登记格式）</read_first>
  <precondition>wrangler login 凭据有效（既有部署链路）；生产入口 https://pushhub.dyun.org 可达（workers.dev 域名有 SNI/DNS 阻断，一切生产访问走自定义域名——STATE.md 既定注意事项）；PH_ADMIN_KEY 明文在用户密码库可供冒烟注入。</precondition>
  <action>
    (a) root package.json version 0.1.10 → 0.1.11（版本先定再部署——部署记录版本即本次代码版本）；
    (b) pnpm run deploy（链式 web-sdk build → server deploy：构建期完成 index.html 与 admin.html 双 ?v=0.1.11 注入与产物复制）；记录 Worker Version ID（部署输出）；
    (c) 生产冒烟：PH_SMOKE_URL=https://pushhub.dyun.org PH_ADMIN_KEY=<secret> node scripts/smoke.mjs → 断言输出 SMOKE OK（含 admin 建频道（新 sendKeys 结构生产路径）→ 发消息 → WS 实收 → 断线补拉 → 401/413 反例全链路）；
    (d) /admin.html 生产对照（SC4 程序化半边）：curl -sI https://pushhub.dyun.org/admin.html 断言 200 且响应头无 x-ph-worker；curl -sI -X POST https://pushhub.dyun.org/api/send（无鉴权 401 反例）断言响应头含 x-ph-worker: 1（stampMarker 对照法，0.1.8 先例）；curl -s https://pushhub.dyun.org/ 与 /admin.html 断言各恰一处 pushhub.js?v=0.1.11（注入生产实证）；
    (e) normalize 生产实证：curl -H "Authorization: Bearer <key>" https://pushhub.dyun.org/api/admin/channels 断言 200 且响应含 smoke- 前缀频道（0.1.0~0.1.10 每版冒烟频道）且每条含 sendKeys 数组结构——旧格式记录兼容列出即 migrate-on-write 零破坏生产证据；若列出的频道中存在非 smoke- 前缀频道，完整列出并在 SUMMARY 标注（不得删除）；
    (f) DEPLOY.md 部署记录表追加 0.1.11 行：版本/时间(UTC)/URL/Version ID/冒烟结果 + 管理页上线说明（admin.html 静态资产 + 五条 Admin API + SC4 标记头证据 + normalize 生产实证）；WINDOWS.md 登记 Task 3 人工验收项（SC4 dashboard 请求计数 + 用户管理页 dogfooding）；
    (g) 网络阻断应对（既定策略）：若 dyun.org 冒烟遇连接超时，不烧重试——全量本地真实 workerd 测试 + journey E2E 为功能等价证据，未跑项登记 WINDOWS.md unrun-verify 待网络窗口（Phase 1/2 先例）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && node -e "const v=require('./package.json').version; if (v!=='0.1.11') { console.error('version: '+v); process.exit(1); } const d=require('fs').readFileSync('DEPLOY.md','utf8'); if (!d.includes('0.1.11')) { console.error('DEPLOY.md missing 0.1.11'); process.exit(1); } console.log('version+registry OK')"</automated>
  </verify>
  <done>0.1.11 部署成功（Version ID 记录）；SMOKE OK；/admin.html 200 无 x-ph-worker 而 API 反例带 x-ph-worker；双页 ?v=0.1.11；生产频道列表含 smoke- 旧频道且结构完整；DEPLOY.md/WINDOWS.md 登记完成。</done>
  <acceptance_criteria>
    - root package.json version 字段值为 0.1.11
    - smoke.mjs 生产运行输出含 SMOKE OK（或按 (g) 登记 WINDOWS.md unrun-verify——两种路径均闭合）
    - /admin.html 响应头检查无 x-ph-worker（curl -sI 输出人工/脚本核对，结果记 SUMMARY）
    - / 与 /admin.html 的 pushhub.js 引用查询串均为 ?v=0.1.11 且各恰一处
    - 生产 /api/admin/channels 响应含至少 1 个 smoke- 前缀频道且该频道对象含 sendKeys 数组键
    - DEPLOY.md 表含 0.1.11 行且行内含 Worker Version ID；WINDOWS.md 含人工验收登记
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 用户人工验收——SC4 dashboard 核对 + 管理页核心旅程 + 冒烟频道删除 dogfooding（one-way 由用户执行）</name>
  <what-built>生产 0.1.11 已上线：/admin.html 管理页（登录/频道 CRUD/多 Send Key/重置踢连/历史排障全功能）+ 五条 Admin API + 静态资产托管；自动化证据（SMOKE OK/标记头对照/?v= 注入）已由 Task 2 收集。</what-built>
  <how-to-verify>
    请用户完成三项验收并逐项反馈结果：
    ① SC4 dashboard 核对：打开 dashboard.cloudflare.com → Workers & Pages → pushhub → Metrics/Requests，随后浏览器多次刷新 https://pushhub.dyun.org/admin.html——Worker 请求计数应无相应增长（静态资产命中不计请求；仅管理页内 API 操作计入）；
    ② 管理页核心旅程走查（浏览器）：登录 → 建频道 → 复制接入片段 → 用 curl/脚本以 Send Key 发一条消息 → 历史区可见该消息 → 重置 Channel Key 观察客户端被踢 →（可选）吊销一个 Key；
    ③ D-34 dogfooding（one-way，用户亲手）：在管理页选中一个 smoke- 前缀旧冒烟频道 → 走删除确认框（体验输入频道名前缀才启用的交互）→ 确认删除 → 频道从列表消失；其余 smoke- 频道自行决定清理或保留。
  </how-to-verify>
  <resume-signal>三项验收完成后回复「approved」或逐项描述异常（异常则走 gsd-debug 流程处理后重验）</resume-signal>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && node -e "const w=require('fs').readFileSync('WINDOWS.md','utf8'); const hit=w.match(/Phase 3[\s\S]{0,2000}/); if (!hit) { console.error('Phase 3 section missing in WINDOWS.md'); process.exit(1); } console.log('WINDOWS.md Phase 3 registry present')"</automated>
  </verify>
  <done>三项验收完成并登记回 WINDOWS.md：dashboard 核对结论（admin.html 刷新不增请求计数）、旅程走查结果、至少 1 个 smoke- 频道经管理页删除成功（D-34 首次真实 dogfooding）；登记项全部勾销或明确遗留原因。</done>
  <acceptance_criteria>
    - WINDOWS.md 含 Phase 3 人工验收登记段，三项各有勾销记录或遗留说明
    - 用户确认管理页删除交互（前缀确认框）在真实浏览器可用
    - 生产频道列表中至少 1 个 smoke- 频道已消失（dogfooding 证据，用户操作）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 生产部署通道（api.cloudflare.com） | wrangler 凭据；部署断开全部 WS 连接（既定混沌） |
| 用户浏览器 → 生产 /admin.html | 生产管理页首次暴露公网（静态资产无鉴权壳 + Bearer API） |
| 用户 → 生产冒烟频道删除 | one-way 数据操作，由用户亲手执行（本任务即 checkpoint） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-20 | Denial of Service | 部署断连全量 WS（0.1.11 上线瞬间） | low | accept | SDK 自动重连 + 补拉（Phase 2 已验证的部署混沌语义）；部署时机由人选择 |
| T-03-21 | Information Disclosure | 生产管理页首次公网可达 | medium | mitigate | 页面壳无敏感数据（密钥全在 Bearer API 后）；CSP + textContent 纪律 + 消毒管道（01-04 plan 威胁缓解全部随本次上线）；Admin Key 仍是唯一凭据（D-28 取舍已锁定） |
| T-03-22 | DoS（不可逆误删） | 用户 dogfooding 删除真实频道 | high | mitigate | 前缀确认交互 + 删除目标限定 smoke- 前缀冒烟频道（本任务文案明示）；one-way 操作由用户亲手执行（checkpoint:human-verify 即 reversibility 门） |
| T-03-23 | Tampering | 生产响应与本地构建字节漂移（SC4 前置） | medium | mitigate | 冒烟含 /pushhub.js 与本地 cmp 逐字节对照惯例（0.1.5 起既定）；?v= 双页注入断言（构建期硬断言 + cache-bust-sync 恒一致） |
</threat_model>

## Artifacts this phase-plan produces

- **测试**：e2e/admin.spec.ts D-41 journey test（单 test 九步全链路）——admin.spec.ts 至此 13 test 定稿
- **部署**：root package.json version=0.1.11；DEPLOY.md 0.1.11 记录行（管理页上线）；WINDOWS.md Phase 3 人工验收登记段
- **生产证据**（记入 SUMMARY/DEPLOY.md）：SMOKE OK、/admin.html 无 x-ph-worker 对照、双页 ?v=0.1.11、normalize 生产实证（smoke- 频道新结构列出）、用户 dogfooding 删除记录

<verification>
- Task 1 后：journey + 全量回归（server N/N + 单测 N/N + e2e N/N）三绿
- Task 2 后：0.1.11 部署 + SMOKE OK + 资产对照四断言 + DEPLOY.md/WINDOWS.md 登记
- Task 3 后：用户三项验收勾销（阶段验证前置条件——之后才可 /gsd-verify-work）
</verification>

<success_criteria>
1. D-41 验收全链路自动化成立（journey test）+ 全量回归零失败
2. 生产 0.1.11：管理页上线 + SC4 双证据（程序化 + dashboard）+ 冒烟绿 + ?v= 注入生产生效
3. normalize 兼容 + D-34 删除经生产 dogfooding 首次真实使用（用户亲手）
4. 部署规约完整：版本 +1 → deploy → 冒烟 → DEPLOY.md 登记 → WINDOWS.md 人工项
</success_criteria>

<output>
Create `.planning/phases/03-admin-keys/03-05-SUMMARY.md` when done
</output>
