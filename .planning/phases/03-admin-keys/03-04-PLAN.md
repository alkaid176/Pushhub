---
phase: 03-admin-keys
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - packages/server/src/chat-room.ts
  - packages/server/src/admin.ts
  - packages/server/test/admin-history.test.ts
  - packages/server/public/admin.js
  - packages/server/public/admin.html
  - packages/web-sdk/e2e/admin.spec.ts
autonomous: true
requirements: [ADM-03]
estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "管理员在详情面板展开「消息历史（排障）」折叠区（首次展开懒加载）：按 seq 倒序看到该频道消息（最新在最上），每条头部含 #seq（mono）+ 时间（mono graytext）+ title 加粗（如有）+ answered 徽标（D-40/SC3）"
    - "消息正文经 PushHub.renderMarkdown 渲染（<script src=/pushhub.js> 引入，零重复实现）——含攻击样本的消息渲染后无 script 元素、无 on* 属性（存储型 XSS 双纵深：消毒管道 + CSP）（D-40/UI Considerations）"
    - "answered 徽标本期恒显示「未回复」（graytext）；「已回复」绿色样式先行定义供 Phase 4 复用（UI-SPEC Accent 保留清单第 2 项）"
    - "底部「加载更多」按钮带 before=<本页最小 seq> 游标翻页，每页 50 条（服务端缺省）；has_more=false 时按钮隐藏（D-40/裁量定稿）"
    - "翻到保留窗口底部且 oldest_kept_seq > 1 时显示分隔线「—— 更早的消息已被清理 ——」（D-10 诚实缺口语义，viewer maybeSeparator 同款判定）（UI Considerations long-text/边界）"
    - "空频道历史区显示空态文案「该频道还没有消息——Webhook 发送第一条消息后会显示在这里。」（UI Considerations empty）"
    - "无 title 的消息不渲染标题行（viewer appendMessage 同款）（UI Considerations partial）"
    - "长 Markdown 消息正文 overflow-wrap: anywhere 不撑破面板（UI Considerations long-text）"
    - "服务端 /history：GET /api/admin/channels/:id/messages?before=&limit= 经 admin 鉴权后 X-PH-Verified 转发 DO；limit 缺省 50 钳制 [1,500]；keyset 倒序无重叠无遗漏；响应 {messages, has_more, oldest_kept_seq}（D-36）"
  artifacts:
    - "packages/server/src/chat-room.ts：GET /history 内部路由（keyset 倒序 + oldest_kept_seq + clampAdminLimit）"
    - "packages/server/src/admin.ts：GET messages 转发分支（admin 参数化路由最后一条）"
    - "packages/server/test/admin-history.test.ts（新：翻页矩阵）"
    - "admin.js/admin.html：历史折叠区（懒加载/倒序渲染/加载更多/分隔线/徽标）"
  key_links:
    - "Worker admin 鉴权（checkAdminAuth）→ KV id: 读 channelId 映射（miss 404 不触 DO）→ X-PH-Verified 转发 DO GET /history——三段链任一断裂：未鉴权直查（越权）或 404 探测面"
    - "DO /history 复用 MESSAGE_COLUMNS + rowToMessageFrame（同文件零成本）——与扇出帧逐字段同构含 answered 四字段，SC3「含回复状态」零额外映射；新写映射函数即制造双管道漂移"
    - "admin.js 历史渲染唯一 innerHTML 入口 = PushHub.renderMarkdown(m.text)；其余全部 textContent——消毒安全关键路径（prohibition：禁双管道）"
---

<objective>
**As a** 管理员，**I want to** 在管理页按频道浏览消息历史（含回复状态），**so that** 机器人接入后排障有入口：消息到了没、内容是什么、是否已被回复（ADM-03/SC3——Phase 4 回复闭环的排障前置）。

本 plan 交付消息历史垂直切片：DO GET /history 内部路由（keyset 倒序翻页 + oldest_kept_seq）+ Worker 转发（D-35 最后一条路由）+ 管理页历史折叠区（D-40：renderMarkdown 复用 + answered 徽标 + 加载更多）+ E2E 切片四。

Purpose: SC3 排障入口；answered 展示是 Phase 4 回复闭环的既有视图复用点。
Output: /history 路由、admin-history.test.ts、历史视图 UI。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-keys/03-CONTEXT.md
@.planning/phases/03-admin-keys/03-RESEARCH.md
@.planning/phases/03-admin-keys/03-PATTERNS.md
@.planning/phases/03-admin-keys/03-UI-SPEC.md
@.planning/phases/03-admin-keys/03-03-SUMMARY.md

@packages/server/src/chat-room.ts
@packages/server/src/admin.ts
@packages/server/public/viewer.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DO GET /history（keyset 倒序翻页）+ Worker GET messages 转发 + 翻页矩阵测试</name>
  <files>packages/server/src/chat-room.ts, packages/server/src/admin.ts, packages/server/test/admin-history.test.ts</files>
  <read_first>packages/server/src/chat-room.ts（sendHistory 425-462 行 keyset 翻页 + LIMIT n+1 技巧 + MESSAGE_COLUMNS 113-115 + rowToMessageFrame 139-160 + MIN(seq) 查询形态 426-428——全部复用内核）, packages/server/src/admin.ts（03-03 后参数化路由——messages 分支扩展点）, packages/server/test/admin-channels.test.ts（adminRequest helper 泛化形态）, .planning/phases/03-admin-keys/03-RESEARCH.md（§Pattern 4 /history 设计 + §Example 2 查询形态 + limit 裁量建议）</read_first>
  <behavior>
    - 频道 seed 3 条消息（seq 1/2/3）后 GET /api/admin/channels/:id/messages → 200 {messages: [seq3, seq2, seq1]（倒序）, has_more: false, oldest_kept_seq: 1}；messages 元素含 answered: false / answered_by: null / answered_at: null / answered_content: null（MessageFrame 同构）
    - ?limit=2 → messages 恰 2 条 [seq3, seq2]，has_more: true
    - ?before=3&limit=2 → [seq2, seq1]，has_more: false；?before=2 → [seq1]——翻页无重叠无遗漏
    - ?limit=0 → 钳制为 1；?limit=501 → 钳制为 500（断言行为：limit=0 返回 1 条）；?limit=abc（非数字）按缺省 50 处理
    - 空频道 → {messages: [], has_more: false, oldest_kept_seq: 0}
    - 不存在 channelId → 404 not_found；无鉴权 → 401 invalid_key
    - 发送含攻击样本文本（<script> 标签）的消息后 /history 返回原文逐字（哑管道，渲染消毒在前端）——服务端不解析不消费
  </behavior>
  <action>
    chat-room.ts（D-36 + RESEARCH Example 2）：
    (a) fetch 路由新增 GET /history 分支（X-PH-Verified 校验后）：读 url.searchParams 的 before 与 limit；limit 解析：null/NaN → 50，钳制 [1, 500]（独立小函数 clampAdminLimit，不导出既有私有的 clampSyncLimit——admin 域独立常量 50/500 与 shared SYNC_* 刻意分离，注释说明）；查询内核复用 MESSAGE_COLUMNS + rowToMessageFrame：before === null 时 SELECT 全列 FROM messages ORDER BY seq DESC LIMIT ?1（limit+1）；否则 WHERE seq < ?1 ORDER BY seq DESC LIMIT ?2（绑定参数 ?n 占位，禁 OFFSET）；has_more = rows.length > limit，多取 1 条裁掉；oldest_kept_seq = SELECT MIN(seq)（.one() as {m: number | null}，空表 0——既有同款查询形态）；响应 200 JSON {messages, has_more, oldest_kept_seq}；全程同步游标 .toArray()/.one() 即收不跨 await（SQL 纪律）。
    admin.ts（D-35 最后一条路由）：
    (b) GET /api/admin/channels/:channelId/messages 分支（参数化骨架的 messages sub）：经 keys.ts 读 id:（miss → 404 not_found 不触 DO）→ 构造转发 Request：INTERNAL_ORIGIN + "/history?" + 原查询串透传（new URL(request.url).search 原样拼接——before/limit 原样到达 DO，钳制在 DO 层单点）+ X-PH-Verified: 1 头 → env.CHANNELS.getByName(channelId).fetch(转发) 直接返回 DO 响应。
    测试 admin-history.test.ts：照 admin-channels 骨架（adminRequest 泛化 path）；behavior 全清单落测；seed 用 sendRequest 发真实消息（经 /api/send 全链路，非直插 SQL）；攻击样本用例注释：服务端哑管道（SRV-02），消毒断言在前端（Task 3 E2E）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/server test -- admin-history && pnpm --filter @pushhub/server test && pnpm --filter @pushhub/server run typecheck</automated>
  </verify>
  <done>admin-history.test.ts 翻页矩阵全部绿（倒序/游标/钳制/空态/answered 字段/404/401）；server 全量零回归；typecheck 绿；/history 查询零 OFFSET、全绑定参数。</done>
  <acceptance_criteria>
    - chat-room.ts 存在 /history 分支与 clampAdminLimit 函数；查询语句含 ORDER BY seq DESC 且 WHERE 子句使用 seq < ? 形态（无 OFFSET 关键字）
    - /history 响应体三键：messages / has_more / oldest_kept_seq；messages 元素含 answered 四字段（rowToMessageFrame 复用，无新映射函数）
    - admin.ts messages 分支经 KV id: 读取后才触 DO 转发，id: miss 返回 404
    - admin-history.test.ts 含 limit=0 与 limit=501 钳制用例、before 翻页两连页用例、空频道用例
    - pnpm --filter @pushhub/server test 退出码 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 管理页历史折叠区——懒加载/倒序渲染/加载更多/清理分隔线/answered 徽标</name>
  <files>packages/server/public/admin.js, packages/server/public/admin.html</files>
  <read_first>packages/server/public/viewer.js（appendMessage 91-123 渲染形态 + maybeSeparator 135-144 分隔线判定——本任务同款逻辑）, packages/server/public/admin.js（03-03 后全貌——错误条/加载态复用）, .planning/phases/03-admin-keys/03-UI-SPEC.md（Interaction #4 消息历史契约 + Copywriting 历史空态/分隔线/加载中文案 + Typography 等宽规则）, packages/server/src/admin.ts（Task 1 后 API 契约）</read_first>
  <action>
    admin.html：详情面板新增 <details> 折叠区（summary「消息历史（排障）」）+ 内部 #history-list（max-height + overflow-y 独立滚动）+ 底部「加载更多」按钮 + 徽标样式（「已回复」#2e9e5b 先行定义、「未回复」#9a9a9a——Accent 保留清单第 2 项 Phase 4 复用）。
    admin.js（D-40 + UI-SPEC Interaction #4）：
    (a) 懒加载：details 的 toggle 事件首次展开时才 GET messages（无 before 首页）；切换选中频道时重置历史状态（列表清空 + 已加载标志复位 + 折叠态复位）；
    (b) 每条渲染（倒序即 API 返回序，最新在最上——直接 appendChild）：头部 = #seq（mono 13.6px）+ 时间（new Date(created_at).toLocaleTimeString()，mono graytext）+ title（如有则 strong textContent；无 title 不渲染标题行）+ answered 徽标（m.answered === true → 「已回复」绿样式；false → 「未回复」graytext 样式）；
    (c) 正文 body.innerHTML = window.PushHub.renderMarkdown(m.text)——全管理页唯一 innerHTML 入口（与 viewer 同款唯一管道纪律）；.msg-body 加 overflow-wrap: anywhere（长 Markdown）；
    (d) 加载更多：按钮点击 GET messages?before=<当前列表最小 seq> → 追加渲染；has_more === false 时按钮 hidden；请求期间按钮 disabled + 「加载中…」；
    (e) 清理分隔线：has_more === false 且 oldest_kept_seq > 1 时在列表底部渲染分隔线「—— 更早的消息已被清理 ——」（D-10 语义：oldest_kept_seq <= 1 表示从未清理不渲染——viewer maybeSeparator 同款判定，每频道每次展开至多一条）；
    (f) 空态：messages 为空数组且未加载过更多 → 空态文案「该频道还没有消息——Webhook 发送第一条消息后会显示在这里。」；
    (g) 错误走既有错误条；发起请求前若无 window.PushHub（SDK 加载失败）显示错误条且不抛异常。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/web-sdk run build && node -e "const s=require('fs').readFileSync('packages/server/public/admin.js','utf8'); for (const t of ['消息历史（排障）','该频道还没有消息','—— 更早的消息已被清理 ——','未回复','已回复']) { if (!s.includes(t)) { console.error('MISSING: '+t); process.exit(1); } } const m=s.match(/innerHTML/g)||[]; if (m.length!==1) { console.error('innerHTML count: '+m.length); process.exit(1); } console.log('history UI tokens OK')"</automated>
  </verify>
  <done>五个文案 token 在位；admin.js 全文件 innerHTML 出现次数恰 1（renderMarkdown 唯一入口——node 检查硬断言）；build 绿。真浏览器行为由 Task 3 断言。</done>
  <acceptance_criteria>
    - admin.js 中 innerHTML 赋值表达式全文件恰出现 1 处且右值为 window.PushHub.renderMarkdown 调用
    - 分隔线判定含 oldest_kept_seq > 1 条件（等于 1 不渲染）与 has_more false 条件
    - 加载更多请求的 before 参数取自当前已渲染列表的最小 seq
    - has_more === false 时加载更多按钮 hidden 属性在位
    - 徽标双态样式定义（已回复/未回复）均在 CSS 或类切换逻辑中
    - pnpm --filter @pushhub/web-sdk run build 退出码 0 且 token 检查输出 history UI tokens OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: E2E 切片四——历史倒序渲染/消毒/徽标/空态 + API 翻页抽查</name>
  <files>packages/web-sdk/e2e/admin.spec.ts</files>
  <read_first>packages/web-sdk/e2e/admin.spec.ts（切片一至三——登录/创建/发送 helper 复用）, packages/web-sdk/e2e/viewer.spec.ts（SC3 攻击样本断言形态——本任务同款消毒断言）, packages/server/public/admin.js（Task 2 产物——锚点对应）</read_first>
  <action>
    admin.spec.ts 追加 2 个 test：
    (a) 历史渲染与消毒：建频道 → 发 3 条消息（第 1 条普通 Markdown 含 **加粗**；第 2 条含攻击样本 <script>alert(1)</script> 后置文本 与 <img src=x onerror=alert(1)>；第 3 条无 title 纯文本）→ 选中频道展开历史区 → 断言：首条（列表最上）#seq 为第 3 条（倒序）→ 第 1 条正文含 strong 元素（renderMarkdown 真路径）→ 第 2 条渲染后 #history-list 内无 script 元素、无 on* 属性（locator('script') 与 [onerror] 计数断言）→ 第 3 条无标题行（strong 计数恰为第 1 条的 1）→ 每条「未回复」徽标在位 → has_more=false 加载更多按钮 hidden；
    (b) 空态与 API 翻页抽查：建第二个频道（不发消息）→ 选中展开 → 空态文案可见；另建频道发 3 条后 request.get messages?before=<第 2 条 seq> 断言返回恰 [seq1]（keyset API 抽查——完整矩阵在集成测试）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/web-sdk e2e -- admin.spec.ts</automated>
  </verify>
  <done>切片四 2 test 全绿；切片一至三 10 test 无回归；消毒断言（无 script/on*）与徽标/倒序/空态全覆盖。</done>
  <acceptance_criteria>
    - 历史 test 断言首条 seq 为最新消息（倒序证据）与攻击样本渲染后 script 元素计数 0
    - 徽标断言覆盖「未回复」文本在位
    - 空 test 断言空态文案与 API before 翻页（返回恰 1 条）
    - pnpm --filter @pushhub/web-sdk e2e -- admin.spec.ts 退出码 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 管理员浏览器 → Worker GET messages 路由 | Bearer Admin Key；消息数据读取入口 |
| Worker → DO GET /history | X-PH-Verified 内部头转发 |
| Webhook 发送方 → 消息文本 → 管理页 DOM | 消息体是任意外部输入，历史视图渲染即跨越此边界（存储型 XSS 面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-16 | Tampering/Elevation | 历史视图存储型 XSS（消息体直通管理页 DOM） | critical | mitigate | PushHub.renderMarkdown（marked + DOMPurify FORBID_ATTR 收敛版）唯一 innerHTML 管道 + CSP script-src 'self' 双纵深 + node 检查硬断言全文件 innerHTML 恰 1 处 + E2E 攻击样本断言 |
| T-03-17 | Information Disclosure | /history 越权读取（无 Admin Key 直查） | high | mitigate | 路由置于 checkAdminAuth 之后 + KV id: 映射 miss 404 不触 DO + DO 侧 X-PH-Verified 前置（双防线继承） |
| T-03-18 | Tampering | before/limit 参数注入 SQL | medium | mitigate | 全查询绑定参数（?n 占位）+ limit 数值钳制 [1,500]（NaN 归缺省）——既有 SQL 纪律延续，无字符串拼接进语句 |
| T-03-19 | Information Disclosure | channelId 探测（messages 路径枚举） | low | mitigate | channelId 16 字符白名单正则 + 格式错与不存在同 404 同文案（既有策略延续） |
</threat_model>

## Artifacts this phase-plan produces

- **API 路由**：`GET /api/admin/channels/:channelId/messages?before=&limit=`（200 {messages, has_more, oldest_kept_seq}）——admin.ts 参数化路由五分支全部落位（D-35 完结）
- **DO 内部路由**：`GET /history`（keyset 倒序 + oldest_kept_seq）；模块内 `clampAdminLimit`（缺省 50 / 钳 [1,500]）
- **测试**：packages/server/test/admin-history.test.ts（翻页矩阵）；e2e/admin.spec.ts 切片四（2 test）
- **UI**：历史折叠区（懒加载 / renderMarkdown 渲染 / 加载更多 / 清理分隔线 / answered 徽标双态样式）
- **querystring 契约**：before（seq 游标，null=首页最新）/ limit（缺省 50 钳 [1,500]）——planner 裁量定稿

<verification>
- Task 1 后：admin-history 测试 + server 全量 + typecheck 三绿
- Task 2 后：build 绿 + 五 token 检查 + innerHTML 恰 1 处硬断言
- Task 3 后：admin.spec.ts 12 test 全绿（4+3+3+2）
</verification>

<success_criteria>
1. ADM-03/SC3 端到端：真浏览器展开历史 → 倒序 → Markdown 消毒渲染 → answered 徽标 → 翻页游标 → 空态/清理分隔线
2. D-36 服务端语义（keyset 无重叠 / 钳制 / oldest_kept_seq / answered 字段集）集成测试全覆盖
3. renderMarkdown 唯一管道纪律以构建期硬断言机制化（innerHTML 恰 1 处）
4. server 全量测试零回归
</success_criteria>

<output>
Create `.planning/phases/03-admin-keys/03-04-SUMMARY.md` when done
</output>
