---
phase: 03-admin-keys
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - packages/server/src/chat-room.ts
  - packages/server/src/admin.ts
  - packages/server/src/keys.ts
  - packages/server/test/admin-reset-kick.test.ts
  - packages/server/test/admin-delete.test.ts
  - packages/server/public/admin.js
  - packages/server/public/admin.html
  - packages/web-sdk/e2e/admin.spec.ts
autonomous: true
requirements: [KEY-02, KEY-04, ADM-01, ADM-02]
estimate:
  tokens: 65000
  raw_tokens: 32000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "管理员重置 Channel Key：确认框逐字文案含「重置后该频道所有已连接的客户端将立即被断开…最长约 1 分钟后全局失效…频道历史消息完整保留」；确认后 201 返回新 Channel Key，管理页明文一次性展示 + 复制按钮 + 60s 双活窗口提示条（#d9a300 边框式）（D-33/UI-SPEC）"
    - "重置后旧 Channel Key 的现有 WS 连接被立即踢出（服务端集成测试断言 close 事件 code 1008；E2E 断言 viewer 状态离开 online——DO 内强一致，SC2）"
    - "重置后旧 Channel Key 新建 WS 连接被拒 401（本地 miniflare 强一致；生产 ≤60s 边缘缓存双活窗口已在确认框与提示条文案文档化）"
    - "重置不丢频道历史：重置前发送的消息在重置后（用新 Key 连接的）首拉 history 帧全量可见（KEY-04/SC2——messages 表不动）"
    - "管理员删除频道：确认框要求输入频道名前缀（placeholder「输入频道名称的开头部分」），输入非空且为频道名前缀时删除按钮才启用（GitHub 删仓库模式，D-34）；删除成功后频道从列表消失、详情面板回到空态「在左侧选择一个频道查看详情。」"
    - "删除后 KV 三前缀键全清：ch:<旧 Key> 与 id:<channelId> 与全部 sk:<Key> 均失效（resolveChannelKey/resolveSendKey 返回 null、listChannels 不再列出）；DO 内 messages/rate_sends 数据清空且 retention alarm 已删除（getAlarm() === null——不再每日唤醒烧额度）（D-34/Pitfall 1）"
  artifacts:
    - "packages/server/src/chat-room.ts：POST /kick-all 与 POST /purge 内部路由（purge = 踢连 + deleteAll + deleteAlarm 成对）"
    - "packages/server/src/admin.ts：POST reset-channel-key 与 DELETE /api/admin/channels/:channelId 路由（编排顺序定稿）"
    - "packages/server/src/keys.ts：resetChannelKey / deleteChannelKeys 写路径函数"
    - "packages/server/test/admin-reset-kick.test.ts、packages/server/test/admin-delete.test.ts（新×2）"
    - "admin.js/admin.html：重置确认框 + 新密钥一次性展示 + 删除确认框（前缀联动）+ 60s 提示条"
  key_links:
    - "重置编排顺序（关键设计）：KV 写先（删 ch:old + 写 ch:new + id: 重写）→ DO /kick-all 转发后——反序（先踢后写）制造旧 Key 无限重挂窗口（被踢客户端立刻以缓存旧值重连成功后再无人踢它）"
    - "删除编排顺序（关键设计）：DO /purge 先 → KV 键删后——反序产生不可达孤儿 DO（频道从列表消失、无法重试）；正序部分失败时频道仍在列表，重试幂等（KV delete 幂等 + purge 对已清 DO 是 no-op）"
    - "deleteAll() 与 deleteAlarm() 必须成对出现在 /purge 内——deleteAll 官方语义不删 alarm，本项目 alarm 尾部无条件重设（chat-room.ts finally 块），漏 deleteAlarm 即僵尸 DO 永久每日唤醒（Pitfall 1 最高价值事实）"
    - "kick close 语义（planner 裁定记入决策）：close(1008, \"channel key reset\")（kick-all）与 close(1008, \"channel deleted\")（purge 内）——web SDK 不区分 close code 一律翻译为重连（实证），E2E 断言只看离开 online"
---

<objective>
**As a** 管理员，**I want to** 重置 Channel Key（踢出全部旧连接且历史保留）和硬删除频道（三前缀键 + DO 数据全清），**so that** 任一级密钥泄露可单独重置不影响其他级（KEY-04），废弃频道可彻底清理不残留僵尸 DO（KEY-02/D-34）。

本 plan 交付两个破坏性编排操作垂直切片：DO /kick-all + /purge 路由（deleteAll+deleteAlarm 成对）+ reset-channel-key/delete 两路由（编排顺序定稿）+ 管理页确认框交互（逐字契约）+ E2E 切片三（viewer 被踢观察）。

Purpose: SC2 的「重置踢连 + 历史保留 + 60s 窗口文档化」与 D-34 的「删除全清理」是本阶段密钥生命周期的闭合环。
Output: kick-all/purge 路由、reset/delete 路由、两个测试文件、重置/删除 UI。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-keys/03-CONTEXT.md
@.planning/phases/03-admin-keys/03-RESEARCH.md
@.planning/phases/03-admin-keys/03-PATTERNS.md
@.planning/phases/03-admin-keys/03-UI-SPEC.md
@.planning/phases/03-admin-keys/03-02-SUMMARY.md

@packages/server/src/chat-room.ts
@packages/server/src/admin.ts
@packages/server/src/keys.ts
@packages/server/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DO /kick-all + /purge 路由 + reset-channel-key/delete 路由（编排顺序定稿）+ 两测试文件</name>
  <files>packages/server/src/chat-room.ts, packages/server/src/admin.ts, packages/server/src/keys.ts, packages/server/test/admin-reset-kick.test.ts, packages/server/test/admin-delete.test.ts</files>
  <read_first>packages/server/src/chat-room.ts（全文——fetch 路由表/publish 死连接 close 先例 295-297/alarm 自愈重设 finally 块 491-493/sendHistory 首拉）, packages/server/src/admin.ts（03-02 后参数化路由骨架——reset/delete 分支扩展点）, packages/server/src/keys.ts（03-02 后全部写路径函数）, packages/server/src/index.ts（INTERNAL_ORIGIN/VERIFIED_HEADER/SEND_KEY_HEADER 转发常量与两处转发先例）, packages/server/test/admin-channels.test.ts（wsRequest/nextMessage helper——踢连 close 断言复用）, .planning/phases/03-admin-keys/03-RESEARCH.md（§Pattern 3/4 + §Example 3 + §Pitfall 1——编排顺序与 deleteAll/alarm 官方语义）</read_first>
  <behavior>
    - POST /api/admin/channels/:id/reset-channel-key → 201 {channelKey: 新 phc_ 36 字符}（name/sendKeys/createdAt 原样保留）
    - 重置前已建立的 WS 连接（accept + 首拉已收）在重置后收到 close 事件，code 1008
    - 重置后旧 channelKey 走 wsRequest → 401 invalid_key（本地无缓存确定性成立；注释注明生产 60s 双活窗口）
    - 重置前发的 2 条消息：重置后用新 channelKey 连 WS 首拉 history 帧含这 2 条（历史完整保留）
    - 重置后原 Send Key 仍可 POST /api/send 200（重置只动 Channel Key——分级重置互不影响，KEY-04）
    - DELETE /api/admin/channels/:id → 204 空体
    - 删除后：listChannels 不含该频道；resolveChannelKey(旧值) 与 resolveSendKey(每个已建 Key) 均 null
    - 删除前频道发过消息（alarm 已设）：删除后经 env.CHANNELS.getByName(channelId) 直达 DO stub 断言 storage.getAlarm() === null 且 messages 表行数为 0（runInDurableObject 先例，01-03 模式）
    - DELETE 不存在 channelId → 404 not_found；无鉴权 → 401
    - purge 对已清空 DO 重放是 no-op（幂等重试安全——二次 DELETE 同频道仍 204 或 404 均不抛错）
  </behavior>
  <action>
    chat-room.ts（D-33/D-34 + RESEARCH Example 3）：
    (a) fetch 路由新增 POST /kick-all 分支（X-PH-Verified 校验后）：this.ctx.getWebSockets() 遍历 try ws.close(1008, "channel key reset") catch 忽略（已死连接——publish 死连接清理同款容错），计数；返回 200 JSON {kicked}；close code/reason 值按 planner 裁定（1008 policy violation——记入 SUMMARY 决策表，Phase 5/6 客户端展示可细化 reason）；
    (b) fetch 路由新增 POST /purge 分支：三步成对执行——踢连（同款遍历 close(1008, "channel deleted")）→ await this.ctx.storage.deleteAll()（SQLite-backed 清整库含 SQL 数据与 KV 型数据，官方原子语义）→ await this.ctx.storage.deleteAlarm()（必须显式——deleteAll 不删 alarm；本项目 alarm 自愈重设使漏删后果为僵尸 DO 每日唤醒，Pitfall 1）；返回 200 JSON {kicked}；注释注明 deleteAll/deleteAlarm 成对红线。
    admin.ts（D-35 路由 + 编排顺序）：
    (c) POST /api/admin/channels/:channelId/reset-channel-key（body 无）：keys.ts resetChannelKey(env, channelId)（miss → 404）——内部顺序恒定：KV delete ch:<旧> → KV put ch:<新>（值 {channelId, name, createdAt} 原样）→ KV put id: 重写（新 channelKey + sendKeys/name/createdAt 不变）；随后 Worker 转发 DO POST /kick-all（X-PH-Verified 头，转发照 index.ts 模式）；201 返回 {channelKey: 新值}——顺序 KV 写先 DO 踢后（key_links 论证：反序制造旧 Key 重挂后无人再踢窗口）；DO 转发失败不阻断 201（踢连是尽力语义，KV 已切换——60s 窗口后旧 Key 自然失效，注释说明）；
    (d) DELETE /api/admin/channels/:channelId：keys.ts 读 id:（经 normalize，miss → 404）→ 先 DO POST /purge 转发（env.CHANNELS.getByName(channelId)）→ 后 keys.ts deleteChannelKeys(env, record)：顺序 await 逐键 KV delete ch:<旧 channelKey>、id:<channelId>、每个 sk:<key>（record.sendKeys 全列表——最多 12 次删除，不同 key 无 1 写/秒限制，KV delete 幂等，额度核算无需分批）→ 204（Response(null, {status: 204})）；顺序 DO 先 KV 后（key_links 论证：反序产生不可达孤儿 DO；正序部分失败可重试）。
    keys.ts：resetChannelKey(env, channelId)（生成 generateChannelKey + 三键写编排，返回新 record）与 deleteChannelKeys(env, record)（逐键 delete）两函数，收敛键空间红线。
    测试两文件照 admin-channels.test.ts 骨架：admin-reset-kick.test.ts 覆盖 behavior 前 5 条（WS close 监听：socket accept 后 addEventListener("close") 预挂再触发重置——attach-before-trigger 铁律）；admin-delete.test.ts 覆盖后 5 条（DO 直达断言用 runInDurableObject(env, channelId, (instance, state) => …) 取 getAlarm 与 sql 计数——cloudflare:test 导入先例 STATE.md 已记）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/server test -- admin-reset-kick admin-delete && pnpm --filter @pushhub/server test && pnpm --filter @pushhub/server run typecheck</automated>
  </verify>
  <done>两新测试文件全部 behavior 用例绿（含 close 1008 断言、历史保留对照、getAlarm null 断言）；server 全量零回归；typecheck 绿；编排顺序与成对清理在代码注释中各有一句红线说明。</done>
  <acceptance_criteria>
    - chat-room.ts 存在 /kick-all 与 /purge 分支；/purge 内 deleteAll 与 deleteAlarm 两个调用成对出现且顺序为 deleteAll 在前
    - /kick-all 与 /purge 的 close 调用第一参数为 1008，reason 分别为 "channel key reset" 与 "channel deleted"
    - admin.ts reset 分支中 KV 写操作（keys.ts resetChannelKey 调用）先于 DO kick-all 转发；delete 分支中 DO purge 转发先于 deleteChannelKeys 调用
    - admin-reset-kick.test.ts 含：close 事件 code 断言、旧 Key 401 断言、历史保留断言（首拉含重置前消息）、Send Key 存活断言
    - admin-delete.test.ts 含：三前缀 miss 断言与 DO getAlarm() === null 断言（runInDurableObject）
    - pnpm --filter @pushhub/server test 退出码 0（全量零回归）
  </acceptance_criteria>
  <reversibility rating="costly">D-34 硬删除语义（one-way）已在 discuss-phase 由用户锁定——本任务是其实现：代码可回滚（costly 级：路由与编排顺序进 Worker↔DO 契约）；测试在 miniflare 隔离存储内执行（每测试独立存储，无真实数据损失）。生产数据的首次真实 one-way 删除在 03-05 Task 3 由用户经管理页亲手执行（checkpoint:human-verify 即该 one-way 门的确认机制）。</reversibility>
</task>

<task type="auto">
  <name>Task 2: 管理页重置与删除交互——确认框逐字契约 + 新密钥一次性展示 + 前缀联动删除</name>
  <files>packages/server/public/admin.js, packages/server/public/admin.html</files>
  <read_first>packages/server/public/admin.js（03-02 后全部既有组件——密钥行/片段卡/错误条/dialog 用法）, .planning/phases/03-admin-keys/03-UI-SPEC.md（Destructive 确认框三契约逐字 + 色彩保留清单 + 60s 提示条样式）, packages/server/src/admin.ts（Task 1 后 reset/delete 契约：201 {channelKey} / 204）</read_first>
  <action>
    admin.html：详情面板新增「Channel Key」区块（h2 + 密钥行 + 「重置 Channel Key」红字按钮——Destructive 保留清单第 3 项）与底部「删除频道」红按钮（保留清单第 1 项）；两个原生 dialog（重置确认/删除确认）+ 60s 提示条容器（1px solid #d9a300 边框式，非填充）。
    admin.js（D-33/D-34 + UI-SPEC 逐字契约）：
    (a) 重置流程：点「重置 Channel Key」→ dialog 标题「重置 Channel Key？」正文逐字（三句：客户端立即断开/旧密钥最长约 1 分钟后全局失效（边缘缓存窗口）/频道历史消息完整保留）按钮 [取消][确认重置]（红）→ 确认 POST reset-channel-key → 201 后：新 channelKey 明文一次性展示块（mono + 复制按钮，同接入片段卡样式）+ 提示条文案「已重置。旧密钥最长约 1 分钟内仍可能被边缘缓存放行，之后全局失效。」（#d9a300 边框式）+ 刷新频道数据（掩码行更新为新 Key 掩码）；
    (b) 删除流程：点「删除频道」→ dialog 标题「删除频道「{name}」」正文逐字（硬删除不可恢复：全部消息历史将被清空，Channel Key 与所有 Send Key 立即失效，所有连接立即断开。输入频道名称以确认：）+ input（placeholder「输入频道名称的开头部分」）+ 删除按钮初始 disabled（文字 #9a9a9a）；input 事件联动：input.value.length > 0 且 channel.name.startsWith(input.value) 时启用按钮（前缀匹配——GitHub 模式宽松变体）→ 确认 DELETE → 204 后：列表移除该频道 + 详情面板回空态「在左侧选择一个频道查看详情。」+ 关闭 dialog；确认按钮文案「我已理解后果，删除频道」；
    (c) 两操作进行中按钮 disabled + 「加载中…」；错误走既有错误条（信封透传）；确认框用 dialog.showModal()（原生焦点陷阱，Esc 关闭不触发操作）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/web-sdk run build && node -e "const s=require('fs').readFileSync('packages/server/public/admin.js','utf8'); for (const t of ['重置 Channel Key？','我已理解后果，删除频道','输入频道名称的开头部分','在左侧选择一个频道查看详情。']) { if (!s.includes(t)) { console.error('MISSING: '+t); process.exit(1); } } console.log('UI contract tokens OK')"</automated>
  </verify>
  <done>四个逐字契约 token（重置标题/删除按钮/placeholder/空态）在 admin.js 在位（node token 检查通过）；前缀联动 startsWith 逻辑实现；60s 提示条为边框式非填充。真浏览器行为由 Task 3 E2E 断言。</done>
  <acceptance_criteria>
    - admin.js 重置确认正文含「最长约 1 分钟后全局失效」与「频道历史消息完整保留」两句
    - 删除联动逻辑使用 startsWith 判定且空输入禁用（input.value.length > 0 条件在位）
    - 删除成功路径包含详情面板空态文案渲染
    - 60s 提示条样式含 #d9a300 且为 border 呈现（非 background 填充）
    - pnpm --filter @pushhub/web-sdk run build 退出码 0 且 token 检查输出 UI contract tokens OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: E2E 切片三——重置踢连（viewer 双页观察）+ 历史保留 + 删除前缀交互</name>
  <files>packages/web-sdk/e2e/admin.spec.ts</files>
  <read_first>packages/web-sdk/e2e/admin.spec.ts（切片一/二——登录/创建 helper 复用）, packages/web-sdk/e2e/viewer.spec.ts（waitViewerOnline 模式——本任务写其反断言）, packages/server/public/viewer.js（URL 参数 ?server=&key= 自动连接——重连观察页用法）, .planning/phases/03-admin-keys/03-RESEARCH.md（§Pitfall 5——踢断言写法红线：断言离开 online，勿断言进入 offline）</read_first>
  <action>
    admin.spec.ts 追加 3 个 test（Playwright 多 page：pageA 开 /admin.html，pageB 开 viewer 页连目标频道）：
    (a) 重置踢连端到端：建频道 + 发 2 条消息 → pageB 以 URL 参数连 viewer（waitViewerOnline 同款等待）→ pageA 走重置确认框（断言 dialog 文本含「最长约 1 分钟」）确认 → 断言 pageB 的 #status-dot 类名不再包含 dot-online（Pitfall 5 红线：SDK 退避重连永不 fatal，勿断言 offline/已断开文案；15s 超时窗口）→ 用新 channelKey 让 pageB 重新以 URL 参数连接 → online 恢复且首拉消息含重置前 2 条（历史保留端到端证据）；
    (b) 旧 Key 失效：重置后旧 channelKey 经 request.get /api/ws/<旧 Key>（Upgrade 头）断言 401（本地无缓存立即生效）；
    (c) 删除交互：建频道 → 打开删除 dialog → 断言按钮初始 disabled → 输入频道名的错误后缀（非前缀，如 name 反转片段）断言仍 disabled → 输入正确前缀断言启用 → 确认 → 频道名从 #channel-list 消失 + 详情空态文案「在左侧选择一个频道查看详情。」可见。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/web-sdk e2e -- admin.spec.ts</automated>
  </verify>
  <done>切片三 3 test 全绿；切片一/二 7 test 无回归；SC2 端到端三证据齐（踢连离开 online + 旧 Key 401 + 历史保留）。</done>
  <acceptance_criteria>
    - 踢连断言为 dot-online 消失（断言表达式为类名不包含判断），全 spec 无对 offline/已断开 终态的等待
    - 历史保留断言：重连 online 后消息列表（或首拉）含重置前发送的 2 条消息文本
    - 删除交互断言覆盖 disabled 初始态、错误输入仍 disabled、正确前缀启用、删除后列表消失与空态文案五要素
    - pnpm --filter @pushhub/web-sdk e2e -- admin.spec.ts 退出码 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 管理员浏览器 → Worker reset/delete 路由 | Bearer Admin Key；破坏性操作入口 |
| Worker → DO /kick-all、/purge | X-PH-Verified 内部头；purge 触发不可逆存储清除 |
| 被踢客户端 → Worker /api/ws/:key（重连） | 旧凭据重放跨越；KV 缓存窗口内的双活边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Denial of Service | DELETE /channels/:id 误触/恶意调用 | high | mitigate | Bearer 鉴权（API 层）+ 前缀确认交互（UI 层）双层；硬删语义 D-34 用户已锁定接受；编排 DO 先 KV 后保证失败可重试不产生孤儿 |
| T-03-12 | Elevation of Privilege | 旧 Channel Key 重连重挂（重置后） | high | mitigate | 编排顺序 KV 写先 DO 踢后（杜绝反序的无限重挂窗口）；残余 ≤60s 边缘缓存窗口已在确认框/提示条/文档三处明示（accept——KV 平台语义） |
| T-03-13 | DoS（资源耗尽） | 僵尸 DO 每日唤醒（删除漏 deleteAlarm） | high | mitigate | /purge 内 deleteAll + deleteAlarm 成对（代码红线注释）+ 测试断言 getAlarm() === null（runInDurableObject） |
| T-03-14 | Spoofing | DO /kick-all、/purge 伪造直连 | medium | mitigate | 新分支置于 X-PH-Verified 校验之后（结构继承）；DO 仅经 binding 可达双重防线 |
| T-03-15 | Tampering | 删除确认框绕过（前端联动被绕） | low | accept | 前缀联动是 UX 防误触层；真实防线是 Bearer Admin Key（API 不依赖确认框）——单管理员密钥即身份模型 |
</threat_model>

## Artifacts this phase-plan produces

- **API 路由**：`POST /api/admin/channels/:channelId/reset-channel-key`（201 {channelKey}）；`DELETE /api/admin/channels/:channelId`（204）——admin.ts 参数化骨架四分支全部落位（messages 分支留 03-04）
- **DO 内部路由**：`POST /kick-all`（close 1008 "channel key reset"，响应 {kicked}）；`POST /purge`（踢连 + deleteAll + deleteAlarm，响应 {kicked}）
- **keys.ts 函数**：`resetChannelKey(env, channelId)`、`deleteChannelKeys(env, record)`
- **决策记录**：kick close code 1008 + reason 双值（planner 裁定，SUMMARY 决策表登记——CONTEXT Discretion 要求）
- **测试**：packages/server/test/admin-reset-kick.test.ts、admin-delete.test.ts；e2e/admin.spec.ts 切片三（3 test）
- **UI**：Channel Key 区块、重置/删除 dialog、新密钥一次性展示、60s 提示条、删除前缀联动

<verification>
- Task 1 后：admin-reset-kick + admin-delete 测试 + server 全量 + typecheck 三绿
- Task 2 后：build 绿 + 四逐字契约 token 检查通过
- Task 3 后：admin.spec.ts 10 test 全绿（4+3+3）
</verification>

<success_criteria>
1. KEY-04 端到端：重置 → 踢连（close 1008 + viewer 离开 online）→ 旧 Key 401 → 历史保留 → Send Key 不受影响
2. KEY-02 删除闭环：三前缀键全清 + DO 数据清空 + alarm 删除（测试三断言）
3. 编排顺序（重置 KV 先 DO 后 / 删除 DO 先 KV 后）在实现与注释中定稿
4. server 全量测试零回归
</success_criteria>

<output>
Create `.planning/phases/03-admin-keys/03-03-SUMMARY.md` when done
</output>
