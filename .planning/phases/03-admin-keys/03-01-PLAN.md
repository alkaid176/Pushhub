---
phase: 03-admin-keys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/keys.ts
  - packages/server/test/admin-channels.test.ts
  - scripts/smoke.mjs
  - packages/web-sdk/e2e/viewer.spec.ts
  - packages/web-sdk/e2e/reconnect.spec.ts
  - packages/web-sdk/e2e/tracer.spec.ts
  - packages/server/public/admin.html
  - packages/server/public/admin.js
  - packages/web-sdk/build.mjs
  - packages/web-sdk/test/cache-bust-sync.test.ts
  - packages/web-sdk/e2e/admin.spec.ts
autonomous: true
requirements: [ADM-01, ADM-05, KEY-02]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "管理员浏览器打开 /admin.html，无 localStorage pushhub.admin 时仅渲染登录卡；输入 Admin Key 登录后看到频道列表与创建表单（D-28/ADM-01）"
    - "输入频道名点「创建频道」：201 后左栏列表出现新频道、自动选中，详情面板顶部展示接入片段卡三块——curl 示例（含 {origin}/api/send 与 Bearer Send Key）、Channel Key 明文+复制、viewer 直达链接（target=_blank + rel=noopener,noreferrer）（D-39）"
    - "错误 Admin Key 登录显示错误条「Admin Key 无效（invalid_key）：请重新输入。」并停留登录屏障；已登录状态遇任何 API 401 清除 localStorage pushhub.admin 回登录屏障（UI Considerations error，D-06 信封 code/message 透传）"
    - "频道列表空态显示「还没有频道」标题与空态正文；0/1/N 个频道同一列表形态，列表区 max-height + overflow-y 独立滚动（UI Considerations empty/zero-one-many/populated）"
    - "密钥默认掩码显示为 key 前 7 字符 + 省略号 + 后 4 字符（如 phc_Ab3…xYz），点眼睛按钮切换明文（inline SVG + aria-label），复制按钮写完整密钥并显示 data-copied 反馈（D-29）"
    - "首次加载列表显示「加载中…」文本占位；全部提交按钮提交期间 disabled（UI Considerations loading）"
    - "GET /api/admin/channels 响应中每个频道含 sendKeys 数组（元素 {key, label, createdAt}），不再含 sendKey 单值字段（D-30/D-35 schema 演进）；生产旧格式 id: 记录经服务端 normalize 兼容列出，前端无感知（UI Considerations partial）"
    - "POST /api/admin/channels 201 响应结构为 {channelId, channelKey, sendKeys: [{key, label: null, createdAt}], name, createdAt}——创建即获得 Channel Key 与 Send Key（SC1）"
    - "admin.html 引用的 /pushhub.js?v= 由 build.mjs 构建期注入根版本号（恰一次硬断言），与 index.html 注入互不影响；cache-bust-sync.test.ts 双文件断言恒一致（ADM-05/SC4 缓存正确性）"
    - "E2E 断言：GET /admin.html 响应 200 且无 x-ph-worker 响应头；GET /api/admin/channels 响应带 x-ph-worker: 1（SC4/ADM-05 程序化对照——管理页静态资产命中不触发 Worker）"
    - "浏览器 console 无 CSP 违规报告（admin.html 原样复制 viewer 的 CSP meta，script-src 'self' 禁 inline script——Pitfall 3）"
  artifacts:
    - "packages/server/src/keys.ts：SendKeyRecord 接口、ChannelRecord.sendKeys 演进、normalizeIdRecord 兼容层、createChannel 恒写新格式"
    - "packages/server/public/admin.html（新：CSP + 登录卡 + 左栏创建表单/频道列表 + 右栏详情面板骨架）"
    - "packages/server/public/admin.js（新：登录屏障/列表/创建/接入片段卡/掩码行/错误条）"
    - "packages/web-sdk/build.mjs 扩展：admin.html 的 pushhub.js?v= 注入（恰一次断言）"
    - "packages/web-sdk/test/cache-bust-sync.test.ts 扩展：admin.html 双断言"
    - "packages/web-sdk/e2e/admin.spec.ts（新：管理页 E2E，后续 plan 递进扩展）"
  key_links:
    - "schema 演进 sendKey → sendKeys[] 与四个消费方同版本联动：admin-channels.test.ts 断言、scripts/smoke.mjs 取值路径、e2e viewer/reconnect/tracer 三 spec 的 createChannel helper——任何一处绿着合入必然漏改另一处（Pitfall 4）"
    - "build.mjs ?v= 注入（恰一次硬断言）→ admin.html script 引用 → 浏览器拿到的 SDK 字节与部署版本对应（静默失效型集成点：漏扩展则构建仍绿但线上管理页吃 stale SDK）"
    - "admin.js 全部 API 调用 = 同源相对路径 + Authorization: Bearer 头（无 CORS/CSRF 面）→ Worker /api/admin/ 前缀分发 → checkAdminAuth（鉴权先于路由判定）"
  backstop:
    - statement: "E2E 揭示态下完整密钥（36+ 字符 mono）以 overflow-wrap: anywhere 呈现，不撑破 #channel-detail 面板布局（UI Considerations overflow，held-out 断言）"
      verification: backstop
---

<objective>
**As a** 管理员，**I want to** 在浏览器里登录管理页并创建频道、立即拿到 Channel Key 与 Send Key 的可复制接入片段，**so that** 无需任何命令行即可完成频道接入的第一步。

本 plan 是 Phase 3 的 tracer 切片：一次性完成 `id:`/`sk:` KV schema 演进（多 Send Key 的地基，D-30/D-35）+ 管理页骨架端到端（登录屏障 → 频道列表 → 创建 → 接入片段卡 → 密钥掩码，D-28/D-37/D-38/D-39）+ build.mjs ?v= 注入扩展（静默失效联动点）+ 第一组真浏览器 E2E。

Purpose: 管理页核心用户旅程（登录 → 建频道 → 拿密钥）先行落地并验证 SC4 静态资产零 Worker 请求；schema 演进先于一切多 Key 功能，避免后续 plan 返工。
Output: admin.html/admin.js 静态资产、keys.ts 新 schema、admin.spec.ts 切片一、build.mjs/cache-bust-sync 扩展。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-keys/03-CONTEXT.md
@.planning/phases/03-admin-keys/03-RESEARCH.md
@.planning/phases/03-admin-keys/03-PATTERNS.md
@.planning/phases/03-admin-keys/03-UI-SPEC.md

@packages/server/src/keys.ts
@packages/server/src/admin.ts
@packages/server/public/index.html
@packages/server/public/viewer.js
@packages/web-sdk/build.mjs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: keys.ts id:/sk: schema 演进（normalize 兼容读 + 恒写新格式）+ 四消费方同任务联动</name>
  <files>packages/server/src/keys.ts, packages/server/test/admin-channels.test.ts, scripts/smoke.mjs, packages/web-sdk/e2e/viewer.spec.ts, packages/web-sdk/e2e/reconnect.spec.ts, packages/web-sdk/e2e/tracer.spec.ts</files>
  <read_first>packages/server/src/keys.ts（全文——三前缀键表与 createChannel/listChannels 现状）, packages/server/test/admin-channels.test.ts（断言联动点：201 三件套与 toEqual）, scripts/smoke.mjs（第 79-89 行 sendKey 取值与正则断言）, packages/web-sdk/e2e/viewer.spec.ts（createChannel helper）, packages/web-sdk/e2e/reconnect.spec.ts（同型 helper）, packages/web-sdk/e2e/tracer.spec.ts（同型 helper）, .planning/phases/03-admin-keys/03-RESEARCH.md（§Pattern 2 schema 演进与 §Pitfall 4 联动点）</read_first>
  <action>
    keys.ts 演进（D-30/D-35）：
    (a) 新增导出接口 SendKeyRecord，字段 key: string、label: string | null、createdAt: number；
    (b) ChannelRecord 的 sendKey: string 字段替换为 sendKeys: SendKeyRecord[]；CreatedChannel 同步；
    (c) SendKeyInfo 接口（sk: 值结构）增加可选字段 label?: string | null——sk: 旧值 {channelId} 天然合法，新写入含 label（D-30）；
    (d) 新增模块内 normalize 兼容层：读取 id: 原始 JSON 后，若存在旧格式顶层字段 sendKey（typeof string），映射为 {sendKeys: [{key: 旧值, label: null, createdAt: 顶层 createdAt}]}；已是新格式（有 sendKeys 数组）原样通过；listChannels 的逐键 get 之后统一走该函数——生产 10+ 条旧格式冒烟频道照常列出（migrate-on-write：任何被管理操作触碰的频道才升级，本任务不写迁移脚本、不直改生产键空间）；
    (e) createChannel 恒写新格式：sk: put 值 {channelId, label: null}；id: put 值 {channelKey, sendKeys: [{key: sendKey, label: null, createdAt}], name, createdAt}（ch: 值结构不变）；201 返回体即新结构。
    四消费方联动（Pitfall 4——响应结构变更与消费方更新必须同任务完成）：
    (f) admin-channels.test.ts：POST 201 断言中 channel.sendKey 的 phs_ 正则与长度断言改为 channel.sendKeys[0].key；列表 toEqual 断言同步（found 与 created 结构均含 sendKeys 数组）；
    (g) scripts/smoke.mjs：createChannel 步骤中 SEND_KEY 取值从 channel.sendKey 改为 channel.sendKeys[0].key；紧邻的 phs_ 正则断言同步作用于新取值路径；
    (h) e2e 三个 spec 的 createChannel helper：保持 helper 返回类型形态不变（ChannelInfo 含 channelKey/channelId/sendKey 三字段），实现改为从响应 sendKeys[0].key 取值填充 sendKey 字段——helper 消费点零改动，最小侵入。
    键空间纪律：全部 KV 写仍收敛在 keys.ts（红线注释已注明），不新增绕过写路径。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/server test -- admin-channels && pnpm --filter @pushhub/server run typecheck && pnpm --filter @pushhub/web-sdk run test && pnpm --filter @pushhub/web-sdk e2e</automated>
  </verify>
  <done>server admin-channels 测试全绿（新结构断言）；server typecheck 零错误；web-sdk 单测全绿；web-sdk 全部 E2E（viewer/reconnect/tracer）全绿——四个消费方无一漏改；keys.ts 无 sendKey 单值字段残留（ChannelRecord/CreatedChannel 接口层面）。</done>
  <acceptance_criteria>
    - packages/server/src/keys.ts 存在 export interface SendKeyRecord 且 ChannelRecord 含 sendKeys: SendKeyRecord[] 字段
    - listChannels 读路径经 normalize：旧格式 {channelKey, sendKey: "phs_...", name, createdAt} 输入映射出 sendKeys 单元素数组（label: null、createdAt 取顶层值）
    - createChannel 写入的 id: 值 JSON 含 sendKeys 数组键名；sk: 值含 label 键
    - admin-channels.test.ts 中不存在对顶层 channel.sendKey 的 toMatch/length 断言（已全部指向 sendKeys[0].key）
    - scripts/smoke.mjs 中 SEND_KEY 赋值表达式取自 channel.sendKeys[0].key
    - e2e 三个 spec 的 createChannel helper 均从响应 sendKeys[0].key 取值，helper 返回类型声明不变
    - pnpm --filter @pushhub/server test -- admin-channels 退出码 0；pnpm --filter @pushhub/web-sdk e2e 全部 spec 通过
  </acceptance_criteria>
  <reversibility rating="costly">KV id:/sk: 值结构变更（D-30 标注 costly）；normalize 兼容层保证旧数据零破坏，新格式回读旧代码不兼容——演进即定型为 Phase 3 契约。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: admin.html + admin.js 骨架（登录/列表/创建/接入片段/掩码）+ build.mjs ?v= 注入扩展</name>
  <files>packages/server/public/admin.html, packages/server/public/admin.js, packages/web-sdk/build.mjs, packages/web-sdk/test/cache-bust-sync.test.ts</files>
  <read_first>packages/server/public/index.html（CSP meta 原文 + system-ui 样式基调 + pre 样式）, packages/server/public/viewer.js（localStorage try/catch 模式、textContent 纪律、错误条形态）, packages/web-sdk/build.mjs（?v= 注入段 47-67 行——扩展形态的 analog）, packages/web-sdk/test/cache-bust-sync.test.ts（双断言结构）, .planning/phases/03-admin-keys/03-UI-SPEC.md（全文——token/文案/交互契约逐字执行）, packages/server/src/keys.ts（Task 1 后的响应结构）</read_first>
  <behavior>
    - 载入无 pushhub.admin 存储 → 仅登录卡可见（#app hidden）
    - 登录提交 → GET /api/admin/channels 验证 → 成功渲染主界面；401 → 错误条文案 + 停留登录屏障
    - 创建频道 201 → 列表新增 + 自动选中 + 片段卡三块渲染（curl 块含 origin 与 sendKey 明文）
    - 掩码函数输出 = key.slice(0,7) + "…" + key.slice(-4)
    - 已存储 key 的 401 响应（会话中途失效）→ 清 localStorage 回登录卡
    - build 后 admin.html 与 index.html 的 ?v= 均等于根 package.json version，各恰一处
  </behavior>
  <action>
    admin.html（D-37/D-38 + UI-SPEC 逐字契约）：
    (a) 原样复制 index.html 的 CSP meta（default-src 'self'; script-src 'self'; … 全串）——物理两文件（CSP 禁 inline script，Pitfall 3）；
    (b) 结构：顶栏（h1 "PushHub 管理" + 右侧「刷新」「登出」文字按钮）；登录卡 #login-form（说明文案按 UI-SPEC Copywriting 表 + input type=password placeholder「粘贴 Admin Key」+「登录」绿色主按钮）；主界面 #app（≥800px 两栏：左栏 300px = 创建表单在上 + #channel-list 在下；右栏 flex:1 = #channel-detail）；
    (c) 引入两条外链：/pushhub.js?v=（写占位版本值，构建注入替换）+ /admin.js；
    (d) 样式 token：间距仅 4/8/16/24/32；字号恰 4 个（16/13.6/18/22.4px）字重恰 2 个（400/700）；色彩 canvas/canvastext 系统色 + 语义色 #2e9e5b（仅 4 保留项）/ #c0392b（仅 4 保留项）/ #9a9a9a / #d9a300 / graytext；卡片 1px solid canvastext + radius 8px + 内边距 8px 16px；密钥/时间戳等宽 ui-monospace 13.6px；
    (e) 频道列表项结构：频道名 + 创建日期（13.6px graytext）；选中态 3px 绿色左指示条 + 文字 700 + aria-current="true"；空态/加载中文案按 UI-SPEC。
    admin.js（D-28/D-29/D-39）：
    (f) localStorage 键 pushhub.admin（独立于 viewer 两键），读写均 try/catch（WR-03 先例）；
    (g) 登录屏障逻辑：载入无存储仅登录卡；有存储直接主界面并发 GET /api/admin/channels 验证；任何 401 → 清存储回登录卡；登出 = 清存储回登录卡；全部 fetch 同源相对路径 + Authorization: Bearer 头；
    (h) 创建频道 POST /api/admin/channels {name}（input maxlength=64 + required）→ 201 后刷新列表、选中新频道、渲染接入片段卡：卡标题「已创建「{name}」——请复制以下接入信息…」+ 第 1 块 bash 代码（curl -X POST {origin}/api/send + Bearer {sendKey} + JSON 载荷示例，{origin}=window.location.origin）+ 第 2 块（服务端地址 + Channel Key 明文 + 复制）+ 第 3 块（{origin}/?server={origin}&key={channelKey} 链接，target="_blank" rel="noopener noreferrer" + 复制）+ [已保存，关闭] 按钮；三块各自带独立复制按钮；sendKey 取自 201 响应 sendKeys[0].key；
    (i) 密钥行组件：[掩码 mono] [眼睛按钮 inline SVG aria-label] [复制按钮] gap 8px；掩码 = key.slice(0,7)+"…"+key.slice(-4)；揭示切换 textContent（禁 innerHTML 写密钥）；复制 = navigator.clipboard.writeText(完整密钥) + 按钮 textContent「已复制」1.5s 复原 + data-copied 属性；
    (j) textContent 纪律：频道名/日期/密钥/错误消息一律 textContent；本任务尚无 innerHTML 入口（历史视图是 Plan 04）；
    (k) 错误条：#c0392b 底白字 radius 6px word-break break-all，文案「{操作}失败（{code}）：{message}——请修正后重试。」（D-06 信封透传）；401 特例文案「Admin Key 无效（invalid_key）：请重新输入。」；网络级失败固定文案；无自动重试（手动刷新即重试入口）；
    (l) 刷新按钮手动重拉列表——禁止任何自动轮询（KV list 独立 1,000 次/天额度红线）；提交/加载期间按钮 disabled + 「加载中…」。
    build.mjs + cache-bust-sync.test.ts（Pattern 6 静默失效联动点）：
    (m) build.mjs 对 ../server/public/admin.html 复制 index.html 同款「读—恰一次命中断言—替换」逻辑（两文件各自恰一次，命中数 ≠1 即 console.error + process.exit(1)）；
    (n) cache-bust-sync.test.ts 增加对 admin.html 的两条同型断言（?v= === 根 version 且恰一次；执行一次构建后仍成立）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/web-sdk run build && pnpm --filter @pushhub/web-sdk run test -- cache-bust-sync && pnpm --filter @pushhub/server run typecheck</automated>
  </verify>
  <done>构建成功且控制台出现两行注入日志（index.html 与 admin.html 各一行 ?v= 注入）；cache-bust-sync 扩展断言绿；admin.html/admin.js 落位 packages/server/public/（wrangler assets 自动托管，零配置改动）；页面在浏览器无 CSP 违规（console 干净——由 Task 3 E2E 断言）。</done>
  <acceptance_criteria>
    - packages/server/public/admin.html 存在且 CSP meta 与 index.html 逐字节同串；引用恰一处 pushhub.js?v= 与恰一处 /admin.js
    - admin.js 中 localStorage 键名为字符串 pushhub.admin，读写均在 try/catch 块内
    - admin.js 中掩码表达式含 slice(0, 7) 与 slice(-4) 调用（掩码格式 UI-SPEC 锁定值）
    - 接入片段卡含三块内容：curl 块（bash 代码，含 /api/send 与 Bearer）、Channel Key 块、viewer 链接块（含 target="_blank" 与 noopener）
    - build.mjs 对 admin.html 的注入存在独立 hits.length !== 1 失败分支
    - cache-bust-sync.test.ts 断言覆盖 admin.html（恰一次 + 等于根 version）
    - pnpm --filter @pushhub/web-sdk run build 输出含 admin.html 注入日志行且退出码 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: E2E admin.spec.ts 切片一——登录/创建/片段卡/掩码/SC4 标记头对照</name>
  <files>packages/web-sdk/e2e/admin.spec.ts</files>
  <read_first>packages/web-sdk/e2e/viewer.spec.ts（spec 骨架：BASE/ADMIN_KEY 常量、createChannel helper、waitForFunction 模式）, packages/web-sdk/playwright.config.ts（webServer 编排——零新配置）, packages/server/public/admin.js（Task 2 产物——断言锚点对应实现）, .planning/phases/03-admin-keys/03-RESEARCH.md（§Pitfall 5/6——踢连与剪贴板断言注意，本任务只涉 Pitfall 6 剪贴板）</read_first>
  <action>
    新建 packages/web-sdk/e2e/admin.spec.ts（D-41 沿用 D-26 模式，挂既有 playwright.config.ts webServer，零新配置）。E2E 锚点用 UI-SPEC #8 惯例（#login-form、#channel-list、#channel-detail）+ data-testid（snippet-card 等）。本切片写 4 个 test：
    (a) 登录反例：goto /admin.html → 输入错误 key 提交 → 断言错误条出现且文案含 invalid_key → 主界面仍隐藏（#app hidden 属性在位）；
    (b) 登录+创建+片段卡：正确 key 登录 → 列表加载 → 输入频道名创建 → 断言：列表出现该频道名且选中态（aria-current="true"）→ 片段卡 data-testid=snippet-card 可见 → curl 块文本含 /api/send 与该频道 sendKeys[0].key 的完整值（经 API 侧 createChannel 响应对照）→ viewer 链接 href 含 server= 与 key= 查询参数且 anchor 带 noopener；剪贴板断言前 grantPermissions(["clipboard-read","clipboard-write"])，点复制按钮断言 data-copied 属性出现（Pitfall 6）；
    (c) 掩码与揭示：频道详情 Channel Key 默认显示掩码形态（正则 ^phc_.{3}….{4}$ 断言 textContent）→ 点眼睛按钮 → textContent 变为完整 36 字符（/^phc_[0-9A-Za-z]{32}$/）→ backstop 断言：揭示态 #channel-detail 的 scrollWidth 不超出其 clientWidth（overflow-wrap: anywhere 生效，UI Considerations overflow held-out）；
    (d) SC4 标记头对照：request 上下文 GET /admin.html 断言 200 且响应头无 x-ph-worker；GET /api/admin/channels（带 Bearer）断言响应头 x-ph-worker 值为 1——资产命中零 Worker 请求 vs API 必经 Worker 的程序化双证据（stampMarker 机制，02-03 先例）。
    每个 test 结束断言无 CSP 违规：收集 page console 错误与 pageerror，出现 CSP 关键字即 fail（Pitfall 3 警示信号）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/web-sdk e2e -- admin.spec.ts</automated>
  </verify>
  <done>admin.spec.ts 切片一 4 个 test 全绿；既有 viewer/reconnect/tracer spec 无回归（Task 1 已联动 helper）；SC4 双断言（资产无标记头 / API 有标记头）在真浏览器+真 wrangler dev 下成立。</done>
  <acceptance_criteria>
    - packages/web-sdk/e2e/admin.spec.ts 存在且含 4 个独立 test（登录反例/登录创建片段/掩码揭示/SC4 标记头）
    - SC4 test 断言 /admin.html 响应头不含 x-ph-worker 且 /api/admin/channels 响应头含 x-ph-worker: 1
    - 掩码 test 断言默认 textContent 匹配 ^phc_.{3}….{4}$ 且揭示后匹配 ^phc_[0-9A-Za-z]{32}$
    - 复制断言使用 grantPermissions 且检查 data-copied 属性
    - pnpm --filter @pushhub/web-sdk e2e -- admin.spec.ts 退出码 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 管理员浏览器 → Worker /api/admin/* | Bearer Admin Key 凭据跨越；全部新 UI 调用经此边界 |
| 浏览器 → 静态资产 /admin.html | 无鉴权公开可达（页面壳）；密钥不在页面内嵌 |
| Worker → KV | 服务端内部；id:/sk: 键空间读写 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing | admin.js 全部 API 调用 | high | mitigate | 全部请求经既有 checkAdminAuth（两段式常时比较 CR-01 形态）——本 plan 不新增路由，既有 POST/GET 复用，零新鉴权面 |
| T-03-02 | Tampering | admin.js 动态内容渲染（频道名/密钥/错误消息） | high | mitigate | textContent 纪律（全文件零 innerHTML 入口）；CSP script-src 'self' 双纵深；E2E 断言 console 无 CSP 违规 |
| T-03-03 | Disclosure | Admin Key localStorage 持有 | medium | accept | D-28 已锁定取舍（密钥即身份，不引入服务端会话）；读取侧 try/catch 防护对齐 WR-03 |
| T-03-04 | Tampering | pushhub.js?v= stale 缓存 | medium | mitigate | build.mjs 恰一次硬断言 + cache-bust-sync 双保险（漏扩展则构建失败而非静默投放旧 SDK） |
| T-03-05 | Information Disclosure | 管理页密钥展示（肩窥/截屏） | medium | mitigate | D-29 掩码默认遮蔽，揭示不跨刷新/跨选择持久（UI-SPEC Interaction #2） |
</threat_model>

## Artifacts this phase-plan produces

- **KV 值结构（演进定型）**：`sk:<key>` → `{channelId, label?}`；`id:<channelId>` → `{channelKey, sendKeys: SendKeyRecord[], name, createdAt}`（SendKeyRecord = `{key, label: string|null, createdAt}`）
- **API 响应结构（演进）**：POST /api/admin/channels 201 体与 GET /api/admin/channels 200 体中 ChannelRecord 含 sendKeys 数组（顶层 sendKey 单值字段移除）
- **函数/类型**：keys.ts 导出 `SendKeyRecord`；模块内 `normalizeIdRecord` 兼容层；ChannelRecord/CreatedChannel 演进
- **静态资产**：packages/server/public/admin.html、packages/server/public/admin.js
- **构建机制**：build.mjs 对 admin.html 的 ?v= 注入（恰一次断言）；cache-bust-sync.test.ts admin.html 双断言
- **测试**：packages/web-sdk/e2e/admin.spec.ts（切片一：4 test）
- **E2E 锚点**：#login-form / #channel-list / #channel-detail / data-testid=snippet-card / data-copied 属性

<verification>
- Task 1 后：server admin-channels 测试 + typecheck + web-sdk 单测 + 全部 E2E 四绿（四消费方联动闭环证据）
- Task 2 后：build 成功 + 注入日志两行 + cache-bust-sync 绿
- Task 3 后：admin.spec.ts 4 test 绿（含 SC4 双断言）
- 全 plan 完成后运行：pnpm test（server + web-sdk 单测全量）确认零回归
</verification>

<success_criteria>
1. 管理员能在浏览器完成：登录（错 key 被拒）→ 创建频道 → 看到接入片段三块 → 掩码/揭示/复制密钥——SC1 前半段 + ADM-01 前半段端到端成立
2. GET /api/admin/channels 返回 sendKeys[] 新结构；旧格式记录 normalize 兼容（服务端测试覆盖 + 生产实证留 Plan 05）
3. /admin.html 静态资产命中零 Worker 请求（E2E 标记头双断言）
4. pushhub.js ?v= 注入对 admin.html 生效（恰一次硬断言 + 恒一致测试）
</success_criteria>

<output>
Create `.planning/phases/03-admin-keys/03-01-SUMMARY.md` when done
</output>
