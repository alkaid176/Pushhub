---
phase: 03-admin-keys
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/server/src/admin.ts
  - packages/server/src/keys.ts
  - packages/server/src/chat-room.ts
  - packages/server/test/admin-send-keys.test.ts
  - packages/server/public/admin.js
  - packages/server/public/admin.html
  - packages/web-sdk/e2e/admin.spec.ts
autonomous: true
requirements: [KEY-03, ADM-02]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "管理员可为选中频道创建带可选标签（≤64 字符，可留空）的 Send Key：201 返回 {key, label, createdAt} 全量，管理页展示该新 Key 的 curl 接入片段卡（同款第 1 块）（D-30/D-39）"
    - "同一频道 Send Key 列表按创建时间展示全部 Key；无标签的 Key 显示「未命名」（graytext）（D-30/UI Considerations partial）"
    - "频道 Send Key 达 10 个后：「创建 Send Key」按钮 disabled 且相邻显示「已达上限（10 个）」提示；第 11 个 API 请求返回 400 code=send_key_limit（D-31——防公网脚本循环建 Key 烧 KV 写额度）"
    - "吊销单个 Send Key：经确认框（逐字文案：标题「吊销 Send Key「{label 或掩码}」？」）确认后该行消失；该 Key 下次 POST /api/send 得 401（本地 miniflare 强一致确定性断言，注释注明生产 ≤60s 边缘缓存窗口）；同频道其余 Key 照常可发（KEY-03 泄露不互伤）"
    - "吊销联动清理：KV sk: 键删除 + id: 反向索引重写（移除该 Key）+ DO rate_sends 表对应行即时删除（D-32——planner 裁定采纳即时清理，决策忠实度优先）"
    - "Send Key 空列表显示空态文案「该频道没有 Send Key——创建一个给脚本使用，不同脚本各用各的 Key，泄露不互伤。」（UI Considerations empty）"
    - "每个 Send Key 行含 [标签/未命名][掩码][创建日期 mono][眼睛][复制][吊销红字] 六要素；吊销按钮为 Destructive 红色保留清单第 2 项（UI-SPEC 色彩契约）"
  artifacts:
    - "packages/server/src/admin.ts：参数化路由（CHANNEL_ID_RE + 正则捕获）+ POST/DELETE send-keys 两路由"
    - "packages/server/src/keys.ts：createSendKeyRecord / revokeSendKeyRecord 写路径函数（收敛 keys.ts 键空间红线）"
    - "packages/server/src/chat-room.ts：POST /cleanup-rate 内部路由（rate_sends 行即时清理）"
    - "packages/server/test/admin-send-keys.test.ts（新）"
    - "admin.js：Send Key 管理区（列表/创建表单/上限态/吊销确认框/片段卡）"
  key_links:
    - "吊销链（三存储联动）：KV sk: delete + id: 重写（keys.ts）→ DO POST /cleanup-rate（X-PH-Send-Key 头转发）→ rate_sends 行删除——漏任何一环：孤儿 sk: 键（列表不显示但能发消息）或僵尸限流行"
    - "上限检查时序：读 id: 计 sendKeys.length >= 10 判定必须在 KV 写之前（防第 11 个 Key 已落盘后才拒绝）"
    - "admin.ts 参数化路由结构：全部新分支置于 checkAdminAuth 通过之后、404 兜底之前（鉴权先于路由判定——不鉴权不暴露路径存在性，结构继承 Pitfall 8）"
---

<objective>
**As a** 管理员，**I want to** 给不同脚本创建各自带标签的 Send Key 并能逐个吊销，**so that** 某个脚本的 Key 泄露时单独吊销即可，不影响其他脚本（KEY-03）。

本 plan 交付 Send Key 全生命周期垂直切片：服务端 POST/DELETE send-keys 路由（D-35 前两条）+ label 校验（D-30）+ 上限 10（D-31）+ 吊销三存储联动清理（D-32，planner 裁定即时清理 rate_sends）+ 管理页 Send Key 管理区 UI + E2E 切片二。

Purpose: KEY-03「不同脚本各用各的 Key，泄露不互伤」是本阶段核心安全价值；D-31 上限是 KV 写额度的公网防线。
Output: send-keys CRUD 路由、cleanup-rate DO 路由、admin-send-keys.test.ts、管理页 Send Key 区。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-keys/03-CONTEXT.md
@.planning/phases/03-admin-keys/03-RESEARCH.md
@.planning/phases/03-admin-keys/03-PATTERNS.md
@.planning/phases/03-admin-keys/03-UI-SPEC.md
@.planning/phases/03-admin-keys/03-01-SUMMARY.md

@packages/server/src/admin.ts
@packages/server/src/keys.ts
@packages/server/src/chat-room.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 服务端 send-keys 路由（参数化路由 + label 校验 + 上限 10 + 吊销联动）+ DO /cleanup-rate</name>
  <files>packages/server/src/admin.ts, packages/server/src/keys.ts, packages/server/src/chat-room.ts, packages/server/test/admin-send-keys.test.ts</files>
  <read_first>packages/server/src/admin.ts（全文——精确匹配现状与 checkAdminAuth 结构；PATTERNS Example 1 参数化路由形态）, packages/server/src/keys.ts（03-01 后的 SendKeyRecord/normalize 结构 + createChannel 写路径形态）, packages/server/src/chat-room.ts（fetch 路由表 197-211 行 + rate_sends DDL 81-87 行 + checkRateLimit 的 send_key 查询形态）, packages/server/test/admin-channels.test.ts（测试骨架：adminRequest/sendRequest/expectErrorEnvelope helper）, .planning/phases/03-admin-keys/03-RESEARCH.md（§Pattern 1 路由扩展 + §Example 1 + 额度核算表）</read_first>
  <behavior>
    - POST /api/admin/channels/:channelId/send-keys body {label: "deploy-bot"} → 201 {key: /^phs_[0-9A-Za-z]{32}$/, label: "deploy-bot", createdAt: number}；列表 API 该频道 sendKeys 含此 Key
    - POST body {} 或 {label: null} → 201 label: null
    - POST body {label: "x".repeat(65)} → 400 invalid_body；{label: 42} → 400 invalid_body
    - 频道已有 10 个 Key 时 POST → 400 code=send_key_limit
    - DELETE /api/admin/channels/:channelId/send-keys/:key → 204 空体；此后该 Key POST /api/send → 401 invalid_key（本地强一致——注释注明生产 60s 双活窗口语义）
    - 吊销后同频道另一 Key POST /api/send → 200（互不影响）
    - DELETE 不存在的 Key / 属于其他频道的 Key / 不存在的 channelId → 404 not_found（统一信封同文案）
    - channelId 段非法（如 15 字符）→ 404 not_found（与不存在同响应，防探测）
    - 两路由无 Authorization 头 → 401 invalid_key
    - 吊销后 DO rate_sends 表无该 send_key 行（经 env.CHANNELS 直达断言或行为等价断言）
  </behavior>
  <action>
    admin.ts 参数化路由（D-35）：
    (a) 现有精确匹配结构扩展：保留 POST/GET /api/admin/channels 精确分支；新增正则捕获 /^\/api\/admin\/channels\/([^/]+)(?:\/(send-keys))?(?:\/(.+))?$/ 形态解析（参照 PATTERNS Example 1：先解析后按 method+sub+tail 分发）；全部新分支置于 checkAdminAuth 之后；channelId 用 CHANNEL_ID_RE = /^[0-9A-Za-z]{16}$/（与 generateChannelId 产出同口径）校验，格式错与不存在同走 errorEnvelope(404, "not_found", "The requested resource was not found.")——本任务先立参数化骨架 + send-keys 两分支，reset-channel-key/messages/delete 分支留占位 404（后续 plan 扩展，路由结构一次定型）；
    (b) POST /api/admin/channels/:channelId/send-keys：body JSON 解析失败 → 400 invalid_json；label 校验——缺省/null 视为无标签；typeof 非 string 或 string 超过 CHANNEL_NAME_MAX_LENGTH(64) UTF-16 码元 → 400 invalid_body（message 提及 label 字段与 64 上限，对齐既有 name 校验三段式）；经 keys.ts 读 id:（miss → 404）计 sendKeys.length，>= 10 → 400 code="send_key_limit" message="Send key limit reached (10)."（admin 域字符串码，不扩 shared 冻结 ErrorCode 枚举——envelope.ts errorEnvelope 接受任意 string 的既有先例）；生成 generateSendKey() + createSendKeyRecord 写路径；201 JSON {key, label, createdAt}（201 是密钥唯一完整返回点先例的延续）；
    (c) DELETE /api/admin/channels/:channelId/send-keys/:key：经 keys.ts resolveSendKey 读 sk:<key>——miss 或 info.channelId !== 路径 channelId → 404 not_found；执行 revokeSendKeyRecord：KV delete sk:<key> + id: 重写（sendKeys 数组移除该 key，migrate-on-write）；随后 Worker 转发 DO POST /cleanup-rate（new Request(INTERNAL_ORIGIN + "/cleanup-rate", {method: "POST", headers: {X-PH-Verified: "1", X-PH-Send-Key: key}}) 经 env.CHANNELS.getByName(channelId).fetch——转发模式照 index.ts 既有两处先例）；返回 204（Response(null, {status: 204})）。DO 转发失败不阻断 204 返回（rate_sends 行无害：键名永不复用 + 每日 alarm 自然清扫兜底——注释引用该语义）；
    keys.ts 写路径（键空间红线：全部收敛本文件）：
    (d) createSendKeyRecord(env, channelId, label)：读 id:（经 normalize）→ 生成 key + Date.now() → KV put sk:<key> 值 {channelId, label} → KV put id: 重写（sendKeys 追加新记录，channelKey/name/createdAt 原样保留）→ 返回新 SendKeyRecord；
    (e) revokeSendKeyRecord(env, channelId, key)：KV delete sk:<key>（幂等）→ KV put id: 重写（sendKeys 过滤移除）；
    chat-room.ts：
    (f) fetch 路由新增分支 POST /cleanup-rate（置于 X-PH-Verified 校验之后——结构继承防护，Pitfall 8）：读 X-PH-Send-Key 头（缺失 → 401 invalid_key 信封，照 publish 同款契约违例处理）；同步 exec("DELETE FROM rate_sends WHERE send_key = ?1", sendKey) 绑定参数；返回 200 JSON {}。
    测试 admin-send-keys.test.ts：照 admin-channels.test.ts 骨架复制（imports/TEST_ADMIN_KEY/adminRequest helper 泛化支持任意 path/sendRequest/expectErrorEnvelope）；behavior 全清单落测；「吊销后立即 401」用例加注释：本地 miniflare KV 无缓存层（源码实证 cacheTtl 只校验不生效），断言确定性成立；生产语义为 ≤60s 边缘缓存双活窗口（已文档化，非测试覆盖目标）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/server test -- admin-send-keys && pnpm --filter @pushhub/server test && pnpm --filter @pushhub/server run typecheck</automated>
  </verify>
  <done>admin-send-keys.test.ts 全部 behavior 用例绿；server 全量测试零回归（既有 13 个测试文件）；typecheck 零错误；send_key_limit 错误码经信封断言（body.error.code === "send_key_limit"）。</done>
  <acceptance_criteria>
    - packages/server/src/admin.ts 存在 CHANNEL_ID_RE 常量（/^[0-9A-Za-z]{16}$/）与 send-keys 两路由分支，且分支代码位于 checkAdminAuth 调用之后
    - packages/server/src/keys.ts 存在 createSendKeyRecord 与 revokeSendKeyRecord 导出函数；函数体内不出现直拼 KEY_PREFIX 常量以外的 KV 键构造（键空间收敛）
    - packages/server/src/chat-room.ts 存在 /cleanup-rate 分支且位于 X-PH-Verified 校验之后；DELETE 语句使用 ?1 绑定参数
    - admin-send-keys.test.ts 含上限用例（第 11 个 POST → 400 send_key_limit）与吊销 401 用例（含生产 60s 窗口注释）
    - pnpm --filter @pushhub/server test 退出码 0（全量零回归）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 管理页 Send Key 管理区——列表/创建表单/上限态/吊销确认框/片段卡</name>
  <files>packages/server/public/admin.js, packages/server/public/admin.html</files>
  <read_first>packages/server/public/admin.js（03-01 后的骨架——密钥行组件/错误条/片段卡渲染函数在此扩展）, packages/server/public/admin.html（详情面板结构）, .planning/phases/03-admin-keys/03-UI-SPEC.md（Interaction #6 Send Key 管理 + Copywriting 吊销确认框逐字契约 + 空态文案表）, packages/server/src/admin.ts（Task 1 后的 API 契约：201 体与 send_key_limit 信封）</read_first>
  <action>
    admin.html：详情面板新增 Send Key 区块（h2「Send Keys」上边距 24px 分隔）——列表容器 + 创建表单（label input maxlength=64 placeholder「如 deploy-bot（可选）」+「创建 Send Key」绿色主按钮 padding 8px 16px）+ 上限提示元素；原生 dialog 元素做吊销确认框（零依赖，UI-SPEC Interaction #3）。
    admin.js（D-30/D-31/D-32 + UI-SPEC 逐字契约）：
    (a) 选中频道时渲染 Send Key 列表：每行 data-testid=sendkey-row，六要素 [标签或「未命名」（无标签时 graytext）][掩码 mono 13.6px][创建日期 mono 13.6px][眼睛按钮][复制按钮][吊销红字按钮 #c0392b padding 4px 8px]；掩码/眼睛/复制复用 03-01 密钥行组件；空列表渲染空态文案「该频道没有 Send Key——创建一个给脚本使用，不同脚本各用各的 Key，泄露不互伤。」；
    (b) 创建流程：POST send-keys {label: 输入值或省略}（input 空字符串视为省略）→ 201 后刷新该频道数据 → 渲染同款片段卡仅第 1 块（该新 Key 的 curl 示例，标题按 UI-SPEC「创建 Send Key 成功后：同款卡片仅含第 1 块」）；提交期间按钮 disabled +「加载中…」；
    (c) 上限态：列表 length === 10 时创建按钮 disabled + 相邻提示「已达上限（10 个）」（文字 #9a9a9a）；< 10 时恢复可用并移除提示；
    (d) 吊销流程：点吊销 → dialog.showModal()（原生焦点陷阱 + Esc 关闭）——标题「吊销 Send Key「{label 或掩码}」？」正文「吊销后使用该密钥的脚本下次调用将收到 401（最长约 1 分钟边缘缓存窗口）。此操作不可撤销。」按钮 [取消][确认吊销]（确认红色）——文案逐字取自 UI-SPEC Destructive 确认框契约；确认 → DELETE 请求 → 204 后行消失 + 刷新频道数据；错误走既有错误条；
    (e) 400 send_key_limit 响应经错误条展示信封 message（竞态兜底——UI 上限态与 API 上限检查双保险）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/web-sdk run build && node -e "const s=require('fs').readFileSync('packages/server/public/admin.js','utf8'); for (const t of ['sendkey-row','已达上限','未命名']) { if (!s.includes(t)) { console.error('MISSING: '+t); process.exit(1); } } console.log('UI tokens OK')"</automated>
  </verify>
  <done>Send Key 区块完整渲染六要素行；空态/上限态/确认框三态文案与 UI-SPEC 逐字一致（构建 + token 检查通过）；吊销后行消失。真浏览器行为验证由 Task 3 E2E 断言。</done>
  <acceptance_criteria>
    - admin.js 存在 sendkey-row 行渲染逻辑，行内标签缺省时 textContent 为「未命名」且样式类含 graytext 语义
    - 吊销确认框文案与 UI-SPEC 逐字一致（标题含「吊销 Send Key」，正文含「最长约 1 分钟」与「此操作不可撤销」）
    - 创建表单 label input 的 maxlength 属性值为 64
    - 上限态逻辑：列表长度达 10 时创建按钮 disabled 属性在位且提示文案「已达上限（10 个）」渲染
    - 片段卡渲染复用 03-01 的 curl 块组件，sendKey 取自 201 响应 key 字段
    - pnpm --filter @pushhub/web-sdk run build 退出码 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: E2E 切片二——建带标签 Key/掩码揭示复制/吊销 401/上限 disabled</name>
  <files>packages/web-sdk/e2e/admin.spec.ts</files>
  <read_first>packages/web-sdk/e2e/admin.spec.ts（03-01 切片一——helper 与锚点复用）, packages/web-sdk/e2e/viewer.spec.ts（sendMessage helper 形态）, packages/server/public/admin.js（Task 2 产物——锚点对应实现）</read_first>
  <action>
    在 admin.spec.ts 追加 3 个 test：
    (a) 建带标签 Key：登录 → 创建频道 → 输入标签 deploy-bot 创建 Send Key → sendkey-row 出现且含标签文本与掩码（^phs_.{3}….{4}$）→ 片段卡出现且 curl 块含完整新 Key（从 API 响应对照）→ 点眼睛揭示完整 36 字符 → 复制按钮 data-copied（grantPermissions 先行）；
    (b) 吊销链路：建第二个 Key（标签 monitor-script）→ 两个 Key 均经 request.post /api/send 得 200（先证明可用）→ 对第一个 Key 走吊销确认框（断言 dialog 文本含「最长约 1 分钟」）→ 确认 → 行消失 → request.post /api/send 用被吊销 Key 断言 401 invalid_key → 第二个 Key 再发一次仍 200（泄露不互伤，KEY-03 核心）；
    (c) 上限态：循环创建至 10 个（request API 直建 8 个 + UI 建 2 个，避免 UI 循环点击过慢）→ 断言创建按钮 disabled + 上限提示可见；第 11 个 request API 直建断言 400 且 body.error.code === send_key_limit。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/web-sdk e2e -- admin.spec.ts</automated>
  </verify>
  <done>切片二 3 个 test 全绿；切片一 4 个 test 无回归；KEY-03 的「单独吊销 + 互不影响」经真浏览器 + 真实 API 双路径断言。</done>
  <acceptance_criteria>
    - admin.spec.ts 含吊销 test：断言确认框文本、行消失、被吊销 Key 401、另一 Key 200 四要素
    - 上限 test 断言 UI disabled 态与 API 400 send_key_limit 双层
    - 标签 test 断言行内 deploy-bot 文本与掩码正则
    - pnpm --filter @pushhub/web-sdk e2e -- admin.spec.ts 退出码 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 管理员浏览器 → Worker /api/admin/channels/:id/send-keys | Bearer Admin Key；新资源路由 |
| 公网脚本 → Worker /api/send（Send Key 持有者） | 吊销后凭据失效跨越此边界 |
| Worker → DO /cleanup-rate | X-PH-Verified 内部头 + X-PH-Send-Key 透传 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | DoS（资源耗尽） | POST send-keys 循环建 Key 烧 KV 写额度 | high | mitigate | D-31 上限 10 硬检查（读 id: 计数在写前）+ 400 send_key_limit；写路径额度核算：建 Key 2 KV 写/次 |
| T-03-07 | Spoofing | 参数化新路由（channelId 路径段） | high | mitigate | 全部分支置于 checkAdminAuth 之后；channelId 16 字符 base62 白名单正则；格式错与不存在同 404 同文案（防探测） |
| T-03-08 | Spoofing | DO /cleanup-rate 伪造直连 | medium | mitigate | 新分支置于 X-PH-Verified 校验之后（结构继承）；DO 仅经 binding 可达，内部头双重防线 |
| T-03-09 | Tampering | Send Key 标签（用户可控字符串）进管理页 | medium | mitigate | 标签渲染一律 textContent（admin.js 纪律）；服务端 64 码元上限 + 类型校验 |
| T-03-10 | Information Disclosure | 吊销后的 Key 重用 | low | accept | phs_ + 32 字符 base62 拒绝采样生成，键名空间永不复用（既有生成器保证） |
</threat_model>

## Artifacts this phase-plan produces

- **API 路由**：`POST /api/admin/channels/:channelId/send-keys`（201 {key, label, createdAt}；400 invalid_json/invalid_body/send_key_limit）；`DELETE /api/admin/channels/:channelId/send-keys/:key`（204；404 not_found）
- **DO 内部路由**：`POST /cleanup-rate`（X-PH-Send-Key 头 → rate_sends 行删除）
- **admin.ts 结构**：`CHANNEL_ID_RE` 参数化路由骨架（send-keys 两分支实现；reset-channel-key/messages/delete 占位 404 待后续 plan）
- **keys.ts 函数**：`createSendKeyRecord(env, channelId, label)`、`revokeSendKeyRecord(env, channelId, key)`
- **错误码（admin 域字符串，不进 shared 冻结枚举）**：`send_key_limit`
- **测试**：packages/server/test/admin-send-keys.test.ts；e2e/admin.spec.ts 切片二（3 test）
- **UI**：Send Key 管理区（sendkey-row / 上限态 / 吊销 dialog / 新 Key 片段卡）

<verification>
- Task 1 后：admin-send-keys 测试 + server 全量 + typecheck 三绿
- Task 2 后：build 绿 + UI token 检查（空态/上限/未命名三 token 在位）
- Task 3 后：admin.spec.ts 7 test（切片一 4 + 切片二 3）全绿
</verification>

<success_criteria>
1. KEY-03 端到端成立：真浏览器建带标签 Key → 掩码管理 → 单独吊销 → 401 与其余 Key 存活双断言
2. D-31 上限双层防线（API 400 + UI disabled）经测试与 E2E 双覆盖
3. 吊销三存储联动（KV sk:/id: + DO rate_sends）服务端测试全绿
4. server 全量测试零回归
</success_criteria>

<output>
Create `.planning/phases/03-admin-keys/03-02-SUMMARY.md` when done
</output>
