---
phase: 01-server-core
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - packages/server/src/keys.ts
  - packages/server/src/admin.ts
  - packages/server/src/index.ts
  - packages/server/test/admin-channels.test.ts
  - scripts/smoke.mjs
  - DEPLOY.md
autonomous: false
requirements: [KEY-01]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Admin Key（env.ADMIN_KEY Worker secret）经 crypto.subtle.timingSafeEqual 常时比较（先比长度再比较，避免长度差异抛错泄漏时序）——错误或缺失 Admin Key 的 /api/admin/* 一律 401 invalid_key 信封（D-13，KEY-01）"
    - "POST /api/admin/channels 创建频道：生成 channelId（16 字符）+ Channel Key（phc_ 前缀 + 32 字符）+ Send Key（phs_ 前缀 + 32 字符），三次 KV 写（ch:/sk:/id: 前缀），返回 201 {channelId, channelKey, sendKey, name, createdAt}（D-12）"
    - "创建返回的 Channel Key 与 Send Key 立即可用：同一测试内串联 /api/send 收 200、WS 连接收首拉帧（三级密钥端到端闭环，KEY-01）"
    - "GET /api/admin/channels 列出全部频道（KV id: 前缀 list），每项含密钥元数据与创建时间"
    - "三级密钥权限隔离成立：Send Key 只能发（不能连 WS）、Channel Key 只能连 WS（不能发）、Admin Key 只能管理（不能发不能连）——互不通用有测试断言"
    - "smoke.mjs 实现完整 D-15 ①②③（admin 建频道 → 发消息 → WS 实收 → 断线重连 since 补拉 → 打印端到端延迟），全绿输出 SMOKE OK"
  artifacts:
    - path: "packages/server/src/admin.ts"
      provides: "Admin API 最小集：POST/GET /api/admin/channels"
    - path: "packages/server/src/keys.ts"
      provides: "密钥写路径：generateKey（crypto.getRandomValues base62）+ createChannel 三键写 + listChannels"
      contains: "createChannel"
    - path: "packages/server/test/admin-channels.test.ts"
      provides: "KEY-01/D-12/D-13 集成测试（创建/列表/鉴权/三级隔离串联）"
    - path: "DEPLOY.md"
      provides: "D-15 五分钟 checklist 完整版 + 版本迭代规则 + 部署记录表"
  key_links:
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/admin.ts"
      via: "/api/admin/* 路由分发 + ADMIN_KEY 常时比较前置"
      pattern: "timingSafeEqual"
    - from: "packages/server/src/admin.ts"
      to: "packages/server/src/keys.ts"
      via: "createChannel/listChannels 调用（三前缀 KV 写唯一入口）"
      pattern: "createChannel"
  prohibitions:
    - requirement_id: "KEY-01"
      category: "privacy"
      status: "unverified"
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT 在服务端日志或错误信息中记录完整密钥（Admin Key 比较失败时的响应与日志均不得回显所提交的凭据值）"
---

<objective>
三级密钥体系闭合（KEY-01）：keys.ts 补齐写路径（crypto.getRandomValues 生成 phc_/phs_ 密钥与 channelId，ch:/sk:/id: 三键写入），admin.ts 落地 D-12 的最小管理 API（创建/列表，curl 可自助建频道），index.ts 接入 timingSafeEqual 常时比较鉴权（D-13）。最后把 smoke.mjs 固化为 D-15 完整五分钟 checklist 并跑通阶段级生产验证。

Purpose: Phase 1 收口——管理 API 是 Phase 3 管理页的直接复用面（零重复建设）；完整冒烟 checklist 是此后每次版本 +1 的固定仪式。
Output: Admin API + 密钥生成/写入 + 完整生产冒烟 + 部署文档定稿。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-server-core/01-CONTEXT.md
@.planning/phases/01-server-core/01-RESEARCH.md
@.planning/phases/01-server-core/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 密钥写路径 + Admin API 最小集 + timingSafeEqual 鉴权</name>
  <reversibility rating="costly">/api/admin/channels 的路径与响应结构是对外契约（D-12 costly——Phase 3 管理页与用户脚本将程序化依赖）</reversibility>
  <precondition>env.ADMIN_KEY secret 已写入（01-01 Task 3 已落位；执行器以 npx wrangler secret list 核对，缺失则提示用户补写后继续）</precondition>
  <files>packages/server/src/keys.ts, packages/server/src/admin.ts, packages/server/src/index.ts, packages/server/test/admin-channels.test.ts</files>
  <read_first>
  - packages/server/src/keys.ts（01-01 的读路径——写路径加在其旁，读函数签名不动）
  - packages/server/src/index.ts（路由结构——admin 分支插入位置与错误信封复用）
  - .planning/phases/01-server-core/01-RESEARCH.md（Pattern 6：KV 键表三前缀 + base62 生成 + timingSafeEqual 用法——"先比长度再比较"的抛错规避说明）
  - .planning/phases/01-server-core/01-CONTEXT.md（D-12/D-13 决策原文：最小集边界——删除/重置/吊销不预建）
  </read_first>
  <action>
  1. keys.ts 增补写路径：generateRandomString(length)（crypto.getRandomValues 派生 base62 字母表——不引外部 ID 库）；generateChannelKey() = "phc_" + 32 字符、generateSendKey() = "phs_" + 32 字符、generateChannelId() = 16 字符；createChannel(env, name)：channelId + 两密钥生成后三次 KV 写（ch:&lt;channelKey&gt; 存 {channelId, name, createdAt}；sk:&lt;sendKey&gt; 存 {channelId}；id:&lt;channelId&gt; 存 {channelKey, sendKey, name, createdAt} 反向索引），返回三件套；listChannels(env)：KV list 以 "id:" 前缀枚举并逐键 get 汇总（list 的分页游标处理到位——频道数超单页时循环拉全）。
  2. admin.ts：handleAdminApi(request, env)——POST /api/admin/channels 读 body {name}（name 缺省或超 64 字符 → 400 invalid_body 信封）调 createChannel 返回 201 与三件套 JSON；GET /api/admin/channels 调 listChannels 返回 200 {channels: [...]}；两者进入前先过 Admin Key 校验。
  3. Admin Key 校验（D-13）：从 env.ADMIN_KEY 取 secret 与请求 Authorization Bearer 值先比长度（不同直接 401，避免 timingSafeEqual 长度不等抛错），等长再 crypto.subtle.timingSafeEqual（Uint8Array 化两侧）常时比较；ADMIN_KEY 未配置（undefined）→ 500 server_error 信封（flagged assumption）；比较失败与缺头一律 401 invalid_key（与业务密钥同一错误码，不区分"接近正确"）。鉴权失败路径不把提交值写日志（Prohibition）。
  4. index.ts：/api/admin/* 前缀路由到 admin.ts；404 兜底复用既有信封。
  5. admin-channels.test.ts（env.ADMIN_KEY 经 vitest 配置注入测试值——cloudflareTest 的 miniflare.bindings 或 wrangler vars 途径，按 01-01 裁决的插件文档操作）：错误 Admin Key / 缺头 → 401 invalid_key；POST 创建 → 201 且返回体含 channelId/channelKey(phc_ 前缀)/sendKey(phs_ 前缀)；串联闭环——用返回的 Send Key 调 /api/send 得 200、用返回的 Channel Key 连 WS 收到首拉 history 帧（三级密钥全链路）；GET 列表含刚建频道；权限隔离——用 Channel Key 调 /api/send 得 401、用 Send Key 连 WS 得 401（KEY-01 隔离断言）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/server test admin-channels && pnpm --filter @pushhub/server test</automated>
  </verify>
  <acceptance_criteria>
  - keys.ts 含 createChannel 与三次 KV put（ch:/sk:/id: 三前缀各一次）
  - 密钥生成仅用 crypto.getRandomValues（无外部随机/ID 依赖 import）
  - index.ts 或 admin.ts 中 Admin Key 比较为长度前置检查 + timingSafeEqual 两段式
  - admin-channels.test.ts 含三级隔离双向断言（Channel Key 发送被拒 + Send Key 连 WS 被拒）
  - 201 响应体的 channelKey/sendKey 分别以 phc_/phs_ 开头且长度为前缀 + 32
  - 全量套件绿
  </acceptance_criteria>
  <done>KEY-01 闭合：三级密钥各司其职有测试证据；curl 自助建频道可用；Phase 3 管理页的 API 面就位</done>
</task>

<task type="auto">
  <name>Task 2: D-15 完整冒烟 checklist 固化 + 阶段级生产验证</name>
  <precondition>生产冒烟需两项外部输入，执行器不可自行获得——① deploy 输出的 workers.dev URL（执行器从部署输出捕获，可自足）；② PH_ADMIN_KEY secret 明文值（01-01 Task 3 由用户经 wrangler secret put 写入，仅用户知晓——执行器须向用户索取后在命令中注入，不得猜测或跳过）。任一项缺失即停下向用户请求，这是本计划 autonomous: false 的原因</precondition>
  <files>scripts/smoke.mjs, DEPLOY.md</files>
  <read_first>
  - scripts/smoke.mjs（01-04 版——本任务把建频道步骤从 kv seed 切换为 admin API 并收尾）
  - DEPLOY.md（01-01 建立的 checklist 与部署记录表——补全为最终版）
  - .planning/phases/01-server-core/01-CONTEXT.md（D-14/D-15 原文）
  </read_first>
  <action>
  1. smoke.mjs 定稿为 D-15 ①②③ 全自动版：① POST /api/admin/channels（Bearer ADMIN_KEY——脚本参数 --admin-key 或环境变量 PH_ADMIN_KEY 传入）建临时冒烟频道拿三件套（替代 kv seed——生产路径全真实）② /api/send + WS 实收（含延迟毫秒打印）③ 断开重连 + sync since 补拉恰补缺失 ④ 无效密钥 401 与超限 413 两反例。结束打印逐项结果与 SMOKE OK；冒烟频道名含时间戳（可重复运行不冲突）。
  2. DEPLOY.md 定稿：D-15 checklist 完整四步（④ dashboard DO duration 与请求曲线观察步骤写清入口路径与预期现象——空闲 duration 平直、部署后 WS 重连尖峰）；版本迭代规则（每次部署测试版本号 +1 于 root package.json 并登记）；部署记录表补齐 Phase 1 各次部署。附常见排障三条（compatibility_date 不识别回退一天、KV namespace id 回填、secret 重写命令）。
  3. 阶段级生产验证：版本号 +1 → deploy → PH_SMOKE_URL + PH_ADMIN_KEY 运行 smoke.mjs 全绿 → 结果（含端到端延迟数值）记入 DEPLOY.md 与 SUMMARY。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/server deploy &amp;&amp; PH_SMOKE_URL=&lt;URL&gt; PH_ADMIN_KEY=&lt;secret&gt; node scripts/smoke.mjs</automated>
  </verify>
  <acceptance_criteria>
  - smoke.mjs 走 admin API 建频道（脚本内不再有 wrangler kv key put 子进程调用）
  - 脚本输出含：延迟毫秒数值（&lt; 2000）、补拉条数恰为断线期间发送数、SMOKE OK
  - DEPLOY.md 四步 checklist 完整（含 dashboard 观察入口与预期现象描述）且部署记录表含本次版本
  - root package.json version 在本次部署前完成 +1（工作区规约）
  </acceptance_criteria>
  <done>Phase 1 生产验证闭环：真实 admin 建频道 → 发送 → 实收 → 补拉 → 反例拒绝，全部在生产环境可重复执行</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 管理脚本/curl → /api/admin/* | 高权限面：ADMIN_KEY 是 God Key——常时比较 + 单一错误码防探测 |
| KV 写路径 ← admin.ts | 三前缀键空间：写错前缀即跨级密钥泄漏（如把 Admin 级凭据写进可列举空间） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Information Disclosure | Admin Key 比较时序侧信道 | medium | mitigate | Task 1：长度前置 + crypto.subtle.timingSafeEqual 常时比较；401 单一错误码不区分接近程度 |
| T-01-04 | Information Disclosure | admin 响应/日志（密钥回显） | medium | mitigate | 201 响应仅在创建时一次性返回密钥（预期行为）；鉴权失败不回显提交值；日志无完整密钥 |
| T-01-09 | Spoofing | ADMIN_KEY secret 管理 | medium | mitigate | secret 仅经 wrangler secret put 写入（不进仓库/.dev.vars 已 gitignore）；泄露可整体轮换（Phase 3 分级重置补全） |
</threat_model>

## Flagged Assumptions（edge-probe 未分类项——未消解，按此实现；冲突时上报）

| Req | 探针 | 假设（当前处理方式） |
|-----|------|---------------------|
| KEY-01 | unclassified | env.ADMIN_KEY 未配置（secret 缺失）时 /api/admin/* 返回 500 server_error 信封（不泄漏配置细节、不暴露 401 区分信号）——REQUIREMENTS 未规定 secret 缺失行为，按最小信息量原则推导 |

## Artifacts this phase produces（本计划新增符号）

- HTTP 路由：POST /api/admin/channels（201 三件套）、GET /api/admin/channels（200 频道列表）
- 密钥格式：phc_ + 32 字符（Channel Key）、phs_ + 32 字符（Send Key）、channelId 16 字符；KV 前缀 id: 反向索引
- 函数：generateRandomString/generateChannelKey/generateSendKey/generateChannelId/createChannel/listChannels/handleAdminApi
- 文件：packages/server/src/admin.ts、packages/server/test/admin-channels.test.ts；DEPLOY.md 定稿；smoke.mjs D-15 全自动版

<verification>
1. pnpm --filter @pushhub/server test 全量绿（13 个测试文件全集：VALIDATION 的 12 + validators）
2. D-14 最终冒烟：版本 +1 → deploy → PH_ADMIN_KEY 注入 smoke.mjs 全绿（含延迟 &lt; 2s 与补拉零丢失的生产证据）
3. <human-check>阶段验收 3 终审：冒烟频道挂空闲 WS 连接 ≥5 分钟，Cloudflare dashboard 的 Durable Objects duration 指标平直不增（SRV-04/验收 3 的最终人工确认——记入 01-05-SUMMARY 供 /gsd-verify-work 引用）</human-check>
</verification>

<success_criteria>
- curl/脚本三行命令自助开通频道（POST admin → 拿密钥 → 发消息收消息）——外部系统接入 PushHub 的最小闭环成立
- 三级密钥权限隔离与常时比较有自动化证据（KEY-01 + D-13）
- Phase 1 全部验收路径可重复执行：全量测试绿 + D-15 checklist 每次部署可跑
</success_criteria>

<output>
Create `.planning/phases/01-server-core/01-05-SUMMARY.md` when done
</output>
