---
phase: 01-server-core
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - packages/server/src/index.ts
  - packages/server/src/chat-room.ts
  - packages/server/test/send-basic.test.ts
  - packages/server/test/send-validation.test.ts
  - packages/server/test/send-payload-fields.test.ts
  - packages/server/test/rate-limit.test.ts
  - scripts/smoke.mjs
autonomous: true
requirements: [SRV-01, SRV-02, KEY-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "超限载荷（text 32769 / title 257 / options 5 项或单项 65 / URL 2049）→ 413 payload_too_large 信封；结构错误（缺 text、类型错、枚举外 priority）→ 400 invalid_body；非 JSON 请求体 → 400 invalid_json——全部 D-06 信封格式（SRV-01，D-02/D-04/D-06）"
    - "options、callback_url、click_url 随 message 帧原样扇出到全部在线客户端（SRV-02），服务端不解析不修改不消费这些 URL（哑管道）"
    - "同一 Send Key 60 秒固定窗口内第 31 条 /api/send → 429 rate_limited 信封 + Retry-After 头（剩余秒数）；窗口滚动后计数重置恢复发送（KEY-05）"
    - "限流计数落 ChatRoom DO 内 rate_sends 表（单频道单 DO 单线程无竞态），每条消息限流开销为 +1 行读 +1 行写"
    - "合法 /api/send 精确返回 200 {id: wid, seq}（wid 为 m_ 前缀 16 字符，seq 频道内单调）"
  artifacts:
    - path: "packages/server/src/index.ts"
      provides: "/api/send 完整校验链：JSON parse → validateSendBody → 错误信封 → DO 转发"
    - path: "packages/server/src/chat-room.ts"
      provides: "publish 路径的固定窗口限流（rate_sends 表）与全字段落库"
      contains: "rate_sends"
    - path: "packages/server/test/send-basic.test.ts"
      provides: "SRV-01 基线集成测试（happy path + 401）"
    - path: "packages/server/test/send-validation.test.ts"
      provides: "D-02/D-04/D-06 校验反例矩阵测试"
    - path: "packages/server/test/send-payload-fields.test.ts"
      provides: "SRV-02 字段透传测试"
    - path: "packages/server/test/rate-limit.test.ts"
      provides: "KEY-05 限流行为测试（31st→429 + Retry-After）"
  key_links:
    - from: "packages/server/src/index.ts"
      to: "packages/shared/src/validators.ts"
      via: "workspace import validateSendBody——入口即拒（10ms CPU 预算内完成，不触 DO）"
      pattern: "validateSendBody"
    - from: "packages/server/src/chat-room.ts"
      to: "packages/shared/src/index.ts"
      via: "RATE_LIMIT_PER_MIN 常量与错误码字面量（限流阈值单一来源）"
      pattern: "RATE_LIMIT_PER_MIN"
  prohibitions:
    - requirement_id: "SRV-02"
      category: "values"
      status: "unverified"
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT 解析、渲染、消毒、截断或改写消息内容（含 options 数组元素与两个 URL 字段）——服务端是哑管道，存储与扇出逐字保持原文"
    - requirement_id: "SRV-02"
      category: "safety"
      status: "unverified"
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT 对发送方提供的 callback_url/click_url 发起任何服务端 fetch——Phase 1 仅校验长度后存储转发（回调是 Phase 4 职责且须带签名）"
---

<objective>
发送侧完整化：把 01-02 冻结的校验器接入 /api/send 入口（413/400/401 全错误矩阵 + D-06 信封），让 options/callback_url/click_url 全字段落库并随帧扇出（SRV-02），并在 ChatRoom DO 内落地每 Send Key 30 条/分钟固定窗口限流（KEY-05）。

Purpose: 至此 /api/send 从"能发"变成"契约完备的开放入口"——超限即拒、枚举即校、滥用即限，防开放中继（ROADMAP 明示 KEY-05 不能"以后再加"）。
Output: 发送路径的四个测试文件全绿 + 生产部署冒烟通过。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-server-core/01-CONTEXT.md
@.planning/phases/01-server-core/01-RESEARCH.md
@.planning/phases/01-server-core/01-01-SUMMARY.md
@.planning/phases/01-server-core/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: /api/send 校验链接入 + SRV-02 字段全量透传</name>
  <files>packages/server/src/index.ts, packages/server/src/chat-room.ts, packages/server/test/send-basic.test.ts, packages/server/test/send-validation.test.ts, packages/server/test/send-payload-fields.test.ts</files>
  <read_first>
  - packages/server/src/index.ts（01-01 建立的 send 路由——在其上插入校验层，KV 预检与 DO 转发结构不变）
  - packages/server/src/chat-room.ts（publish 的 INSERT 与扇出——扩展字段透传）
  - packages/shared/src/validators.ts 与 packages/shared/fixtures/message-frame.negative.json（反例矩阵与拒绝 code 的唯一依据——测试用例逐条取自这里，不自行发明）
  </read_first>
  <action>
  1. index.ts 的 POST /api/send 处理链在 KV sk: 预检之后、DO 转发之前插入两步：await request.json() 捕获解析异常→非 JSON 返回 400 invalid_json 信封；validateSendBody(body)→ok:false 时按其 status（413/400）与 code 返回 D-06 信封（信封 JSON 形态与 fixtures 中 error-envelope 例逐字节同构）；ok:true 时将 normalized 载荷作为内部 publish 请求体转发 DO（title/text/priority/options/callback_url/click_url 原样透传——服务端不解析 Markdown、不消费 URL）。
  2. chat-room.ts 的 publish：INSERT 补齐 options（string[] 序列化为 JSON 字符串存储，空/省略存 NULL）、callback_url、click_url 三列；扇出 message 帧时 options 反序列化为数组、省略字段不出现（与冻结的 MessageFrame 形态一致）；内部请求体携带 send key 原值（经 X-PH-Send-Key 内部头或内部体字段，供 Task 2 限流分键——内部头经 Worker→DO 可信通道，不外泄响应）。
  3. 三个测试文件（一场景一文件；频道名 crypto.randomUUID() 派生；经 exports.default.fetch 走真实 Worker 入口）：
     - send-basic.test.ts：合法载荷 → 200 且 {id 前缀 m_、seq 单调}；无效 Send Key → 401 invalid_key 信封（SRV-01 基线）。
     - send-validation.test.ts：逐条驱动 message-frame.negative.json 全部反例经 HTTP 入口，断言 status 与 code 与 fixtures 元数据一致；另断言非 JSON 请求体 → 400 invalid_json；断言错误响应体为严格信封结构（无多余键）。
     - send-payload-fields.test.ts：带 options 三项 + callback_url + click_url + priority:"high" 的载荷 → WS 客户端收到的 message 帧逐字段等于发送值（原样透传断言）；text 含 Markdown 语法与尖括号标签字符时帧内 text 逐字节等于原文（哑管道断言）；省略 options 时帧中无该键。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/server test send-basic send-validation send-payload-fields</automated>
  </verify>
  <acceptance_criteria>
  - index.ts 中 send 路由在校验失败路径返回的 JSON 均形如 {"error":{"code":..., "message":...}} 且 status 取 401/413/400 之一
  - chat-room.ts 的 INSERT 语句列数覆盖 options/callback_url/click_url；message 帧构造中 options 为数组或省略，永不为空数组
  - send-validation.test.ts 的用例数不少于 message-frame.negative.json 的反例数（fixtures 反例全部被驱动）
  - send-payload-fields.test.ts 含逐字节 text 相等断言（哑管道证据）
  - 全量 pnpm --filter @pushhub/server test 仍绿（ws-fanout/validators/fixtures-contract 无回归）
  </acceptance_criteria>
  <done>发送侧契约完备：正例 200 {id,seq}，全部反例按冻结错误码拒绝，SRV-02 字段端到端透传可断言</done>
</task>

<task type="auto">
  <name>Task 2: KEY-05 限流——ChatRoom DO 内固定窗口计数表</name>
  <reversibility rating="reversible">限流表结构为可调实现（CONTEXT 裁量项）；窗口大小/阈值改动不动表结构</reversibility>
  <files>packages/server/src/chat-room.ts, packages/server/test/rate-limit.test.ts, scripts/smoke.mjs</files>
  <read_first>
  - packages/server/src/chat-room.ts（publish 同步块——限流检查插入在 seq 分配之前，RESEARCH Pattern 5 逐字模式）
  - .planning/phases/01-server-core/01-RESEARCH.md（Pattern 5：rate_sends 表结构与固定窗口逻辑、KV/独立 DO 方案被排除的额度论证）
  - .planning/phases/01-server-core/01-CONTEXT.md（KEY-05 与限流裁量说明：30 条/分钟、超限 429，实现自由）
  </read_first>
  <action>
  1. chat-room.ts 构造器幂等 DDL 增加第二张表 rate_sends(send_key TEXT PRIMARY KEY, window_start INTEGER NOT NULL, count INTEGER NOT NULL)（Pattern 5 逐字）。
  2. publish 入口第一步（先于 seq 分配，同一同步块内）：按 RESEARCH Pattern 5 三分支实现——无记录或窗口已过（now - window_start &gt;= 60000）→ INSERT ... ON CONFLICT DO UPDATE 重置计数为 1；窗口内且 count &gt;= RATE_LIMIT_PER_MIN → 返回 429 JSON 信封（code rate_limited）+ Retry-After 头（60 减已过秒数，向上取整），不落库不扇出；窗口内未超 → UPDATE count+1 后继续正常发布。窗口长度 60000 与阈值均引用 shared 常量（阈值改动只改一处）。
  3. Worker 入口透传 DO 的 429 响应（含 Retry-After 头）——index.ts 对 DO 响应的透传逻辑核对一遍，头不丢失。
  4. rate-limit.test.ts：同一 Send Key 连发 30 条全 200 → 第 31 条 429 + 信封 code + Retry-After 头存在且为正整数；另一 Send Key 同频道不受首键计数影响（分键隔离）；直接操作 DO 内表将 window_start 回拨 61 秒（runInDurableObject 或重开唯一频道名绕过）后恢复 200（窗口滚动语义，避免测试等待真实 60 秒）。
  5. scripts/smoke.mjs 追加一步：发送 text 超限载荷（32769 字符）断言生产环境返回 413 + payload_too_large（D-15 冒烟随契约同步扩步）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/server test rate-limit && pnpm --filter @pushhub/server test</automated>
  </verify>
  <acceptance_criteria>
  - chat-room.ts 含 rate_sends 表 DDL 且限流分支三态齐全（重置/拒绝/自增）
  - 第 31 条请求的响应 status 为 429、体为 {"error":{"code":"rate_limited",...}}、头含 Retry-After
  - 限流拒绝路径不产生 messages 表写入（计数表不动 messages 行——测试可用 seq 不增长断言）
  - 限流检查位于 seq 分配之前（被拒消息不消耗 seq，后续消息 seq 连续无空洞）
  - 全量套件绿
  </acceptance_criteria>
  <done>KEY-05 落地：30/min 固定窗口 + 429 + Retry-After，分键隔离与窗口滚动可测</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook 发送方 → POST /api/send | 不受信输入全量校验在此落地：载荷上限、枚举、结构、限流四道闸 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege / Abuse | POST /api/send（泄露的 Send Key 轰炸频道） | high | mitigate | Task 2：每 Send Key 30/min 限流 + 429 + D-02 载荷上限 + 独立重置路径（KV sk: 键，Phase 3 全套） |
| T-01-05 | DoS | /api/send 载荷（超大 text/数组撑爆 CPU/存储） | high | mitigate | Task 1：入口即拒 413（text 32KB/title 256/options 4×64/URL 2048），远低于平台 32MiB 帧/2MB 行上限 |
| T-01-06 | Information Disclosure | 错误响应 | low | mitigate | 拒绝信封 message 复用 fixtures 冻结的通用文案（不含堆栈/内部键名/密钥回显） |
</threat_model>

## Flagged Assumptions（edge-probe 未分类项——未消解，按此实现；冲突时上报）

| Req | 探针 | 假设（当前处理方式） |
|-----|------|---------------------|
| SRV-01 | unclassified | 非 JSON 请求体 → 400 invalid_json；缺 text / 字段类型错 → 400 invalid_body——REQUIREMENTS 未规定畸形输入行为，按 D-06 通用信封规则推导（已实现为测试断言） |
| KEY-05 | unclassified | 固定 60 秒窗口边界允许瞬时 2× 突发（59s 末 30 条 + 新窗口 30 条）——研究 Open Question 3 与 CONTEXT 裁量项文档化接受；Retry-After 为窗口剩余秒数向上取整 |

## Artifacts this phase produces（本计划新增符号）

- SQL：rate_sends 表（send_key/window_start/count）
- HTTP 行为：/api/send 的 413/400/401/429 全错误矩阵 + Retry-After 头
- 内部头：X-PH-Send-Key（Worker→DO 限流分键，可信内部通道）
- 测试文件：send-basic.test.ts、send-validation.test.ts、send-payload-fields.test.ts、rate-limit.test.ts

<verification>
1. pnpm --filter @pushhub/server test 全量绿（新增 4 文件 + 既有全部）
2. D-14 部署冒烟：deploy + PH_SMOKE_URL=&lt;URL&gt; node scripts/smoke.mjs 输出 SMOKE OK（smoke 脚本此时应含一条超限反例断言 413——脚本内追加一步）；版本号 +1 记入 DEPLOY.md
3. 手动对照：冒烟脚本对生产环境连发 31 条确认 429（或降低对生产的打扰：本地测试已覆盖，生产仅验证 200/401/413 三态）
</verification>

<success_criteria>
- 发送方脚本可以程序化依赖错误契约：任何非法输入得到可预期的 status + code（发送方排障无需看日志）
- 滥用防线就位：单 Send Key 无法以超过 30/min 的速率向频道注入消息
- 服务端保持哑管道：字段透传逐字节可断言
</success_criteria>

<output>
Create `.planning/phases/01-server-core/01-03-SUMMARY.md` when done
</output>
