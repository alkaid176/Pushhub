---
phase: 01-server-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - .gitignore
  - packages/shared/package.json
  - packages/shared/src/index.ts
  - packages/server/package.json
  - packages/server/wrangler.jsonc
  - packages/server/vitest.config.ts
  - packages/server/public/index.html
  - packages/server/src/index.ts
  - packages/server/src/chat-room.ts
  - packages/server/src/keys.ts
  - packages/server/test/ws-fanout.test.ts
  - scripts/smoke.mjs
autonomous: false
requirements: [SRV-01, SRV-03, SRV-04, KEY-01]
user_setup:
  - service: cloudflare
    why: "wrangler deploy 生产冒烟（D-14）与 KV namespace / ADMIN_KEY secret 写入"
    env_vars:
      - name: ADMIN_KEY
        source: "自行生成的高熵随机串（如 openssl rand -hex 32 或密码管理器）；经 npx wrangler secret put ADMIN_KEY 写入 Worker"
    dashboard_config:
      - task: "完成 wrangler login OAuth 授权（终端运行 npx wrangler login，浏览器点击 Allow）"
        location: "终端 → npx wrangler login（浏览器 OAuth 流程）"

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "生产端到端：发送方以 Send Key 调 POST /api/send，同频道所有在线 WS 客户端收到该 message 帧——本地 E2E 测试（ws-fanout.test.ts）与 workers.dev 生产冒烟（scripts/smoke.mjs）双重验证，生产实测端到端 < 2s"
    - "无效或缺失 Send Key 的 /api/send 请求与无效 Channel Key 的 WS 握手均返回 401 与 {\"error\":{\"code\":\"invalid_key\",...}} 错误信封，且无效密钥在 Worker 层即被拒绝、不创建 DO stub"
    - "每条消息写入 DO SQLite messages 表，频道内 seq 从 1 起单调递增（显式 COALESCE(MAX(seq),0)+1 赋值，禁 AUTOINCREMENT），wid 形如 m_ + 16 字符"
    - "ChatRoom 使用 ctx.acceptWebSocket 接入 Hibernation API（代码中不存在标准 WS accept 调用），构造器重设 setWebSocketAutoResponse 且 ping/pong 字面量均为带 v:1 的 JSON；连接状态经 serializeAttachment 跨休眠存活"
    - "ChatRoom DO 类名经 wrangler.jsonc exports 字段声明（storage: sqlite），部署后 DO duration 空闲不增长（生产 dashboard 人工核对）"
    - "wrangler deploy 成功部署到 workers.dev，冒烟脚本输出 SMOKE OK"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "pnpm workspace 声明（packages/*）"
      contains: "packages"
    - path: "packages/server/wrangler.jsonc"
      provides: "Worker 配置：exports 声明 ChatRoom + kv_namespaces + assets"
      contains: "ChatRoom"
    - path: "packages/server/src/chat-room.ts"
      provides: "ChatRoom DO：Hibernation WS + SQLite messages 表 + seq 分配 + 扇出"
      exports: ["ChatRoom"]
    - path: "packages/server/src/index.ts"
      provides: "Worker 入口：/api/send 与 /api/ws/:channelKey 路由 + KV 密钥预检"
    - path: "packages/server/src/keys.ts"
      provides: "KV 三前缀（ch:/sk:）读路径封装"
    - path: "packages/server/test/ws-fanout.test.ts"
      provides: "端到端扇出集成测试（WS+DO，官方 fixture 模式）"
    - path: "scripts/smoke.mjs"
      provides: "D-15 生产冒烟脚本（send → WS 实收 + 延迟测量）"
    - path: "packages/server/vitest.config.ts"
      provides: "cloudflareTest 测试接线（wrangler.configPath 同步 compat/bindings）"
  key_links:
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/keys.ts"
      via: "KV sk:/ch: 前缀解析（cacheTtl 60）——无效密钥不创建 DO stub"
      pattern: "resolveSendKey|resolveChannelKey"
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/chat-room.ts"
      via: "CHANNELS.getByName(channelId).fetch 内部请求（X-PH-Verified: 1 可信头）"
      pattern: "getByName"
    - from: "packages/server/src/chat-room.ts"
      to: "packages/shared/src/index.ts"
      via: "workspace:* 导入 MessageFrame / PROTOCOL_VERSION 构造 v:1 扇出帧"
      pattern: "@pushhub/shared"
  prohibitions:
    - requirement_id: "PHASE-01"
      category: "values"
      status: "unverified"
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT 解析、渲染、消毒、截断或改写消息 Markdown 原文——服务端是哑管道（存储与扇出逐字保持原文，渲染责任全在客户端，10ms CPU 预算）"
    - requirement_id: "PHASE-01"
      category: "privacy"
      status: "unverified"
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT 在服务端日志或错误信息中记录完整密钥（Channel Key 路径段、Send Key Bearer 头、Admin Key）——密钥即身份（bearer 凭据），日志泄漏等同账号泄漏"
    - requirement_id: "PHASE-01"
      category: "safety"
      status: "unverified"
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT 在 Phase 1 对发送方提供的 callback_url/click_url 发起任何服务端 fetch（仅校验长度后存储转发；回调送达是 Phase 4 职责且须带签名）"
---

<objective>
Walking Skeleton（本阶段 tracer 计划）：从零搭起 pnpm monorepo，用一条真实消息打穿全栈——POST /api/send（Send Key KV 预检）→ ChatRoom Durable Object（Hibernation WS + SQLite 落库 + seq 分配 + 全连接扇出）→ WS 客户端实收，vitest 在真实 workerd 中验证，最后 wrangler deploy 到 workers.dev 完成生产冒烟。

**用户故事（源自 ROADMAP Phase 1 Goal 的直译框架）：**
**作为** webhook 发送方（自动化脚本作者），
**我想要** POST 一条通知后同频道所有在线客户端 2 秒内实时收到、断线重连后零丢失补拉，
**如此** 我的团队实时协同且不漏任何告警。（本计划交付故事的前半段：发送 → 实时扇出 → 生产部署；补拉语义由 01-04 交付）

Purpose: Phase 1 是全新代码库的第一铲土——架构（DO 类名 ChatRoom、exports 声明、KV 键表、协议 v:1）一旦部署即定型，必须用最薄的真实切片先证明全栈成立（Walking Skeleton），后续四个计划在此骨架上垂直扩展。
Output: 可部署可测试的 monorepo 骨架 + 端到端消息管道一条 + 生产冒烟脚本。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-server-core/01-RESEARCH.md
@.planning/phases/01-server-core/01-PATTERNS.md
@D:\AIworkspaces\PushHub\.claude\CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: 测试栈包合法性确认（@cloudflare/vitest-plugin，SUS 标记）</name>
  <what-built>尚无代码——本检查点是包安装前置门（Package Legitimacy Gate）。@cloudflare/vitest-plugin 1.1.0 创建于 2026-08-20（仅 6 天龄，周下载 29k），被研究标记为 SUS 并要求人工确认后才可安装。</what-built>
  <how-to-verify>
    1. 访问 https://www.npmjs.com/package/@cloudflare/vitest-plugin 确认：
       a. 仓库指向 github.com/cloudflare/workers-sdk（官方）；
       b. 1.1.0 存在、发布者可信；
       c. 无 postinstall 脚本、无异常依赖。
    2. 访问 https://developers.cloudflare.com/workers/testing/vitest-integration/ 确认官方文档当前以该包（cloudflareTest() Vite 插件）为推荐路径。
    3. 决策：批准使用 @cloudflare/vitest-plugin@1.1.0，或切换到成熟退路 @cloudflare/vitest-pool-workers@0.22.0（defineWorkersConfig 旧 API，2.5M 周下载，同仓同团队同 known-issues，切换成本一次配置改动）。
  </how-to-verify>
  <resume-signal>回复 "approve-plugin"（用 vitest-plugin 1.1.0）或 "use-fallback"（切 vitest-pool-workers 0.22.0）</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: 全栈 tracer 切片——scaffold + /api/send + ChatRoom DO + WS 扇出 + E2E 测试</name>
  <precondition>Task 1 检查点已裁决测试栈选型（approve-plugin 或 use-fallback 均可继续，后续步骤按裁决调整包名与 vitest.config 写法）</precondition>
  <files>pnpm-workspace.yaml, package.json, tsconfig.base.json, .gitignore, packages/shared/package.json, packages/shared/src/index.ts, packages/server/package.json, packages/server/wrangler.jsonc, packages/server/vitest.config.ts, packages/server/public/index.html, packages/server/src/index.ts, packages/server/src/chat-room.ts, packages/server/src/keys.ts, packages/server/test/ws-fanout.test.ts</files>
  <read_first>
  - .planning/phases/01-server-core/01-RESEARCH.md（Pattern 1-8 与 Code Examples 1-5——全部实现模式的逐字出处）
  - .planning/phases/01-server-core/01-PATTERNS.md（文件→模式映射与反模式清单）
  - .planning/phases/01-server-core/01-CONTEXT.md（D-05/D-07/D-13 决策与 Claude's Discretion 项）
  - D:\AIworkspaces\PushHub\.claude\CLAUDE.md（免费额度硬数据与 Do NOT use 清单）
  </read_first>
  <action>
  按 RESEARCH.md Pattern 逐字模式搭起 monorepo 并打穿一条消息（每层一个文件，真实错误处理，无占位逻辑）：

  1. 根 scaffold：pnpm-workspace.yaml（packages 数组含 "packages/*"）；根 package.json（private、packageManager 字段固定 "pnpm@10.33.0"、scripts：test 与 deploy 透传 server 包）；tsconfig.base.json（strict、target ESNext、moduleResolution bundler、resolveJsonModule 开启——fixtures 静态 import 前提）；.gitignore（含 node_modules、.wrangler、.dev.vars、dist）。

  2. shared 包（最小集，后续 01-02 扩展）：package.json 命名 @pushhub/shared，exports 直接指向 src/*.ts（internal-package 模式，无构建步，peerDeps 仅 typescript）；src/index.ts 导出 PROTOCOL_VERSION=1 常量与最小 MessageFrame 类型（v:1、wid、seq、title 可选、text、priority——完整字段集 01-02 冻结，此处仅放本切片用到的）。

  3. server 包配置：package.json（@pushhub/server，dependencies 含 "@pushhub/shared": "workspace:*"，devDependencies 按研究 Standard Stack 装版本锁定：wrangler@4.126.0、typescript@7.0.2、vitest@4.1.11，测试插件按 Task 1 裁决装 @cloudflare/vitest-plugin@1.1.0 或 @cloudflare/vitest-pool-workers@0.22.0；scripts：test 为 "vitest run --max-workers=1 --no-isolate"（WS+DO 不支持按文件隔离，Pitfall 1，标志必须原样保留）、cf-typegen 为 "wrangler types --include-runtime=false"、dev 为 "wrangler dev"、deploy 为 "wrangler deploy"）。

  4. wrangler.jsonc（按 RESEARCH Pattern 1 + Pattern 7 逐字适配，字段名精确）：name 为 "pushhub"；main 为 "src/index.ts"；compatibility_date 为 "2026-08-25"（A4）；durable_objects.bindings 为 [{name: "CHANNELS", class_name: "ChatRoom"}]；exports 为 {"ChatRoom": {"type": "durable-object", "storage": "sqlite"}}（禁止与 migrations 数组并存——互斥且不可回退，Pitfall 2）；kv_namespaces 为 [{binding: "KV", id: "local-placeholder"}]（真实 id 在 Task 3 部署时创建并回填）；assets 为 {directory: "./public", binding: "ASSETS", not_found_handling: "none"}（不设 run_worker_first，默认 asset-first，Pattern 7）。注意注释用 // JSONC 语法。

  5. vitest.config.ts（按 RESEARCH Code Example 4 官方逐字模式）：cloudflareTest 插件 + wrangler.configPath 指向 "./wrangler.jsonc"（自动同步 compatibility_date 与全部 bindings；若 Task 1 裁决 fallback，则改用 defineWorkersConfig 等价配置——miniflare 键对齐 wrangler 内容，compatibility_date 手动与 wrangler.jsonc 保持一致并在注释中注明同步义务）。

  6. public/index.html：最小占位页（标题 PushHub + 一行说明；文件路径不得以 api 前缀开头——asset-first 遮蔽风险，Pitfall 6）。

  7. keys.ts（KV 读路径，Pattern 6）：常量 KEY_PREFIX_CH = "ch:"、KEY_PREFIX_SEND = "sk:"；导出 resolveSendKey(env, key)（KV get，type "json"，cacheTtl 60 默认——命中返回 {channelId}，miss 返回 null）与 resolveChannelKey(env, key)（同法，命中返回 {channelId, name, createdAt}）。写路径与 id: 前缀归 01-05。

  8. chat-room.ts（ChatRoom DO，核心文件，按 Pattern 2 + 3 + 4 + Code Example 1 逐字模式）：
     - extends DurableObject（import 自 "cloudflare:workers"）；构造器内幂等 DDL 建全量冻结版 messages 表（13 列：seq INTEGER PRIMARY KEY、wid、title、text、options、callback_url、click_url、priority、answered、answered_by、answered_at、answered_content、created_at——见 RESEARCH Pattern 4 原文，不建二级索引）+ setWebSocketAutoResponse(new WebSocketRequestResponsePair(...))，ping/pong 字面量均为含 v:1 的 JSON 对象字面量（满足 D-07 全帧带版本；两串各远小于 2048 字符上限）。
     - fetch 处理两类内部请求（仅信 Worker 转发的 X-PH-Verified: 1 内部头，Pattern 8）：
       a. publish（POST）：同步块内 SELECT COALESCE(MAX(seq),0)+1 取 next → 显式 INSERT（两句 exec 之间零 await = 自动原子提交，Pitfall 9 游标同步 .one() 收完）→ 构造 v:1 message 帧（含 answered: false 与三个 answered_* null 字段，D-03 字段集一次定全）→ for 循环 this.ctx.getWebSockets() 逐个 ws.send，try/catch 收集死连接后统一 close(1011)（workers-chat-demo 模式）→ 返回 200 JSON {id: wid, seq}。
       b. WS 升级：new WebSocketPair() → this.ctx.acceptWebSocket(server)（绝不调用标准 accept 方法——烧时长额度，Anti-Pattern #1）→ server.serializeAttachment({clientId: crypto.randomUUID(), connectedAt: Date.now()}) → 返回 101 + webSocket: client。webSocketMessage 本期收到非 ping 业务帧时忽略（sync 处理归 01-04）；webSocketClose 调 ws.close(code, reason)。
     - wid 生成：手写 nanoid 形态（字母表去掉易混淆字符的 URL-safe 集合，crypto.getRandomValues 派生 16 字符，前缀 "m_"，D-05）——不引入任何外部 ID 库。
     - 内存字段一律视为可丢弃缓存（休眠唤醒即清空，可从 getWebSockets/attachment/SQLite 重建，PITFALLS 2.2）。

  9. index.ts（Worker 入口，Pattern 8 + 6）：
     - POST /api/send：读 Authorization Bearer → resolveSendKey → miss 即 401 + D-06 错误信封 {"error":{"code":"invalid_key","message":"..."}}（message 为通用文案，不含堆栈与内部键名）→ 命中则 CHANNELS.getByName(channelId).fetch 转发内部 publish 请求（原样透传请求体——本切片暂不校验载荷，D-02/D-04 校验在 01-03 接入）→ 透传 DO 响应。
     - GET /api/ws/:channelKey：从路径段取 key → resolveChannelKey → miss 即 401 信封（不创建 DO stub，防 DoS，T-01-02）→ 命中则重写内部 URL 后 CHANNELS.getByName(channelId).fetch 转发并附加 X-PH-Verified: 1 头 → 透传 101 升级响应。
     - 未知路径 404 + 同款信封；结构化日志不打印完整 URL query 与密钥（Prohibition 禁令 #2）。

  10. test/ws-fanout.test.ts（E2E，官方 fixture 模式逐字，Code Example 3）：import { env } from "cloudflare:workers"（fallback 路径等价 import）+ import { exports } from "cloudflare:workers"（Worker 入口集成——exports.default.fetch() 直调入口处理器；若 fallback 且不支持 exports，则以官方文档给出的 SELF 等价方式 fetch 入口，两路均记录在测试文件头注释）；流程：唯一频道名 idFromName（crypto.randomUUID() 派生）→ 测试内 env.KV.put 直接种入 ch:/sk: 两键指向该 channelId → exports.default.fetch 调 POST /api/send（Bearer Send Key，合法载荷）断言 200 且 {id: "m_" 前缀, seq: 1} → env.CHANNELS.get(id).fetch 带 Upgrade: websocket 头开两条客户端 WS（response.webSocket + socket.accept()）→ 再 send 一条 → 两个客户端均在消息事件中收到 v:1 message 帧且 seq=2、text 逐字一致 → 无效 Send Key 的 /api/send 断言 401 信封 code。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm install && pnpm --filter @pushhub/server exec wrangler types --include-runtime=false && pnpm --filter @pushhub/server test ws-fanout</automated>
  </verify>
  <acceptance_criteria>
  - wrangler.jsonc 同时含 exports 字段（ChatRoom + storage: sqlite）与 durable_objects.bindings（CHANNELS），且不含 migrations 数组
  - pnpm --filter @pushhub/server test ws-fanout 退出码 0（vitest run --max-workers=1 --no-isolate 下两客户端均收到 seq=2 的 v:1 帧）
  - chat-room.ts 含 acceptWebSocket( 与 serializeAttachment( 与 setWebSocketAutoResponse( 各至少一次
  - chat-room.ts 中 COALESCE(MAX(seq) 出现于 INSERT 前的同步赋值语句
  - packages/server/src 与 packages/shared/src 的 .ts 文件中 grep 不到 Node 专有模块导入与 Node 全局（服务端零 Node 依赖硬约束）
  - vitest test script 字符串含 --max-workers=1 且含 --no-isolate
  - worker-configuration.d.ts 已生成且含 KV 与 CHANNELS 的 Env 声明
  </acceptance_criteria>
  <done>E2E 测试绿：POST /api/send → 双 WS 客户端实收同一条 v:1 message 帧；无效密钥 401 信封；scaffold 全部文件就位并提交</done>
</task>

<task type="auto">
  <name>Task 3: 生产部署与首次冒烟（wrangler login 门 + KV namespace + deploy + smoke）</name>
  <precondition>执行器先跑只读检查 npx wrangler whoami：已登录且 env.ADMIN_KEY secret 路径可用（secret 在首次 deploy 后由 Task 内步骤写入，登录态是唯一前置）——未登录时停下创建 checkpoint:human-action 请用户在终端完成 npx wrangler login（浏览器 OAuth，Claude 无法代办），完成并验证 whoami 有账号后继续</precondition>
  <files>packages/server/wrangler.jsonc, scripts/smoke.mjs, DEPLOY.md</files>
  <read_first>
  - packages/server/wrangler.jsonc（回填真实 KV namespace id）
  - .planning/phases/01-server-core/01-CONTEXT.md（D-14/D-15 部署节奏与 checklist 原文）
  - .planning/phases/01-server-core/01-VALIDATION.md（Manual-Only Verifications 表——dashboard duration 观察步骤）
  </read_first>
  <action>
  1. 认证与命名空间：确认 wrangler 登录态（whoami）；运行 npx wrangler kv namespace create KV（在 packages/server 目录下）取输出中的 id，回填 wrangler.jsonc 的 kv_namespaces（替换 local-placeholder）。

  2. 首次部署：pnpm --filter @pushhub/server deploy；从输出捕获 workers.dev URL（https://pushhub.<subdomain>.workers.dev）。若 deploy 报 compatibility_date 不识别，按 A4 退一天再试并同步 wrangler.jsonc。

  3. 写入 ADMIN_KEY secret：请用户提供（或生成）高熵随机值，npx wrangler secret put ADMIN_KEY（在 server 目录，交互输入）——本计划尚无 admin 路由，secret 先落位供 01-05 使用。

  4. scripts/smoke.mjs（D-15 自动化部分 ①②③的当前切片版）：Node 22 原生 fetch + 全局 WebSocket，零依赖；参数 --url &lt;worker-url&gt;（或环境变量 PH_SMOKE_URL）；KV namespace id 由脚本自行从 packages/server/wrangler.jsonc 解析（剔除 // 注释后正则取 kv_namespaces 的 id 字段）；流程：
     a. 经 npx wrangler kv key put（child_process 调用，--namespace-id 取自上一步解析值）种入冒烟用 ch:/sk: 两键指向固定 channelId "smoketest"；
     b. POST /api/send（Bearer 冒烟 Send Key，载荷 title="smoke" text="# hello smoke"）断言 200 且响应含 id 与 seq；
     c. 打开 WS /api/ws/&lt;channelKey&gt;，再发第二条消息，断言 2 秒内收到 v:1 message 帧且 text 一致，打印端到端延迟毫秒数（验收 1 的量化证据）；
     d. 断言无效 Send Key 得 401 + code: invalid_key 信封；
     e. 全部通过输出一行 SMOKE OK，任何一步失败非零退出。
  5. DEPLOY.md（仓库根）：记录 D-15 五分钟 checklist 全文（① 建频道+发消息 ② WS 收消息 ③ 断线重连+since 补拉 ④ dashboard 看 DO duration 与请求曲线——本计划 ①用 kv seed 代替、③仅收消息部分，随 01-04/01-05 补全）、版本号规则（每次部署测试后 root package.json version 递增 +1，工作区规约）与本次部署记录（版本、URL、时间）。
  6. 版本号初始化 root package.json version 为 0.1.0 并在 DEPLOY.md 部署记录表登记首次部署。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && PH_SMOKE_URL=&lt;deploy 输出的 workers.dev URL&gt; node scripts/smoke.mjs</automated>
  </verify>
  <acceptance_criteria>
  - pnpm --filter @pushhub/server deploy 成功，wrangler.jsonc 的 kv_namespaces[0].id 为真实 namespace id（非 local-placeholder）
  - PH_SMOKE_URL=&lt;真实URL&gt; node scripts/smoke.mjs 输出含 SMOKE OK 且打印的端到端延迟数值 &lt; 2000ms（脚本自行从 wrangler.jsonc 解析 namespace id）
  - npx wrangler secret list（server 目录）列出 ADMIN_KEY
  - DEPLOY.md 存在且含 D-15 checklist 四步与部署记录表
  - 冒烟脚本对无效密钥路径断言了 401 + invalid_key（脚本内含该用例）
  </acceptance_criteria>
  <done>生产 URL 上冒烟全绿（SMOKE OK + 延迟 &lt; 2s）；ADMIN_KEY secret 落位；DEPLOY.md 就位；用户在 Cloudflare dashboard 观察：冒烟频道空闲若干分钟后 DO duration 指标无增长（验收 3 首次人工核对，记入 SUMMARY）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook 发送方 → Worker /api/send | 不受信输入：任意载荷 + Bearer Send Key（可能无效/伪造） |
| WS 客户端 → Worker /api/ws/:channelKey | 不受信输入：路径段携带 Channel Key（浏览器 WS 无法带头） |
| Worker → ChatRoom DO | 可信内部（仅经 binding 可达；X-PH-Verified: 1 内部头） |
| npm registry → devDeps | 供应链面（wrangler/typescript/vitest/vitest-plugin 安装） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm devDeps 安装（@cloudflare/vitest-plugin 6 天龄 SUS） | high | mitigate | Task 1 blocking-human 包合法性检查点（不可自动批准）+ 全部包版本锁定精确版本号 + 退路 vitest-pool-workers 0.22.0 |
| T-01-02 | DoS | GET /api/ws/:channelKey | high | mitigate | Worker 层 KV ch: 预检先于 DO stub 创建——无效密钥握手不唤醒不创建 DO（Task 2 第 9 步） |
| T-01-04 | Information Disclosure | Worker 入口日志/错误（Channel Key 在路径段） | medium | mitigate | 结构化日志不打印完整 URL query；错误信封 message 通用化（Prohibition #2，Task 2 第 9 步） |
| T-01-01 | Elevation/Abuse | POST /api/send | high | mitigate | Phase 1 由 KV sk: 预检 + 载荷上限（01-03 落地 KEY-05 限流后完整缓解） |
</threat_model>

## Flagged Assumptions（edge-probe 未分类项——未消解，按此实现；与决策/需求冲突时必须上报）

| Req | 探针 | 假设（当前处理方式） |
|-----|------|---------------------|
| SRV-03 | unclassified | 非法格式的 Channel Key（含斜杠、超长、非 base62 字符）按 KV miss 同路径处理返回 401 信封，不单独区分错误码——REQUIREMENTS 未规定畸形密钥格式的行为 |

## Artifacts this phase produces（本计划新增符号——评审源锚定用）

- 文件：pnpm-workspace.yaml、tsconfig.base.json、根 package.json、.gitignore、DEPLOY.md、scripts/smoke.mjs、packages/shared/{package.json, src/index.ts}、packages/server/{package.json, wrangler.jsonc, vitest.config.ts, public/index.html, src/index.ts, src/chat-room.ts, src/keys.ts, test/ws-fanout.test.ts}、packages/server/worker-configuration.d.ts（生成物）
- 绑定/常量：DO binding CHANNELS、KV binding KV、ASSETS binding、内部头 X-PH-Verified: 1、KV 前缀 ch: / sk:（id: 归 01-05）
- 类型/常量：MessageFrame（最小集）、PROTOCOL_VERSION=1、KEY_PREFIX_CH / KEY_PREFIX_SEND、wid 前缀 m_
- HTTP 路由：POST /api/send、GET /api/ws/:channelKey
- WS 帧字面量：v:1 ping / v:1 pong（auto-response 对）、message 帧
- SQL：messages 表（13 列冻结 DDL）
- 类：ChatRoom（extends DurableObject）——类名部署即定型，不可改名

<verification>
1. pnpm --filter @pushhub/server test 全量绿（当前含 ws-fanout + 后续计划追加的文件）
2. wrangler deploy 成功；node scripts/smoke.mjs（真实 URL）输出 SMOKE OK 且延迟 &lt; 2000ms（D-14）
3. grep 确认服务端源码零 Node 专有 API（评审硬检查，Pitfall 4——vitest 自动注入 nodejs_compat 会掩盖违规，生产部署冒烟兜底）
4. <human-check>用户在 Cloudflare dashboard → Workers → pushhub → Durable Objects 观察冒烟频道：空闲数分钟后 duration 指标不增长（验收 3 / SRV-04，wrangler dev 不驱逐 DO 故只能生产验证）</human-check>
</verification>

<success_criteria>
- 全栈切片在生产环境可运行：POST → SQLite 落库 → 双客户端 WS 实收，延迟 &lt; 2s
- DO 类名 ChatRoom、exports 声明、KV 前缀、v:1 帧结构一次性定型（后续计划零架构变更）
- 测试基建就绪：vitest workerd 真运行时 + 一场景一文件范式 + no-isolate 隔离策略落地
</success_criteria>

<output>
Create `.planning/phases/01-server-core/01-01-SUMMARY.md` when done
</output>
