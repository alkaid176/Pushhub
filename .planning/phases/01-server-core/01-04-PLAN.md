---
phase: 01-server-core
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - packages/server/src/chat-room.ts
  - packages/server/test/sync-catchup.test.ts
  - packages/server/test/seq-monotonic.test.ts
  - packages/server/test/group-semantics.test.ts
  - packages/server/test/ws-hibernation-wiring.test.ts
  - packages/server/test/retention-alarm.test.ts
  - scripts/smoke.mjs
autonomous: true
requirements: [SRV-04, SRV-05, SRV-06]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "并发与中断安全由 DO 单线程模型保证：seq 分配在无 await 的同步块内原子完成（自动事务提交语义），并发/重试 publish 不产生重复 seq；休眠唤醒（evictDurableObject 模拟）后每连接 attachment 完整恢复、ping 自动应答仍然生效（SRV-04 探针消解）"
    - "断线客户端重连后发送 sync since=last_seq 补拉到全部错过的消息：keyset 查询（seq 严格大于 since 升序 LIMIT），limit 缺省 200、上限 500，翻页以 has_more:true 续拉，零丢失零重复（SRV-05/D-11）"
    - "新连接 accept 后服务端立即推送一次 history 帧（最近 50 条，INITIAL_FETCH 常量，D-09 首屏语义）；请求的 since 早于 oldest_kept_seq 时不报错不断连，history 帧以 oldest_kept_seq 诚实标记窗口边界（D-10）"
    - "频道保留最近 500 条：alarm 执行 DELETE FROM messages WHERE seq <= (SELECT MAX(seq)-500 FROM messages)（永不删当前 max 行）并清理过期限流桶；alarm 处理器自捕获异常并重设下一天（D-08，Pitfall 5/7）"
    - "多客户端同频道消息互通；某客户端断开不影响其余客户端继续收消息；断开期间的消息经补拉完整到达（SRV-06 群聊语义）"
    - "入站 WS 业务帧经 validateInboundFrame 校验：v 不等于 1 → 回 WsErrorFrame invalid_version 并忽略该帧（不断连）；JSON 解析失败/未知 type → invalid_frame（SRV-07 假设实现）"
  artifacts:
    - path: "packages/server/src/chat-room.ts"
      provides: "webSocketMessage 的 sync/history 处理 + alarm 保留清理 + 首连推送"
      contains: "oldest_kept_seq"
    - path: "packages/server/test/sync-catchup.test.ts"
      provides: "D-09/D-10/D-11 补拉语义测试（首拉 50/翻页/窗口缺口）"
    - path: "packages/server/test/seq-monotonic.test.ts"
      provides: "seq 单调与幂等去重语义测试"
    - path: "packages/server/test/group-semantics.test.ts"
      provides: "SRV-06 多客户端互通零丢失测试"
    - path: "packages/server/test/ws-hibernation-wiring.test.ts"
      provides: "SRV-04 休眠接线测试（evict 后 attachment/auto-response 存活）"
    - path: "packages/server/test/retention-alarm.test.ts"
      provides: "D-08 alarm 清理测试（500 条窗口 + oldest_kept_seq 联动）"
  key_links:
    - from: "packages/server/src/chat-room.ts"
      to: "packages/shared/src/validators.ts"
      via: "webSocketMessage 对每条入站业务帧先过 validateInboundFrame（v:1 校验单一来源）"
      pattern: "validateInboundFrame"
    - from: "packages/server/src/chat-room.ts"
      to: "packages/shared/src/index.ts"
      via: "INITIAL_FETCH/SYNC_LIMIT_DEFAULT/SYNC_LIMIT_MAX/RETENTION_KEEP 常量驱动补拉与清理参数"
      pattern: "INITIAL_FETCH|SYNC_LIMIT_MAX"
  prohibitions:
    - requirement_id: "SRV-06"
      category: "values"
      status: "unverified"
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT 解析、渲染、消毒、截断或改写消息内容——补拉与历史帧同样逐字透传 SQLite 原文（哑管道贯穿扇出与补拉两条路径）"
---

<objective>
接收侧完整化：webSocketMessage 落地 sync 补拉（首拉 50、keyset 翻页、oldest_kept_seq 缺口诚实语义），alarm 落地每日保留清理（500 条窗口 + 限流桶清扫），并以群聊语义、seq 幂等、休眠接线三组测试钉死 SRV-04/05/06。至此断线重连补拉零丢失的完整链路成立。

Purpose: 验收标准 2（离线补拉零丢失零重复）与验收标准 3（空闲 DO duration 不增长）的全部服务端机制在本计划就位；Phase 2 SDK 的重连协议依赖这里冻结的 sync/history 语义。
Output: 接收侧五个测试文件全绿 + alarm 生产行为可观察。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-server-core/01-CONTEXT.md
@.planning/phases/01-server-core/01-RESEARCH.md
@.planning/phases/01-server-core/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: sync 补拉语义——首连推送 50 条 + keyset 翻页 + oldest_kept_seq 缺口标记</name>
  <reversibility rating="costly">sync/history 帧语义进 golden fixtures（01-02 已冻结）；本任务是其服务端实现——语义改动即协议变更（D-11 原文）</reversibility>
  <files>packages/server/src/chat-room.ts, packages/server/test/sync-catchup.test.ts</files>
  <read_first>
  - packages/server/src/chat-room.ts（webSocketMessage 当前为忽略态——本任务实现完整处理器；publish 同步块结构不动）
  - packages/shared/src/index.ts（HistoryFrame/SyncFrame 形态与 INITIAL_FETCH/SYNC_LIMIT_* 常量——响应帧必须与冻结类型逐字段一致）
  - packages/shared/fixtures/history-frame.positive.json（oldest_kept_seq 与 has_more 的语义样例——实现对照）
  - .planning/phases/01-server-core/01-RESEARCH.md（Pitfall 9 游标同步消费；Pattern 4 keyset 查询 WHERE seq &gt; ?1 ORDER BY seq ASC LIMIT ?2）
  </read_first>
  <action>
  1. chat-room.ts 的 WS 升级路径：acceptWebSocket + serializeAttachment 之后、返回 101 之前不做任何查询（升级轻量）；新连接的首屏推送放在 accept 成功后立即执行——查最近 INITIAL_FETCH(50) 条（seq 降序 LIMIT 50 后内存反转，或 max(seq)-49 keyset——二选一，读取 .toArray() 同步收完）send 一条 history 帧（has_more 按"频道内是否还有更早消息"计算，oldest_kept_seq 取 MIN(seq)，空频道 oldest_kept_seq 为 0 且 messages 为空数组）。
  2. webSocketMessage 完整实现：非 string 消息（ArrayBuffer）直接忽略；string 先交 validateInboundFrame（shared）——invalid_version → ws.send(WsErrorFrame{v:1,type:"error",code:"invalid_version",...}) 后返回（连接保持，flagged assumption 语义）；invalid_frame → 同法 code invalid_frame；ping 类型不进这里（auto-response 零唤醒层已拦截，本处理器理论上收不到，防御性忽略）；sync 类型 → limit 钳制（缺省 SYNC_LIMIT_DEFAULT、大于 SYNC_LIMIT_MAX 按 500、小于 1 按 1——flagged assumption），since 为 null 走首拉语义同第 1 步，since 为数字走 keyset：SELECT ... WHERE seq &gt; since ORDER BY seq ASC LIMIT limit+1（多取 1 条判定 has_more，返回前裁掉），全部 .toArray() 同步消费后再 send（Pitfall 9）；响应 history 帧含 oldest_kept_seq = 当前 MIN(seq)。
  3. sync-catchup.test.ts（一场景一文件，唯一频道名）：发 220 条 → 新连接收到首拉 history 帧 50 条（seq 最大 50 条区间）→ sync since=最后 seq 翻页三次（200+20）拿到其余全部且无重复 seq（Set 大小断言）→ sync limit 填 999 断言单页至多 500 条（钳制）→ since=0（早于 oldest_kept_seq 时）返回不断连、帧内 oldest_kept_seq 为实际最老 seq 且不报错（D-10 语义）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/server test sync-catchup</automated>
  </verify>
  <acceptance_criteria>
  - chat-room.ts 的 sync 查询为 keyset 形态（seq 严格大于 + 升序 + LIMIT），不存在 OFFSET 分页
  - history 帧构造含 oldest_kept_seq 与 has_more 两字段且空频道时 oldest_kept_seq 为 0
  - 所有 SQL 游标在进入下一个 await 前已 .toArray()/.one() 收完（无跨 await 持有游标）
  - 新连接收到首拉 50 条；220 条场景翻页零丢失零重复（220 条全部到达，seq 集合恰为预期区间）
  - limit 钳制三态（缺省/超上限/下限）均有断言
  </acceptance_criteria>
  <done>补拉链路语义与 golden fixtures 一致：首拉 50、keyset 翻页、缺口诚实标记，全部可测</done>
</task>

<task type="auto">
  <name>Task 2: 群聊语义 + seq 幂等 + 休眠接线三组测试（SRV-04/05/06 行为钉死）</name>
  <files>packages/server/test/group-semantics.test.ts, packages/server/test/seq-monotonic.test.ts, packages/server/test/ws-hibernation-wiring.test.ts</files>
  <read_first>
  - packages/server/src/chat-room.ts（被测行为：扇出循环、死连接清理、attachment、构造器 auto-response 重设）
  - .planning/phases/01-server-core/01-RESEARCH.md（Code Example 3 官方 WS-over-DO 测试模式；cloudflare:test 的 evictDurableObject/runInDurableObject API）
  - .planning/phases/01-server-core/01-02-SUMMARY.md（fixtures 冻结的 MessageFrame 形态——断言模板）
  </read_first>
  <action>
  三个测试文件（生产代码仅在测试暴露缺陷时才动——本任务以行为验证为主）：
  1. group-semantics.test.ts：三客户端连同一频道，逐一断言每条新消息三端同帧到达（v、wid、seq、text 相等）；断开其中一端后其余两端继续正常收消息且不抛错；重连该端后 sync since 补拉到断开期间全部消息（离线窗口内多发 5 条，补拉恰为 5 条，seq 连续）；死连接清理路径——直接 close 一端不通知，继续发消息，服务端扇出对已死连接不中断其余端收件（try/catch 分支有效）。
  2. seq-monotonic.test.ts：并发发起 20 个 /api/send（Promise.all，经真实 Worker 入口），全部 200 后断言返回的 seq 集合恰为 1..20 无重复（DO 单线程 + 同步块原子性证据）；同一文本重发不产生相同 seq（无幂等去重服务端语义，wid 每条唯一）。
  3. ws-hibernation-wiring.test.ts：客户端 A 连接后发送 v:1 ping 字面量帧，断言收到 v:1 pong（auto-response 生效）；经 cloudflare:test 的 evictDurableObject(stub,{webSockets:"hibernate"}) 驱逐 DO（模拟生产休眠/驱逐），断言连接仍存活（再次 send ping 仍得 pong——唤醒后 auto-response 恢复，Pitfall 3 回归）；驱逐后再发一条消息，客户端 A 收到该 message 帧（getWebSockets 跨驱逐仍持有休眠连接句柄的证据）；attachment 恢复——deserializeAttachment 后 clientId 与驱逐前一致（runInDurableObject 内读对照）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/server test group-semantics seq-monotonic ws-hibernation-wiring</automated>
  </verify>
  <acceptance_criteria>
  - group-semantics：三端同帧断言通过；断开重连补拉恰为离线窗口消息数（零丢失零重复）
  - seq-monotonic：20 并发 seq 集合等于 1..20（无重复无空洞；被限流拒绝的发送不计入——测试用不同 Send Key 或分批避开 30/min 窗口，测试内注明策略）
  - ws-hibernation-wiring：evictDurableObject 后 ping 仍获 pong 且新消息仍达已驱逐频道的既有连接
  - 三文件全部使用 crypto.randomUUID() 派生频道名（no-isolate 共享存储隔离）
  </acceptance_criteria>
  <done>SRV-04/05/06 的核心行为全部有自动化钉子：群聊互通、并发 seq 原子、休眠唤醒恢复</done>
</task>

<task type="auto">
  <name>Task 3: 保留清理 alarm——500 条窗口 + 限流桶清扫 + 永不删 max 行</name>
  <reversibility rating="reversible">保留数值与清理频率均为可调常量（D-08 reversible）；清理逻辑本身不随数值变化</reversibility>
  <files>packages/server/src/chat-room.ts, packages/server/test/retention-alarm.test.ts, scripts/smoke.mjs</files>
  <read_first>
  - packages/server/src/chat-room.ts（构造器与 publish——alarm 设置点必须避开构造器，Pitfall 7）
  - .planning/phases/01-server-core/01-RESEARCH.md（Pattern 4 alarm 段逐字：DELETE 语句、6 次重试后放弃故自 catch、构造器 setAlarm 干扰）
  </read_first>
  <action>
  1. chat-room.ts 增加 alarm() 处理器：try 内执行两条 SQL——DELETE FROM messages WHERE seq &lt;= (SELECT MAX(seq) - 500 FROM messages)（RETENTION_KEEP 常量，严格小于当前 max，永不删 max 行——Pitfall 5）与 DELETE FROM rate_sends WHERE window_start &lt; 当前时间减 24 小时（过期桶清扫）；catch 捕获后不重抛（alarm 自带 6 次重试即放弃——自 catch 才能保每日节奏）；finally 尾部 setAlarm(Date.now() + 24*60*60*1000) 重设下一天。
  2. 首个 alarm 的设置点：publish 成功路径内 this.ctx.storage.getAlarm() 判空后才 setAlarm(now + 24h)（构造器绝不 setAlarm——每次唤醒会覆盖未触发 alarm，Pitfall 7 原文）。
  3. retention-alarm.test.ts：唯一频道发 600 条（分批避开限流——多 Send Key 轮换或直调 DO publish 内部端点，测试内注明策略）→ 经 cloudflare:test 的 runDurableObjectAlarm(stub) 立即触发 alarm → 断言：补拉 sync since=0 恰返回最近 500 条（oldest_kept_seq = 101）；再次触发 alarm 幂等（不重复删、不删 max 行——继续发 1 条 seq 仍为 601）；alarm 执行后 getAlarm 已重设为未来 24h 内某时刻（runInDurableObject 读）。
  4. scripts/smoke.mjs 扩展为 D-15 ②③ 完整版：连 WS → 记录收到的最大 seq → 主动断开 → 经 /api/send 再发 2 条 → 重连并发送 sync since=记录值 → 断言恰好补到 2 条且 seq 连续（断线补拉零丢失的生产证据）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/server test retention-alarm && pnpm --filter @pushhub/server test</automated>
  </verify>
  <acceptance_criteria>
  - chat-room.ts 的 DELETE 条件为 seq 小于等于 max 减 500 形态（不等式方向与常量经 grep 可核）
  - 构造器内不存在任何 setAlarm 调用（grep 构造器区段为 0——alarm 设置仅在 publish 判空后与 alarm 处理器尾部）
  - alarm 处理器整体被 try/catch 包裹且尾部无条件重设下一天
  - 600 条场景清理后恰存 500 条且最老 seq 为 101；继续发送 seq 为 601（单调不回退）
  - 全量套件绿（至此 01-VALIDATION 的 12 个测试文件全部就位）
  </acceptance_criteria>
  <done>D-08 落地：保留窗口、限流桶清扫、alarm 自愈重设全部可测；SQLite 行写额度受控（删除一天一次批量）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS 客户端 → webSocketMessage | 不受信入站帧：v/type/since/limit 全量校验（validateInboundFrame）后才进 SQL |
| sync 响应 → 客户端 | 数据出口：history 帧只含本频道数据（DO 实例即频道隔离边界） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering | webSocketMessage 入站帧 | medium | mitigate | Task 1：validateInboundFrame 白名单校验（v:1、type 枚举、limit 钳制 1..500），非法帧回 error 帧并忽略——任何入站字符串不直接进 SQL（参数化绑定） |
| T-01-04 | Information Disclosure | sync/history 路径 | medium | mitigate | keyset 查询参数化；帧仅含本频道行（DO 名空间即频道）；日志不打印 channelKey 路径段 |
| T-01-08 | DoS | sync 翻页滥用（高频大翻页拖 DO） | low | accept | 单 DO 软上限 1000 req/s 远超个人/小团队消息量；limit 上限 500 已钳制单帧成本；KV 预检拦住了未鉴权流量——v1 接受，dashboard 观察后再议 |
</threat_model>

## Flagged Assumptions（edge-probe 未分类项——未消解，按此实现；冲突时上报）

| Req | 探针 | 假设（当前处理方式） |
|-----|------|---------------------|
| SRV-05 | unclassified | sync.limit 大于 500 时钳制为 500、小于 1 时钳制为 1，均不报错——D-11 只定默认与上限未定越界语义（已实现为钳制并测试） |
| SRV-06 | unclassified | 成员变更（连接/断开/驱逐）不产生任何系统广播帧；"成员变更不丢消息"实现为扇出与补拉均不依赖在线列表——REQUIREMENTS 未规定成员事件语义，按最小协议面处理 |

## Artifacts this phase produces（本计划新增符号）

- WS 行为：首连 history 推送（50 条）、sync 处理器（keyset 翻页 + limit 钳制 + oldest_kept_seq）、WsErrorFrame 回帧（invalid_version/invalid_frame）
- alarm()：每日保留清理（RETENTION_KEEP=500）+ rate_sends 过期桶清扫 + 自愈重设
- 测试文件：sync-catchup / seq-monotonic / group-semantics / ws-hibernation-wiring / retention-alarm 五个 .test.ts（至此 VALIDATION 的 12 文件全集闭合）

<verification>
1. pnpm --filter @pushhub/server test 全量绿（12 个测试文件 + validators + fixtures-contract 全集）
2. D-14 部署冒烟：deploy + PH_SMOKE_URL=&lt;URL&gt; node scripts/smoke.mjs——脚本此时应追加断线重连补拉步骤（②③ 完整）：连 WS → 记 last_seq → 主动断开 → 服务端再发 2 条 → 重连 + sync since → 断言恰补 2 条；版本号 +1 记入 DEPLOY.md
3. <human-check>验收 3 复核：部署后冒烟频道挂一条空闲 WS 连接数分钟，用户在 Cloudflare dashboard 确认 DO duration 不增长（本计划后 sync/alarm 全部上线，是验收 3 的完整复核点）</human-check>
</verification>

<success_criteria>
- 断线重连补拉零丢失零重复有自动化证据（220 条翻页 + 离线窗口场景）
- 免费额度防线闭合：休眠接线、保留清理、限流桶清扫全部生效，空闲频道不烧 GB-s
- 至此 Phase 1 的 12 个测试文件全集就位，全量绿
</success_criteria>

<output>
Create `.planning/phases/01-server-core/01-04-SUMMARY.md` when done
</output>
