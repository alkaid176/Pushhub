---
phase: 01-server-core
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/shared/src/index.ts
  - packages/shared/src/validators.ts
  - packages/shared/README.md
  - packages/shared/tsconfig.json
  - packages/shared/fixtures/message-frame.positive.json
  - packages/shared/fixtures/message-frame.negative.json
  - packages/shared/fixtures/sync-frame.positive.json
  - packages/shared/fixtures/sync-frame.negative.json
  - packages/shared/fixtures/history-frame.positive.json
  - packages/shared/fixtures/history-frame.negative.json
  - packages/shared/fixtures/error-envelope.invalid-key.json
  - packages/shared/fixtures/error-envelope.payload-too-large.json
  - packages/shared/fixtures/error-envelope.rate-limited.json
  - packages/shared/fixtures/error-envelope.invalid-body.json
  - packages/shared/fixtures/ws-error-frame.json
  - packages/server/test/validators.test.ts
  - packages/server/test/fixtures-contract.test.ts
autonomous: false
requirements: [SRV-07, SRV-02]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "shared 包导出 PROTOCOL_VERSION=1 与全部 WS 帧类型（message/sync/history/pong/error），所有帧顶层含 v 字段且值为 1（D-07）"
    - "options 为空数组时按省略处理（存储 NULL、帧中不出现该字段），单元素数组合法、null 与缺省均视为未提供——不报错（SRV-02 边界：空输入探针，D-02）"
    - "全部长度上限按 JS string.length（UTF-16 码元）判定，不按字节、码点或字素簇：text 32768 通过且 32769 拒绝（413）、title 256、options 每项 64、URL 2048（SRV-02 边界：编码探针，D-02）"
    - "validators 纯函数覆盖 D-04 枚举（low/normal/high，缺省默认 normal，枚举外值拒绝 400）与 D-02 全部上限；priority 校验为枚举匹配而非自由字符串"
    - "error-envelope fixtures 逐 code 一例（401 invalid_key / 413 payload_too_large / 429 rate_limited / 400 invalid_body），每帧类型至少 1 正 1 反（D-06）"
    - "fixtures 契约测试对每个 fixture 做严格相等断言（逐字节冻结），不使用任何子集匹配式宽松断言（Pitfall 10）"
    - "README 记录协议演进规则：只加字段不改语义、未知字段必须忽略、Rust 侧禁用 deny_unknown_fields、客户端不识别的 v 即断连（D-07）"
  artifacts:
    - path: "packages/shared/src/index.ts"
      provides: "冻结的线协议：全部帧 TS 类型 + PROTOCOL_VERSION=1 + 上限/枚举/错误码常量"
      contains: "PROTOCOL_VERSION"
    - path: "packages/shared/src/validators.ts"
      provides: "纯函数校验（长度/枚举/结构），server 与未来 SDK 共用"
      contains: "validateSendBody"
    - path: "packages/shared/fixtures/"
      provides: "golden JSON fixtures 正反例全集（四端契约基线；Phase 5/6 Rust/Kotlin 直接读仓库内这些文件）"
    - path: "packages/server/test/fixtures-contract.test.ts"
      provides: "fixtures 逐字节契约测试"
    - path: "packages/shared/README.md"
      provides: "协议演进规则与帧/错误码清单文档"
  key_links:
    - from: "packages/server/test/fixtures-contract.test.ts"
      to: "packages/shared/fixtures/*.json"
      via: "静态 import JSON（resolveJsonModule）——fixtures 是测试的编译期依赖而非运行时读取"
      pattern: "from \"@pushhub/shared/fixtures"
    - from: "packages/shared/src/validators.ts"
      to: "packages/shared/src/index.ts"
      via: "校验阈值全部引用 index.ts 导出的常量，不出现魔法数字"
      pattern: "from \"./index\"|from \"\\./index"
  prohibitions: []
---

<objective>
冻结 PushHub v1 线协议：把 CONTEXT D-01~D-07 的 schema 决策落成 @pushhub/shared 包的完整 TS 类型、纯函数校验器与 golden JSON fixtures（正反例），并以逐字节契约测试锁定。这是三端移植（Phase 2 Web SDK、Phase 5 Tauri、Phase 6 Android）的唯一契约基线——SRV-07 的全部内容。

Purpose: 协议一次定对，四端联动返工成本是单端四倍（ROADMAP 核心论断）。冻结后字段删除或语义变更即破坏性协议变更。
Output: shared 包完整版 + fixtures 全集 + 契约测试绿。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-server-core/01-CONTEXT.md
@.planning/phases/01-server-core/01-RESEARCH.md
@.planning/phases/01-server-core/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: v1 协议冻结确认（one-way 决策门的实体关闭时刻）</name>
  <decision>是否按 CONTEXT D-01~D-07 原样冻结 v1 线协议 schema（消息帧字段集、上限表、priority 枚举、wid 形态、错误信封、v:1 版本语义）并产出 golden fixtures？</decision>
  <context>
  这是协议冻结的最后一次低成本修订机会。D-01（click_url）、D-03（answered 字段集一次定全）、D-05（wid=m_+16 字符）、D-07（v:1 版本语义）在 CONTEXT 中标记为 one-way：fixtures 冻结并被 Phase 2/5/6 三端实现引用后，字段删除或语义变更需要协议版本升级 + 四端联动改造。01-01 已部署的 message 帧结构与本冻结目标一致（含 answered 三字段与 v:1）。
  </context>
  <options>
    <option id="freeze-as-spec">
      <name>按 D-01~D-07 原样冻结（推荐）</name>
      <pros>与已部署骨架零冲突；discuss 阶段已逐条裁决；竞品对照过（click_url 是 table stakes）</pros>
      <cons>冻结后变更走协议版本升级（v:2），成本高</cons>
    </option>
    <option id="amend-now">
      <name>现在修订某字段后再冻结</name>
      <pros>最后一次机会调整字段名/上限/枚举值，只影响 01-01 一端的少量代码</pros>
      <cons>需要重新走决策；推迟后续三个计划</cons>
    </option>
  </options>
  <resume-signal>回复 "freeze"（按 D-01~D-07 冻结）或指出要修订的字段</resume-signal>
</task>

<task type="auto">
  <name>Task 2: shared 完整类型 + 常量 + validators 纯函数 + 演进规则 README</name>
  <reversibility rating="one-way">D-01/D-03/D-05/D-07 汇聚点：帧 schema 进入 golden fixtures 并被四端引用后，变更即协议破坏（D-03 原文：Phase 4 只加逻辑不改 schema）</reversibility>
  <precondition>Task 1 裁决为 freeze（若裁决修订，先按修订更新本任务字段表再实现）</precondition>
  <files>packages/shared/src/index.ts, packages/shared/src/validators.ts, packages/shared/README.md, packages/shared/tsconfig.json, packages/server/test/validators.test.ts</files>
  <read_first>
  - packages/shared/src/index.ts（01-01 建立的最小集，在其上扩展——不破坏已用到的导出）
  - .planning/phases/01-server-core/01-CONTEXT.md（D-01~D-07 决策原文——字段表唯一依据）
  - .planning/phases/01-server-core/01-RESEARCH.md（Pitfall 10 与 Architecture Patterns 消息表冻结版 DDL——帧字段与列一一对应）
  </read_first>
  <action>
  1. packages/shared/src/index.ts 扩展为完整冻结版（保留 01-01 已导出的符号原名不动）：
     - 常量：PROTOCOL_VERSION=1；LIMITS 对象（TEXT_MAX=32768、TITLE_MAX=256、OPTIONS_MAX_COUNT=4、OPTIONS_ITEM_MAX=64、URL_MAX=2048）；RETENTION_KEEP=500；INITIAL_FETCH=50；SYNC_LIMIT_DEFAULT=200；SYNC_LIMIT_MAX=500；RATE_LIMIT_PER_MIN=30（可配置常量，KEY-05）；WID_PREFIX="m_"；WID_LENGTH=16。
     - 类型：Priority="low"|"normal"|"high"（D-04）；MessageFrame（v:1、type:"message"、wid、seq、title 可选、text、options 可选 string[]、callback_url 可选、click_url 可选、priority、answered: boolean、answered_by/answered_content: string|null、answered_at: number|null、created_at: number——D-03 字段集一次定全，与 messages 表 13 列一一对应）；ClientFrame（v:1 + type:"ping" 字面量约定 或 type:"sync" 含 since: number|null 与 limit?: number）；ServerFrame（MessageFrame | HistoryFrame | PongFrame | WsErrorFrame）；HistoryFrame（v:1、type:"history"、messages: MessageFrame[]、oldest_kept_seq: number、has_more: boolean——D-10/D-11）；PongFrame（v:1、type:"pong"）；WsErrorFrame（v:1、type:"error"、code、message）；ErrorEnvelope（{error:{code:ErrorCode, message:string}}）；SendBody（title?、text、priority?、options?、callback_url?、click_url?——/api/send 请求体）；SendResult（{id:string, seq:number}）。
     - ErrorCode 字符串字面量联合：invalid_key、payload_too_large、rate_limited、invalid_body、invalid_json、server_error、invalid_frame、invalid_version（HTTP 侧前六个，WS 帧侧后两个——flagged assumption 见下）。
     （KV 前缀 ch:/sk:/id: 是服务端实现细节而非线协议——留在 server 的 keys.ts，不上提 shared。）
  2. validators.ts 纯函数（不依赖 Workers 运行时，可被未来 SDK 复用）：
     - validateSendBody(body: unknown)：返回判别联合 {ok:true, normalized:NormalizedSendBody} 或 {ok:false, status:413|400, code:"payload_too_large"|"invalid_body"|"invalid_json", message:string}。规则：非对象→invalid_body；text 非 string 或缺省→invalid_body；长度全部按 string.length（UTF-16 码元）判定（D-02 宽松档）；超长→413 payload_too_large（区分 text/title/options 项/URL 四类超长，message 注明哪个字段）；priority 非三枚举之一→400 invalid_body（缺省填 "normal"）；options 非 string[]、超过 4 项、任一项超 64 →对应 400/413；options 为空数组→normalized 中归一为 undefined（省略语义）；callback_url/click_url 非 string 超 2048→413；未知字段一律忽略不报错（D-07 演进规则）。
     - validateInboundFrame(raw: string | ArrayBuffer)：JSON.parse 失败→invalid_frame；解析后 v 不等于 PROTOCOL_VERSION→invalid_version；type 非 ping/sync→invalid_frame；sync 的 since 非 number|null 或 limit 非 number→invalid_frame。返回判别联合供 01-04 使用。
     - 所有阈值引用 index.ts 常量，文件内不出现裸数字阈值。
  3. README.md：协议演进规则三条（只加字段不改语义 / 未知字段必须忽略——含 Rust serde 禁用 deny_unknown_fields 说明 / 客户端不识别的 v 即断连报错）；帧清单表（每帧类型一行：方向、字段、语义）；错误码清单（HTTP 六码 + WS 两码，含触发条件）；上限常量表；fixtures 目录说明（TS 端 workspace import、Rust/Kotlin 端仓库相对路径直接读文件）。
  4. packages/shared/tsconfig.json：extends 根 tsconfig.base.json，include src 与 fixtures（供 typecheck 脚本用）；shared package.json 加 scripts.typecheck。
  5. packages/server/test/validators.test.ts（纯函数单测，一场景一文件规范内的 validators 场景）：text 恰 32768 通过 / 32769 拒 413；title 256/257；options 恰 4 项各 64 通过、5 项拒、单项 65 拒；空数组归一为省略；priority 缺省 normal、"urgent" 拒 400；url 2048/2049；非对象/缺 text 拒 400；未知字段被忽略且不影响结果；validateInboundFrame 对 v:2 帧返回 invalid_version、非 JSON 返回 invalid_frame。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/shared typecheck && pnpm --filter @pushhub/server test validators</automated>
  </verify>
  <acceptance_criteria>
  - packages/shared/src/index.ts 导出含 PROTOCOL_VERSION、LIMITS、RATE_LIMIT_PER_MIN、ErrorCode 类型、MessageFrame/HistoryFrame/SyncFrame（ClientFrame 内）/WsErrorFrame
  - MessageFrame 含 answered/answered_by/answered_at/answered_content 四字段（D-03）与 click_url（D-01）
  - validators.test.ts 全绿且覆盖 32768/32769 与 256/257 与 4×64/5项/65 边界对
  - validators.ts 中不存在裸数字 32768/32769/256/2048/64（阈值全部经 LIMITS 常量引用）
  - README.md 含"未知字段"与"deny_unknown_fields"与断连规则表述
  - shared 包不含任何 KV 前缀定义（协议包与服务端实现细节分离；前缀唯一来源仍是 server keys.ts）
  </acceptance_criteria>
  <done>类型与校验器冻结并单测全绿；README 演进规则就位；01-01 的既有导出未被破坏（pnpm --filter @pushhub/server test ws-fanout 仍绿）</done>
</task>

<task type="auto">
  <name>Task 3: golden fixtures 正反例全集 + 逐字节契约测试</name>
  <reversibility rating="one-way">fixtures 是四端契约基线（Rust/Kotlin 将直接断言这些 JSON 字节）；冻结后任何字节变化都是协议事件</reversibility>
  <files>packages/shared/fixtures/message-frame.positive.json, packages/shared/fixtures/message-frame.negative.json, packages/shared/fixtures/sync-frame.positive.json, packages/shared/fixtures/sync-frame.negative.json, packages/shared/fixtures/history-frame.positive.json, packages/shared/fixtures/history-frame.negative.json, packages/shared/fixtures/error-envelope.invalid-key.json, packages/shared/fixtures/error-envelope.payload-too-large.json, packages/shared/fixtures/error-envelope.rate-limited.json, packages/shared/fixtures/error-envelope.invalid-body.json, packages/shared/fixtures/ws-error-frame.json, packages/server/test/fixtures-contract.test.ts</files>
  <read_first>
  - packages/shared/src/index.ts（fixtures 字段必须与冻结类型逐字段一致）
  - packages/shared/src/validators.ts（反例取值与校验器拒绝路径一一对应）
  - .planning/phases/01-server-core/01-RESEARCH.md（Pitfall 10：反例未冻结 = 三端各自发明校验行为）
  </read_first>
  <action>
  1. fixtures 组织（Claude's Discretion 已定：按帧类型/错误码分文件，文件名即语义）：
     - message-frame.positive.json：一条含全字段的消息帧（title、options 三项、callback_url、click_url、priority:"high"、answered:false、三个 answered_* null、v:1、wid "m_" 前缀 16 字符示例、seq 正整数）。
     - message-frame.negative.json：数组多例——text 超 32768 一字符、title 257、options 5 项、options 单项 65 字符、priority:"urgent"、callback_url 2049、缺 text、options 含非字符串项；每例附 {violation: 字段名与违规则} 元数据键（元数据以 _ 前缀标注，说明其不参与序列化契约、仅供消费方理解反例意图）。
     - sync-frame.positive.json：since:null 与 since:123 与 since:123+limit:500 两形态（均为合法）；sync-frame.negative.json：since 为负数、limit 为 0、limit 为 501、v 为 2、缺 type。
     - history-frame.positive.json：含 oldest_kept_seq 与 has_more:true 及两条消息的完整翻页例 + has_more:false 首拉 50 条截断例（D-10/D-11 语义样例）；history-frame.negative.json：oldest_kept_seq 缺失、messages 内帧缺 v、has_more 非布尔。
     - error-envelope 四文件（D-06 逐 code 一例）：invalid_key（HTTP 401）、payload_too_large（413，message 指明超长字段）、rate_limited（429，附 Retry-After 提示语义说明键）、invalid_body（400，枚举违例）；结构一律 {"error":{"code":"...","message":"..."}}，message 为通用文案不含内部细节。
     - ws-error-frame.json：WsErrorFrame 两例（invalid_version、invalid_frame），v:1 + type:"error"。
  2. fixtures-contract.test.ts：静态 import 全部 fixtures（workspace import 路径 + resolveJsonModule）；断言逐例严格相等——正例对 MessageFrame 等类型做字段全集断言（Object.keys 排序后 toEqual 期望数组），反例对每例跑 validateSendBody/validateInboundFrame 断言被拒且 code 匹配 violation 元数据；错误信封四例断言 JSON 结构与 code 精确值。全文件禁止子集匹配式宽松断言（golden 的意义在逐字节冻结）。
  </action>
  <verify>
    <automated>cd /d/AIworkspaces/PushHub && pnpm --filter @pushhub/server test fixtures-contract validators && pnpm --filter @pushhub/server test</automated>
  </verify>
  <acceptance_criteria>
  - packages/shared/fixtures/ 下 12 个 JSON 文件全部存在且均可被 JSON.parse
  - 每类帧至少 1 正 1 反（message/sync/history 均有成对文件）；error-envelope 逐 code 一例共四例
  - fixtures-contract.test.ts 全绿；测试文件中宽松子集匹配断言的调用次数为 0（grep 计数为 0——严格相等与全键断言 Only）
  - message-frame.negative.json 的每个反例在测试中都驱动 validators 返回对应拒绝 code（反例与校验器闭环）
  - 全量套件（含 01-01 的 ws-fanout）同时绿
  </acceptance_criteria>
  <done>fixtures 契约测试逐字节全绿；正反例与 validators 拒绝路径闭环；全量测试套件绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixtures 消费方（四端）← shared 包 | 契约面：fixtures 字节即协议事实，任何端不得自行发明校验行为 |
| /api/send 载荷 → validators | 不受信输入（Phase 1 服务端校验的唯一判定源，01-03 接入 HTTP 路径） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Information Disclosure | 错误信封 message 文案 | low | mitigate | fixtures 冻结的 message 全部为通用文案（无堆栈/内部键名/密钥回显）——D-06 信封由本计划冻结其形态 |
| T-01-05 | DoS | validators 入口 | high | mitigate（前置） | 上限常量与 413 拒绝路径本计划冻结，01-03 接入 HTTP 入口后即入口即拒 |
</threat_model>

## Flagged Assumptions（edge-probe 未分类项——未消解，按此实现；冲突时上报）

| Req | 探针 | 假设（当前处理方式） |
|-----|------|---------------------|
| SRV-07 | unclassified | 服务端收到 v 不等于 1 的入站业务帧时：回一帧 WsErrorFrame（code invalid_version）并忽略该帧，不断连——"不识别的 v 即断连"是客户端侧职责（D-07 原文），服务端侧行为属假设；01-04 实现时按此处理 |

## Artifacts this phase produces（本计划新增符号）

- 常量：PROTOCOL_VERSION、LIMITS（TEXT_MAX/TITLE_MAX/OPTIONS_MAX_COUNT/OPTIONS_ITEM_MAX/URL_MAX）、RETENTION_KEEP、INITIAL_FETCH、SYNC_LIMIT_DEFAULT、SYNC_LIMIT_MAX、RATE_LIMIT_PER_MIN、WID_PREFIX、WID_LENGTH
- 类型：Priority、MessageFrame、ClientFrame、ServerFrame、HistoryFrame、PongFrame、WsErrorFrame、ErrorEnvelope、SendBody、SendResult、ErrorCode（invalid_key/payload_too_large/rate_limited/invalid_body/invalid_json/server_error/invalid_frame/invalid_version）
- 函数：validateSendBody、validateInboundFrame
- 文件：packages/shared/src/validators.ts、packages/shared/README.md、packages/shared/tsconfig.json、fixtures/ 12 个 JSON、packages/server/test/validators.test.ts、packages/server/test/fixtures-contract.test.ts

<verification>
1. pnpm --filter @pushhub/server test 全量绿（validators + fixtures-contract + ws-fanout）
2. D-14 部署冒烟：pnpm --filter @pushhub/server deploy 后 PH_SMOKE_URL=&lt;URL&gt; node scripts/smoke.mjs 输出 SMOKE OK（纯类型变更，回归确认生产路径未受损；版本号 +1 记入 DEPLOY.md）
3. fixtures 字节冻结性：git diff 确认本计划提交后 fixtures 目录与 01-02-SUMMARY 记录的冻结版一致（后续任何字节变化必须走显式协议变更流程）
</verification>

<success_criteria>
- 四端契约基线就位：任何一端可仅凭 shared 包 + fixtures 实现协议（零口头约定）
- 正反例闭环：每个反例都驱动校验器返回冻结的错误码
- 协议冻结检查点已裁决并记录（one-way 门已过，后续计划引用不再重议）
</success_criteria>

<output>
Create `.planning/phases/01-server-core/01-02-SUMMARY.md` when done
</output>
