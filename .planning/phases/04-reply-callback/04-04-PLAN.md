---
phase: 04-reply-callback
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - packages/server/public/test.html
  - packages/server/public/test.js
  - packages/web-sdk/build.mjs
  - packages/web-sdk/test/cache-bust-sync.test.ts
  - packages/web-sdk/e2e/test-page.spec.ts
  - scripts/callback-receiver.mjs
  - scripts/smoke.mjs
  - DEPLOY.md
autonomous: false
requirements: [ADM-04, RPL-04, KEY-06, WEB-03]
estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "浏览器打开 /test.html 仅经静态资产加载（不产生 Worker 请求计费）：填 server + Channel Key 即实时收流；构造表单（title/text/priority/options 最多 4/callback_url）以 Send Key Bearer 发送 POST /api/send，消息即时出现在流中（ADM-04 边界由五区块全交互解析）"
    - "点击快捷选项后该消息的快捷按钮冻结并追加'已由X回复：内容'行（匿名显示'已回复'）；answered_by 经 textContent、answered_content 经 window.PushHub.renderMarkdown 后 innerHTML——含攻击样本的回复不执行脚本（D-53 + SC1 页面侧）"
    - "验签器（D-56）：粘贴合法三头 + body + secret → 分步显示时间窗通过 / HMAC 重算一致 / 常时比较通过；篡改 body 任一字节或 timestamp 超 5 分钟 → 对应步骤 FAIL（KEY-06 encoding 消费侧：毫秒 + hex + XOR 常时比较同口径）"
    - "callback-receiver.mjs（D-57）：收到回调先验三步——缺任一签名头即 { ok:false, reason 为 missing headers }（KEY-06 empty 边界：空 body/缺头回调被拒）；时间窗外拒收；重算 HMAC 两段式常时比较；重复 message_id 打印 DUPLICATE 但验签 ok（SC5 幂等语义演示）"
    - "失败记录查询区块（D-58）：持 Channel Key 调 GET /api/callback-failures 渲染 failed 行表格（wid/url/last_error/attempts/final_failed_at），无失败显示空态"
    - "SC5 生产端到端：'部署完成通知 → 人工在测试页点确认 → callback-receiver.mjs 打印验签 OK 并续行日志'——真实自动化语境经用户人工验收 approved"
  artifacts:
    - packages/server/public/test.html + test.js — 五区块双向测试页（vanilla 零构建）
    - packages/web-sdk/build.mjs — injectCacheBustVersion 第三注入项（test.html）
    - packages/web-sdk/e2e/test-page.spec.ts — 全交互 E2E
    - scripts/callback-receiver.mjs — Node 验签参考实现（SC5 证据 + 用户可拷贝）
    - scripts/smoke.mjs — 回复链冒烟步骤扩展
    - DEPLOY.md — 0.1.13 部署登记
  key_links:
    - test.js → window.PushHub.renderMarkdown（唯一渲染管道）+ hub.reply()（04-03 API）+ hub.on("answered")（按钮冻结回写）
    - test.js 发送区块 → POST /api/send（Bearer Send Key）；失败查询区块 → GET /api/callback-failures（Bearer Channel Key）
    - E2E: 测试页操作 → 本地 callback-receiver.mjs 收 POST → 验签断言（wrangler dev 下 callback_url 指本地非标准端口）
  prohibitions:
    - "测试页不得内嵌第二套 Markdown 渲染管道——text 与 answered_content 一律经 window.PushHub.renderMarkdown（双管道漂移 = 消毒防线失守）"
    - "浏览器验签器不得调用 crypto.subtle.timingSafeEqual——它是 Workers 专有非标准扩展，Chrome/Firefox/Safari 抛 TypeError（Pitfall 3）；用手写 XOR 累加常时比较"
    - "Send Key 不得写入 localStorage——只存 server/channel/name 三项，Send Key 会话内存态且页面注明取舍（Pitfall 7）"
    - "viewer 保持轻量不动（D-22 只收不发的定位延续）——测试页是新独立资产，不改 viewer.js/index.html 行为"

user_story: "**As a** PushHub 接入方（发送方脚本作者或三端客户端开发者），**I want to** 一个可视化页面构造消息、看实时流、点回复、验回调签名、查失败记录，**so that** 不写代码就能完成接入联调与协议排障。"
---

<objective>
交付测试页（ADM-04/SC4）与 Node 回调接收器（D-57/SC5），扩展生产冒烟，部署 0.1.13 并完成 SC5 真实自动化场景的人工验收——Phase 4 五条成功标准的页面侧与生产侧全部闭环。

Purpose: 测试页是三端联调与排障工具的定位落成；callback-receiver 是发送方可拷贝的验签参考实现与 SC5 验收证据。
Output: test.html/test.js 静态资产 + E2E 全绿 + callback-receiver.mjs + smoke 扩展 + 生产 0.1.13 + SC5 用户验收。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reply-callback/04-CONTEXT.md
@.planning/phases/04-reply-callback/04-RESEARCH.md
@.planning/phases/04-reply-callback/04-PATTERNS.md
@.planning/phases/04-reply-callback/04-02-SUMMARY.md
@.planning/phases/04-reply-callback/04-03-SUMMARY.md

（02/03 SUMMARY 提供 /api/callback-failures 响应形状与 SDK reply API 签名）
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test.html + test.js 五区块测试页 + build.mjs 注入扩展</name>
  <files>packages/server/public/test.html, packages/server/public/test.js, packages/web-sdk/build.mjs, packages/web-sdk/test/cache-bust-sync.test.ts</files>
  <read_first>
- packages/server/public/index.html — CSP meta 纵深（index.html:8-11，script-src 'self' 禁 inline）、表单 grid 样式（index.html:26-47）、pushhub.js?v= 注入锚点与勿手改注释（index.html:129）
- packages/server/public/viewer.js — IIFE + use strict 骨架（viewer.js:22-23）、元素句柄集中声明（:27-35）、setStatus/showError（:50-61）、appendMessage 渲染纪律（:91-109——title textContent / text 经 renderMarkdown）、localStorage try-catch 免填 + URL 参数预填、window.__pushhub 调试句柄
- packages/server/public/admin.js — 掩码显示与纯文本纪律注释（admin.js:28-32）
- packages/web-sdk/build.mjs — injectCacheBustVersion 调用清单（build.mjs:71-72）与 refRe 恰命中一次硬断言（build.mjs:60-66）
- packages/web-sdk/test/cache-bust-sync.test.ts — 注入一致性双保险测试模式
  </read_first>
  <behavior>
- 构建断言: pnpm --filter @pushhub/web-sdk run build 后 test.html 中 pushhub.js 引用恰含一次 ?v=<根版本号>；cache-bust-sync.test.ts 对 test.html 的断言绿
- 页面行为（Task 2 E2E 逐条驱动）: 连接→发送→收流→回复→冻结→追加回复行→失败查询空态
  </behavior>
  <action>
test.html（照 index.html 模式，per D-55）：CSP meta 整体复制（script-src 'self' 禁 inline——test.js 必须独立文件；connect-src 已放行外呼 fetch 验签/查询无碍）；五区块布局——连接配置区（server/Channel Key/Send Key/展示名）、消息构造表单区（title/text/priority/options 最多 4 行/callback_url 逐字段输入）、实时消息流区、验签器区（secret/三个头/rawBody 粘贴 + 分步验证结果）、失败记录查询区；底部 script 引用 /pushhub.js 与 /test.js（pushhub.js 行带 ?v= 注入锚点，格式照 index.html）。
test.js（照 viewer.js 骨架逐模式复制，per D-55/D-56/D-58）：IIFE + use strict、getElementById 句柄集中声明、setStatus/showError、localStorage try-catch（存 server/channel/name 三项——Send Key 只存会话内存变量并在 UI 注明取舍理由，Pitfall 7）、URL 参数预填、window.__pushhub 调试句柄。
消息流渲染（唯一管道纪律）：每消息 wid→DOM 节点索引 Map；text 一律 window.PushHub.renderMarkdown 后 innerHTML；title/wid/answered_by 一律 textContent。发送区块：POST /api/send（同源相对路径，Authorization Bearer Send Key，body 含 title/text/priority?/options?/callback_url?）。回复交互：快捷选项渲染为按钮（options 存在时），点击调 hub.reply(wid, { selected_option }, by)；自定义输入框提交调 hub.reply(wid, { text }, by)；by 取展示名输入框值。hub.on("answered") 回调：按 wid 定位节点——冻结该消息全部快捷按钮（disabled）并追加回复行（answered_by 非空显示'已由' + 名字 + '回复：'，空显示'已回复：'；answered_content 经 renderMarkdown）。错误帧 already_replied 到达时 error 事件提示'已被他人回复'（D-44 败者 UI）。
验签器（D-56）：输入 secret/PushHub-Message-Id/PushHub-Timestamp/PushHub-Signature/rawBody，按钮触发三步分步显示——第一步时间窗 |Date.now() - Number(ts)| <= 300000；第二步 crypto.subtle.importKey HMAC SHA-256 重算 HMAC hex（signed payload 为 ts + "." + rawBody，毫秒口径与 04-02 一致）；第三步常时比较用手写 XOR 累加函数 timingSafeEqualHex（对两 hex 字符串的 Uint8Array，diff = a.length ^ b.length 起始逐字节 OR，结果为零即相等——长度不等直接不等）。每步独立显示 PASS/FAIL 与原因。失败记录查询区块：按钮以 Bearer Channel Key 调 GET /api/callback-failures，渲染 failed 行表格（wid/url/last_error/attempts/final_failed_at），空数组显示空态文案。
build.mjs：injectCacheBustVersion 调用清单追加第三行 test.html（refRe 恰命中一次断言自动护航）；cache-bust-sync.test.ts 同步扩展 test.html 断言。viewer.js/index.html 不动（prohibition）。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/web-sdk run build && pnpm --filter @pushhub/web-sdk exec vitest run test/cache-bust-sync.test.ts</automated>
  </verify>
  <acceptance_criteria>
- packages/server/public/ 下存在 test.html 与 test.js；test.html CSP 含 script-src 'self'
- 构建后 test.html 恰含一处 pushhub.js?v=<版本> 引用；cache-bust-sync.test.ts 全绿（含 test.html 断言）
- test.js 中消息 text 与 answered_content 的渲染均经 window.PushHub.renderMarkdown（源断言：两处调用点存在）；title/wid/answered_by 无 innerHTML 赋值
- localStorage 仅写 server/channel/name 三键（源断言：无 Send Key 键写入）
- viewer.js 与 index.html 零字节变更（git diff 为空）
  </acceptance_criteria>
  <done>测试页五区块就位、构建注入联动、渲染唯一管道与密钥落盘纪律成立——静态资产可被浏览器直接使用。</done>
  <reversibility rating="reversible">纯新增静态资产 + 构建脚本一行扩展，回滚即删。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: callback-receiver.mjs + 测试页 E2E + smoke 回复链扩展</name>
  <files>scripts/callback-receiver.mjs, packages/web-sdk/e2e/test-page.spec.ts, scripts/smoke.mjs</files>
  <precondition>Task 1 已合入（test.html 可访问）；wrangler dev 本地环境可 fetch localhost（04-02 已在集成层同模式验证）。</precondition>
  <read_first>
- scripts/smoke.mjs — 零依赖 Node 22 脚本骨架、结构化 console.log OK/FAIL、send() 助手（smoke.mjs:1-57）
- packages/web-sdk/e2e/viewer.spec.ts 与 admin.spec.ts — 同 testDir E2E 模式（playwright.config.ts 的 wrangler dev 127.0.0.1:4911 webServer 链式 build 前置——不改配置即免 Pitfall 9）
- .planning/phases/04-reply-callback/04-RESEARCH.md 的 callback-receiver 验签核心示例（Code Examples 节——verify() 三步完整实现）
  </read_first>
  <behavior>
- callback-receiver.mjs 单跑行为: node scripts/callback-receiver.mjs --port 4933 --secret phsig_xxx 启动监听；POST 缺 PushHub-Signature 头 → 响应/日志 { ok:false, reason: "missing headers" }；伪造签名 → "signature mismatch"；timestamp 偏移超 300000 → "timestamp outside tolerance"；合法回调 → ok:true 且打印 message_id 与验签耗时；同 message_id 二次合法 POST → 打印 DUPLICATE 标记且 ok 仍 true
- E2E（test-page.spec.ts）:
  - 全流程: 打开 /test.html → 填连接（Channel Key + 展示名）→ 构造消息（options 两个 + callback_url 指向 beforeAll 拉起的本地 receiver 端口）→ 发送 → 流中出现消息与两个快捷按钮 → 点其一 → 按钮冻结 + 追加含选项文本的回复行 → receiver 日志/落盘文件断言：收到恰一次 POST、Node 侧验签 ok:true、PushHub-Message-Id 等于消息 wid
  - 消毒断言: 构造含攻击样本的 text 回复（含 script 标签字符串）→ 页面 DOM 无新 script 节点执行痕迹（渲染经消毒管道）
  - 失败查询: 点击查询按钮 → 空态显示（新频道无 failed 行）
  - 验签器交互: 从 receiver 日志取真实三头与 body 粘贴 → 三步全 PASS；篡改 body 一字符 → 第二/三步 FAIL
  </behavior>
  <action>
callback-receiver.mjs（per D-57，照 smoke.mjs 骨架——零依赖 Node 22、#!/usr/bin/env node、结构化日志、非零退出码用于脚本化断言）: node:http createServer 监听 CLI 可配端口（默认非标准端口 4933，CLAUDE.md 端口规约）；--port/--secret 参数与 PH_SIGNING_SECRET 环境变量两取一；POST 处理——收集 rawBody 字符串后三步验证（照 RESEARCH Code Examples verify() 落地）: 任一签名头缺失即 { ok:false, reason: "missing headers" }；Math.abs(Date.now() - Number(ts)) > 300000 即 timestamp outside tolerance；node:crypto createHmac("sha256", secret).update(ts + "." + rawBody).digest("hex") 重算后与 PushHub-Signature 两段式比较（Buffer 长度不等先拒，等长才 crypto.timingSafeEqual）。按 message_id 记忆化：重复 id 打印 DUPLICATE 标记（验签仍执行、ok 仍 true——SC5 幂等语义演示：去重责任在接收方）。每个请求打印结构化 OK/FAIL 行（含 message_id/reason）。启动打印监听地址与验签参数。
test-page.spec.ts（沿既有 testDir 与 playwright.config.ts 不改配置）: beforeAll 以子进程拉起 callback-receiver.mjs（非标准端口、--secret 用测试内创建频道所得 signingSecret），afterAll 杀进程；按 behavior 四段组织用例。测试频道经 Admin API 创建（admin.spec.ts 模式）取得 Channel Key/Send Key/signingSecret。
smoke.mjs 扩展: send() 助手载荷增 options 与 callback_url 字段（可选）；新增回复链步骤——WS 连接后对接收到的最新消息发送 reply 帧（恰一载荷），断言收到 answered 帧（wid 匹配、answered_by/answered_content 断言）；回调断言不在生产冒烟做（生产 DO 无法回连本机 localhost——SC5 回调验证由 Task 3 人工验收与 E2E 本地链路覆盖），步骤注释注明此取舍。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/web-sdk run e2e --grep test-page</automated>
  </verify>
  <acceptance_criteria>
- scripts/callback-receiver.mjs 存在且 node --check 通过；缺头/超窗/伪造签名/合法/重复五路径行为与上述一致（E2E receiver 断言或本地 curl 抽查证据记录进 SUMMARY）
- test-page.spec.ts 全流程用例绿：发送→流→点击回复→按钮冻结→receiver 恰一次 POST 且验签 ok:true
- 消毒断言绿（攻击样本不执行）；验签器 PASS/篡改 FAIL 两分支绿；失败查询空态绿
- smoke.mjs 含回复链步骤（answered 帧断言）且既有步骤零回归
  </acceptance_criteria>
  <done>测试页全交互经真浏览器 E2E 证明；callback-receiver 成为可拷贝的验签参考实现与 SC5 证据实体；冒烟脚本覆盖回复链。</done>
  <reversibility rating="reversible">新增脚本与测试，回滚即删。</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 生产部署 0.1.13 + 冒烟 + SC5 人工验收</name>
  <files>DEPLOY.md, scripts/smoke.mjs</files>
  <precondition>04-01/02/03 全部合入且 pnpm test 全量绿 + e2e 全绿；用户在场（本任务含两个人工验收点）。</precondition>
  <read_first>
- DEPLOY.md — 版本 +1 规约（部署前补丁位递增）、D-15 生产冒烟 checklist、既有部署记录格式
- scripts/smoke.mjs — Task 2 扩展后的回复链步骤
- .planning/STATE.md — 生产访问走 https://pushhub.dyun.org（workers.dev 本机被 SNI/DNS 污染阻断）
  </read_first>
  <what-built>
回复链与回调送达全栈已自动化验证完毕（server 集成 + SDK 单测 + 测试页 E2E 全绿）：Task 3 由 Claude 执行部署与冒烟后，请用户完成 SC5 真实场景验收——生产测试页发通知、人工点确认、callback-receiver 验签续行。
  </what-built>
  <action>
按 DEPLOY.md 规约部署: 根版本号 0.1.12 → 0.1.13（部署前 +1）；pnpm run deploy（链式先 build 后 deploy——test.html 随资产上线且 ?v= 注入生效）。生产冒烟: PH_SMOKE_URL=https://pushhub.dyun.org PH_ADMIN_KEY=... node scripts/smoke.mjs（含回复链新步——answered 帧断言）。资产对照: /test.html 响应无 x-ph-worker 标记头（静态资产直接命中零计费，照 SC4 标记头对照模式）+ 引用的 pushhub.js?v= 与构建产物一致。
SC5 人工验收（checkpoint——用户操作）: (1) 接收器跑在公网可达处——用户 Linux 服务器运行 node scripts/callback-receiver.mjs（或以 webhook.site 作观察窗对照本地验签器粘贴验证，D-56 组合）；(2) 浏览器开 https://pushhub.dyun.org/test.html 发送'部署完成通知'（options 含'确认上线'、callback_url 指接收器）；(3) 人工点击'确认上线'；(4) 接收器打印验签 OK 与续行日志——'通知→确认→续行'闭环成立。辅助核对: Cloudflare dashboard 频道计数与测试产生的频道变化对照（照 03-05 生产验收三件套模式）。用户 approved 后在 DEPLOY.md 登记 0.1.13 行（版本、变更摘要、冒烟结果、验收结论）。
  </action>
  <how-to-verify>
1. 确认 Claude 已报部署成功且 smoke 输出 SMOKE OK（含回复链步骤）。
2. 公网可达机器上运行 node scripts/callback-receiver.mjs --port <非标准端口> --secret <频道 signingSecret>（secret 从创建频道响应或 Admin API GET reveal 获取）。
3. 浏览器打开 https://pushhub.dyun.org/test.html，填 server=https://pushhub.dyun.org、Channel Key、Send Key，构造消息：title=部署完成通知、options 含"确认上线"、callback_url=http://<接收器公网地址>:<端口>，发送。
4. 消息出现在流中后点击"确认上线"——按钮冻结、出现"已由X回复"行。
5. 回到接收器终端：应打印验签 OK（message_id 匹配、HMAC 一致、时间窗内）与续行日志。
6. （可选）Cloudflare dashboard 核对频道计数与 Workers 请求量变化。
  </how-to-verify>
  <resume-signal>用户回复"approved"（SC5 验收通过）即完成 DEPLOY.md 登记收尾；回复问题则回到对应 Task 修复后重新部署</resume-signal>
  <verify>
    <automated>PH_SMOKE_URL=https://pushhub.dyun.org node scripts/smoke.mjs（输出 SMOKE OK 含回复链步骤）</automated>
    <human-check>SC5 端到端走查：测试页发通知 → 人工确认 → callback-receiver 验签 OK 续行；dashboard 计数核对；测试页资产零计费对照。</human-check>
  </verify>
  <acceptance_criteria>
- 生产部署 0.1.13 成功；smoke 输出 SMOKE OK 且回复链步骤通过（answered 帧断言 OK）
- /test.html 生产可访问且无 x-ph-worker 头（资产直接命中）
- SC5 四步经用户操作完成并 approved；DEPLOY.md 含 0.1.13 登记行
  </acceptance_criteria>
  <done>Phase 4 五条成功标准的生产侧证据闭环：SC1-4 经冒烟与页面走查、SC5 经真实自动化场景人工验收，0.1.13 上线登记。</done>
  <reversibility rating="costly">生产部署不可逆（回滚需再部署）；DEPLOY.md 登记为项目账本。</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器测试页 ↔ 生产 Worker | Send Key/Channel Key 经页面持有与发送；回调三头/body 是粘贴输入 |
| callback-receiver ↔ 公网 | 监听端口收回调 POST——签名验证是唯一信任锚 |
| 任意回复者 → 测试页 DOM | answered_by/answered_content 是任意外部输入（XSS 载体） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Tampering(XSS) | 测试页渲染 answered_by/answered_content | high | mitigate | 唯一管道纪律：answered_content 经 window.PushHub.renderMarkdown（marked+DOMPurify）、answered_by/title/wid 一律 textContent（D-53）；E2E 攻击样本断言（prohibition 双保险：源断言禁第二管道） |
| T-04-16 | Information Disclosure | Send Key 滞留浏览器存储 | medium | mitigate | localStorage 只存 server/channel/name 三项、Send Key 会话内存态 + 页面明示取舍（Pitfall 7）；源断言无 Send Key 键写入 |
| T-04-17 | Spoofing | 验签器/receiver 被诱导信任伪回调 | medium | mitigate | 验签三步缺一不可（missing headers 即拒——KEY-06 empty 边界）；浏览器手写 XOR 常时比较（禁 Workers 专有 API，Pitfall 3）；Node 两段式 timingSafeEqual |
| T-04-18 | Information Disclosure | receiver 日志泄露 secret | low | mitigate | secret 仅经 CLI 参数/环境变量输入、日志不打印 secret 本体（只打印验签参数摘要） |
</threat_model>

## Artifacts this phase produces（本计划新增）

- 静态资产: packages/server/public/test.html、packages/server/public/test.js（五区块）
- build.mjs: injectCacheBustVersion 第三注入项（test.html 进 ?v= 联动清单）
- E2E: packages/web-sdk/e2e/test-page.spec.ts
- 脚本: scripts/callback-receiver.mjs（verify 三步 + DUPLICATE 幂等标记 + --port/--secret/PH_SIGNING_SECRET）；scripts/smoke.mjs 回复链步骤
- 部署: DEPLOY.md 0.1.13 登记行
- 浏览器函数: test.js 内 timingSafeEqualHex（XOR 常时比较）

<verification>
- E2E: test-page.spec.ts 全用例（真浏览器全交互 + receiver 验签断言 + 消毒断言 + 验签器双分支）
- 构建: build 后 cache-bust-sync.test.ts 全绿（三页注入一致）
- 冒烟: 生产 SMOKE OK 含回复链步骤
- 人工: SC5 四步走查 approved + dashboard 对照
</verification>

<success_criteria>
- 测试页成为三端联调工具：构造/发送/收流/回复/冻结/验签/失败查询全交互真浏览器可操作（SC4/ADM-04）
- callback-receiver.mjs 可拷贝即用，五路径验签行为正确（SC5 证据实体）
- 生产 0.1.13 上线：冒烟 OK、资产零计费对照、SC5 人工验收 approved、DEPLOY.md 登记
</success_criteria>

<output>
Create `.planning/phases/04-reply-callback/04-04-SUMMARY.md` when done
</output>
