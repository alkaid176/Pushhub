---
phase: 04-reply-callback
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - packages/web-sdk/src/frames.ts
  - packages/web-sdk/src/connection-machine.ts
  - packages/web-sdk/src/pushhub.ts
  - packages/web-sdk/README.md
  - packages/web-sdk/test/frames.test.ts
  - packages/web-sdk/test/machine-events.test.ts
  - packages/web-sdk/test/adapter-lifecycle.test.ts
autonomous: true
requirements: [WEB-03, RPL-02]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SDK 实例 online 时调 reply(wid, { selected_option }) 或 reply(wid, { text }, by)——WS 上发出合法 v:1 reply 帧（恰一载荷）；非 online 调用立即触发 code 为 not_connected 的 error 事件，不排队不重试不抛异常（WEB-03 边界由 fail-fast 语义解析——Pattern 7 定稿）"
    - "answered 帧到达时，hub.on(\"answered\", cb) 收到含 wid/seq/answered_by/answered_at/answered_content 的帧对象；ack 帧被静默消费、无公共事件（Q4 定稿——answered 扇出即公共确认信号）"
    - "同一 wid 的重复 answered 帧幂等到达宿主回调——answered 事件路径与 message 帧去重（SeqDedup）完全隔离"
    - "reply 帧的 text 载荷以 Markdown 原文发送（SDK 不做任何转义预处理）——渲染消毒仍由宿主经 renderMarkdown 完成（RPL-02 客户端侧）"
    - "既有四事件（message/history/error/status）API 表面零变更——answered 是纯加法第五事件（D-16 one-way 契约不动，与 D-07 演进哲学同构）"
  artifacts:
    - packages/web-sdk/src/frames.ts — parseServerFrame 的 answered（isAnsweredShape 深校验）与 ack（宽松直通）分支
    - packages/web-sdk/src/connection-machine.ts — MachineAction 联合新增 emitAnswered、handleFrame answered/ack 分支
    - packages/web-sdk/src/pushhub.ts — reply() 公开方法 + answered 第五事件
    - packages/web-sdk/README.md — 回复 API 与 answered 事件文档
  key_links:
    - DO answered 扇出（04-01）→ SDK frames 守卫 → connection-machine emitAnswered 动作 → pushhub listeners answered 分发 → 宿主回调
    - @pushhub/shared 三帧类型 + answered-frame fixture ← SDK 单测直接 import（契约同源验证）
  prohibitions:
    - "reply() 不得实现排队/重试语义——连接层状态机只管连接生命周期，用户动作失败语义属业务层（fail-fast 定稿）；重试是把业务策略混进连接层"
    - "展示名（by）不得由 SDK 持久化——SDK 无 localStorage 依赖纪律，持久化由测试页自己承担（D-24/D-52 模式）"

user_story: "**As a** 宿主页面开发者，**I want to** 用一行 hub.reply() 让页面用户回复推送消息、并用 hub.on(\"answered\") 实时感知群里任何人的处置结果，**so that** 我的页面无需理解线协议就能完整参与回复闭环。"
---

<objective>
Web SDK 补齐回复能力（WEB-03）：帧守卫接受 answered/ack、状态机输出 emitAnswered 动作、pushhub.ts 暴露 reply() 方法与 answered 第五事件——SDK 继续充当三端移植的参考实现。

Purpose: 测试页（04-04）的回复交互全部经此 API；Phase 5/6 的 Tauri/Kotlin 移植将按同构语义复刻。
Output: frames/connection-machine/pushhub 三层扩展 + 单测全绿 + README API 文档增补。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reply-callback/04-CONTEXT.md
@.planning/phases/04-reply-callback/04-RESEARCH.md
@.planning/phases/04-reply-callback/04-PATTERNS.md
@.planning/phases/04-reply-callback/04-01-SUMMARY.md

（三帧类型与 AnsweredFrame 字段集以 04-01 冻结形态为准）
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 帧守卫 answered/ack 分支 + 状态机 emitAnswered 动作</name>
  <files>packages/web-sdk/src/frames.ts, packages/web-sdk/src/connection-machine.ts, packages/web-sdk/test/frames.test.ts, packages/web-sdk/test/machine-events.test.ts</files>
  <read_first>
- packages/web-sdk/src/frames.ts — isMessageShape 逐字段深校验先例（frames.ts:62-87）、parseServerFrame 的 switch（frames.ts:133-151）、pong 宽松直通 case（frames.ts:142-143）、isErrorShape 透明 code（frames.ts:105-107）
- packages/web-sdk/src/connection-machine.ts — MachineAction 联合（connection-machine.ts:92-102，emitHistory 在 :101）、handleFrame 的 history case（connection-machine.ts:224-227）、SeqDedup 仅作用 message 帧的路径（connection-machine.ts:209-213）
- packages/web-sdk/test/frames.test.ts — shared golden fixtures 静态 import 模式与正反例断言
- packages/web-sdk/test/machine-events.test.ts — 帧→动作的状态机单测模式
  </read_first>
  <behavior>
- Test 1（frames 守卫）: import @pushhub/shared/fixtures/answered-frame.positive.json 的全部条目 → parseServerFrame 全部 ok:true 且字段透传；本地构造 ack 帧 {v:1,type:"ack",wid:"w"} → ok:true；answered 字段违例帧（answered_by 为数字、answered_content 缺失、type 拼错）→ 非 fatal 丢弃（ok:false 且非 invalid_version 路径）
- Test 2（状态机）: 输入 answered 帧 → 输出恰一个 kind 为 emitAnswered 的动作且携带原帧；输入 ack 帧 → 无动作输出；answered 路径不经过去重状态（同 wid 两次 answered 帧输入 → 两次 emitAnswered 输出）
  </behavior>
  <action>
frames.ts: 新增 isAnsweredShape 结构校验（照 isMessageShape 逐字段检查：v===1、type 为 answered、wid string、seq 非负整数、answered 为 boolean、answered_by string 或 null、answered_at 数字、answered_content string 或 null——未知字段忽略照 D-07）；parseServerFrame 的 switch 加 type 为 answered 的 case 走 isAnsweredShape（通过则 ok 返回结构化帧、否则非致命丢弃）与 type 为 ack 的 case（宽松直通照 pong 模式：恰查 v/type/wid 三键）。错误帧路径不动（already_replied/not_found 新 code 在 isErrorShape 下天然透明通过——消费侧只查 code 为 string）。
connection-machine.ts: MachineAction 联合新增 { kind: "emitAnswered", frame }（照 emitHistory 声明模式）；handleFrame 加 case "answered" 输出 emitAnswered 动作并 return——该 case 明确置于去重逻辑之外（answered 不是 message 帧，SeqDedup 路径不得触碰，Pitfall 2 的 SDK 侧防线）；加 case "ack" 静默消费无输出（Q4 定稿：answered 扇出即公共确认，单独 ack 公共事件徒增 API 面）。两 case 对未知结构帧的兜底仍是既有非致命丢弃路径。
frames.test.ts import 04-01 冻结的 answered-frame.positive.json 全条目断言 + 本地构造反例；machine-events.test.ts 按 behavior Test 2 扩展。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/web-sdk exec vitest run test/frames.test.ts test/machine-events.test.ts</automated>
  </verify>
  <acceptance_criteria>
- parseServerFrame 对 answered-frame.positive.json 每一条目返回 ok:true（透传字段与 fixture 一致）；ack 三键帧 ok:true；结构违例帧被非致命丢弃
- 状态机 answered 输入产出 emitAnswered 动作、ack 无输出、重复 answered 不被去重（三断言全绿）
- 既有 frames/machine-events 用例零回归
  </acceptance_criteria>
  <done>SDK 能识别并分发服务端新帧：answered 深校验入站、ack 静默消费、去重路径隔离。</done>
  <reversibility rating="reversible">纯加法分支，回滚即删 case。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pushhub.ts reply() 公开方法 + answered 第五事件 + README 增补</name>
  <files>packages/web-sdk/src/pushhub.ts, packages/web-sdk/test/adapter-lifecycle.test.ts, packages/web-sdk/README.md</files>
  <read_first>
- packages/web-sdk/src/pushhub.ts — listeners 注册表（pushhub.ts:70-78）、on() 重载（pushhub.ts:110-120）、sendSync 动作接线与 readyState 防御（pushhub.ts:170-180）、emit 重载（pushhub.ts:291-298）、machine.status 公开只读口（pushhub.ts:382-384）
- packages/web-sdk/test/adapter-lifecycle.test.ts — adapter 全生命周期单测模式（事件触发/ws.send 拦截）
- packages/web-sdk/README.md — 既有 API 文档结构与事件表
  </read_first>
  <behavior>
- Test 1（发送）: 拦截 ws.send——online 状态调 reply("wid1", { selected_option: "确认" }, "运维笔记本") → send 被调恰一次，JSON 帧为 v:1/type:reply/wid:wid1/selected_option:"确认"/by:"运维笔记本"（恰一载荷键存在）
- Test 2（text 变体）: reply("wid1", { text: "**done**" }) → 帧含 text 且无 selected_option、无 by 键（undefined 不序列化为 by 键或序列化为可被服务端接受的缺省——与服务端校验一致即可，断言帧解析后恰一载荷成立）
- Test 3（fail-fast）: offline/connecting 状态调 reply → error 事件触发且 code 为 not_connected、ws.send 未被调、方法不抛异常
- Test 4（本地防御）: reply("w", { selected_option: "a", text: "b" }) 同真或 ({ }) 同假 → error 事件 code 为 invalid_frame、不 send
- Test 5（answered 事件）: 模拟服务端 answered 帧入站 → hub.on("answered") 回调收到帧对象；模拟 ack 帧入站 → 无任何公共事件（on 四事件 + answered 均不触发）
  </behavior>
  <action>
pushhub.ts（per WEB-03 与 Pattern 7 定稿——reply 不进状态机）：listeners 注册表加 answered Set（第五事件，纯加法）；on() 与 emit() 的重载各加 answered 分枝（照既有四事件声明模式）。
新增公开方法 reply(wid: string, payload: { selected_option: string } | { text: string }, by?: string): void——三步防御：(1) 载荷本地检查：selected_option 与 text 同真或同假时 emitError(code 为 invalid_frame) 并 return（服务端仍是权威校验，SDK 提前拒绝省一次往返）；(2) 连接检查：machine.status 非 online（或 ws 为 null / readyState 非 WebSocket.OPEN）时 emitError(code 为 not_connected) 并 return——fail-fast 不排队（用户重试语义属宿主业务层）；(3) 通过则照 sendSync 接线模式 ws.send(JSON.stringify({ v: PROTOCOL_VERSION, type: "reply", wid, ...(payload), ...(by !== undefined ? { by } : {}) }))。apply 分发回路为 emitAnswered 动作加 case → emit("answered", frame)（emitError 沿既有 error 事件载荷结构 { message, code }，D-16 载荷形制不动）。
adapter-lifecycle.test.ts 按 behavior 五组扩展（ws.send 拦截断言帧字节、模拟帧入站断言事件）。README.md 增补：reply() 签名与 fail-fast 语义（not_connected/invalid_frame 两种本地拒绝）、answered 事件载荷说明、恰一载荷约束与 by 上限 64 引用、最小接入示例更新。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/web-sdk exec vitest run test/adapter-lifecycle.test.ts test/frames.test.ts test/machine-events.test.ts</automated>
  </verify>
  <acceptance_criteria>
- 五组行为测试全绿：帧字节恰一载荷断言、fail-fast not_connected、本地 invalid_frame、answered 事件分发、ack 无公共事件
- pushhub.ts 存在 reply 公开方法与 answered 事件注册；既有四事件 API 表面无任何签名变更（既有 adapter-lifecycle 用例零回归）
- README.md 含 reply 方法与 answered 事件条目
  </acceptance_criteria>
  <done>宿主页面一行 hub.reply() 即可发起回复、一个 on("answered") 即可同步处置状态——SDK 回复能力（WEB-03）完成并文档化。</done>
  <reversibility rating="costly">reply()/answered 事件进 SDK 公开 API 表面（02-01 one-way API 契约的增量扩展）——发布后宿主依赖此签名。</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 服务端帧 → SDK 守卫 | answered/ack 帧是网络输入——结构守卫是第一道防线 |
| SDK → 宿主回调 | answered_by/answered_content 将交由宿主渲染（XSS 载体经 SDK 透传） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Tampering | 畸形 answered/ack 帧注入 | low | mitigate | isAnsweredShape 逐字段深校验 + 未知字段忽略（D-07）——畸形帧非致命丢弃不进宿主回调 |
| T-04-14 | Tampering(XSS) | answered_by/answered_content 透传宿主 | medium | mitigate | SDK 不渲染只透传（渲染是宿主/renderMarkdown 职责）；README 明示 answered_content 须经 renderMarkdown、answered_by 须 textContent（D-53）；04-04 E2E 攻击样本断言兜底 |
</threat_model>

## Artifacts this phase produces（本计划新增）

- frames.ts: isAnsweredShape 校验、parseServerFrame 的 answered/ack case
- connection-machine.ts: emitAnswered 动作种类（MachineAction 联合扩展）、handleFrame answered/ack 分支
- pushhub.ts: reply(wid, payload, by?) 公开方法、answered 第五事件（listeners/on/emit 扩展）、emitAnswered 接线
- 错误码（SDK 本地）：not_connected、invalid_frame（复用协议字面量，经 error 事件载荷 code 传递）
- 文档: README.md reply/answered 条目

<verification>
- 单元: frames.test.ts（含 shared fixture 同源验证）、machine-events.test.ts、adapter-lifecycle.test.ts 五组新用例
- 回归: pnpm --filter @pushhub/web-sdk exec vitest run（SDK 全量单测零回归）
</verification>

<success_criteria>
- online 回复发帧、offline fail-fast、载荷恰一本地校验、answered 事件分发、ack 静默——五行为测试全绿
- 既有 API 表面零破坏（四事件不变，answered 纯加法）
- README 文档与实现同步（宿主可按文档接入）
</success_criteria>

<output>
Create `.planning/phases/04-reply-callback/04-03-SUMMARY.md` when done
</output>
