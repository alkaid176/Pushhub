---
phase: 04-reply-callback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/index.ts
  - packages/shared/src/validators.ts
  - packages/shared/fixtures/reply-frame.positive.json
  - packages/shared/fixtures/reply-frame.negative.json
  - packages/shared/fixtures/answered-frame.positive.json
  - packages/shared/fixtures/ws-error-frame.json
  - packages/server/src/chat-room.ts
  - packages/server/test/reply-chain.test.ts
  - packages/server/test/validators.test.ts
  - packages/server/test/fixtures-contract.test.ts
autonomous: false
requirements: [RPL-01, RPL-02, RPL-05]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "客户端经已鉴权 WS 发送 type:\"reply\" 帧（selected_option 与 text 恰提供其一），服务端向回复者本人回 type:\"ack\" 帧、并向频道全部连接扇出 type:\"answered\" 帧（含 wid/seq/answered_by/answered_at/answered_content）——两个在线客户端都能实时看到同一 wid 的 answered 四字段（RPL-01/RPL-05，D-45）"
    - "对已回复消息的第二次 reply 收到 code:\"already_replied\" 错误帧，对不存在 wid 的 reply 收到 code:\"not_found\"——两者严格区分，且均不断开连接（D-42；RPL-05 竞态边界由 CONTEXT 决议解析）"
    - "selected_option 不在原消息 options 白名单内、selected_option 与 text 同真或同假、by 超过 64 个 UTF-16 码元——一律回 code:\"invalid_frame\" 错误帧且连接保持（D-46/D-53；RPL-01 边界由 CONTEXT 决议解析）"
    - "两条 reply 对同一 wid 相继到达（含同毫秒竞态）：恰一条成功收到 ack，另一条收 already_replied——DO 单线程先到先得，测试证明无双成功（D-44）"
    - "自定义回复 text 以 Markdown 原文透存进 answered_content，服务端不做任何转义/解析/包装（RPL-02——渲染消毒是客户端侧职责，经 renderMarkdown 管道在 04-03/04-04 落地）"
    - "answered 同步以独立 answered 帧承载，绝不重发 message 帧（SDK SeqDedup 按 seq 去重会静默丢弃同 seq 重发帧——代码层硬约束，discretion 项就此定稿）"
  artifacts:
    - packages/shared/src/index.ts — ReplyFrame/AckFrame/AnsweredFrame 三接口 + ErrorCode 追加 already_replied/not_found + BY_MAX=64 常量
    - packages/shared/src/validators.ts — validateInboundFrame 的 reply 分支（结构层恰一/长度校验）
    - packages/shared/fixtures/ 下 4 个 fixture 文件（reply 正/反例、answered 正例、ws-error 追加两例）
    - packages/server/src/chat-room.ts — webSocketMessage reply 分支 + attachment displayName 字段
    - packages/server/test/reply-chain.test.ts — 回复链集成测试（含双客户端竞态）
  key_links:
    - webSocketMessage switch 的 reply 分支 → messages 表 answered 四列 UPDATE（同步块零 await）→ ack 单发 + answered 全连接扇出
    - @pushhub/shared 三帧类型 ← server 与 web-sdk 双消费（单一事实源；04-03 SDK 守卫直接 import）
  prohibitions:
    - "不得用 message 帧重发同步 answered 状态——SDK SeqDedup 按 seq 去重会静默丢弃同 seq 重发帧（D-17 硬约束），answered 必须是独立帧类型"
    - "不得为回复另开 HTTP 接口——回复只走 WS 内 v:1 reply 帧（D-45 锁定，one-way）"

user_story: "**As a** 告警群成员，**I want to** 在客户端上对收到的消息回复（快捷选项或自定义 Markdown）并让群里所有人实时看到该消息已被处置，**so that** 重复处置被天然防止（阶段故事第一条切片——回调链在 04-02 落地）。"
---

<objective>
Tracer 切片：在冻结协议（D-07）上完成第一次合规演进——新增 reply/ack/answered 三帧与 already_replied/not_found 两错误码，DO 内挂载 reply 处理（恰一+白名单+一次锁定），双客户端集成测试证明端到端最小回复闭环。

Purpose: 这是 Phase 4 全部后续工作的地基——04-02（回调投递）挂在 reply 处理尾部，04-03（SDK）消费三帧类型，04-04（测试页）驱动 SDK reply API。先以最薄切片证明协议演进 + DO 挂载 + 扇出全链路成立。
Output: shared 协议三帧 + 4 个 golden fixtures + DO reply 分支 + reply-chain 集成测试（含竞态），全部绿灯并提交。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reply-callback/04-CONTEXT.md
@.planning/phases/04-reply-callback/04-RESEARCH.md
@.planning/phases/04-reply-callback/04-PATTERNS.md
@.planning/phases/04-reply-callback/04-VALIDATION.md

协议演进规则与既有帧定义先例：
@packages/shared/src/index.ts
@packages/shared/README.md
@packages/shared/src/validators.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 三帧协议演进 + DO reply 处理——端到端回复闭环</name>
  <files>packages/shared/src/index.ts, packages/shared/src/validators.ts, packages/server/src/chat-room.ts, packages/server/test/reply-chain.test.ts, packages/server/test/validators.test.ts</files>
  <read_first>
- packages/shared/src/index.ts — 既有帧定义先例（SyncFrame index.ts:122-127、HistoryFrame index.ts:139-145）、ErrorCode 枚举（index.ts:70-78 现有 8 码）、LIMITS 常量区
- packages/shared/src/validators.ts — validateInboundFrame 的 sync 分支先例（validators.ts:264-290）与文件头"不出现裸数字阈值"禁令
- packages/shared/README.md — 协议演进三规则（只加字段不改语义/未知字段忽略/未知 v 断连）
- packages/server/src/chat-room.ts — webSocketMessage 入站帧 switch（chat-room.ts:626-646）、handlePublish 扇出遍历与死连接清理（chat-room.ts:506-516）、同步块零 await 先例（chat-room.ts:471-480）、serializeAttachment 现结构（chat-room.ts:607-610）、messages 表 answered 四列
- packages/server/test/retention-alarm.test.ts — directPublish/nextFrame/WS 连接全套测试基建（:56-125，attach-then-trigger 铁律）
- packages/server/test/validators.test.ts — 边界对测试模式（恰好上限通过、超一字符拒绝）
  </read_first>
  <behavior>
- 集成测试（reply-chain.test.ts，先写后实现）：
  - Test 1: directPublish 带 options 的消息 → 两个 WS 客户端（attach-then-trigger，监听先挂、accept 与首帧监听间零 await）→ 客户端 A 发 reply 帧（selected_option）→ A 收到 ack 帧（恰 v/type/wid 三键）→ A 与 B 各收到 answered 帧（wid/seq/answered_by/answered_at/answered_content 全量断言）
  - Test 2: B 随后对同 wid 发 reply → B 收到 code 为 already_replied 的 error 帧，连接仍开
  - Test 3: 对不存在 wid 发 reply → 收到 code 为 not_found 的 error 帧
  - Test 4: 双客户端竞态——A/B 先后对同一 wid 发 reply（零间隔）→ 恰一方收 ack、另一方收 already_replied，无双成功
  - Test 5: 白名单外 selected_option / selected_option 与 text 同真 / 同假 / by 超长 → code 为 invalid_frame 的 error 帧，连接保持
  - Test 6: 带 by 的回复 → answered 帧的 answered_by 为该展示名；不带 by → answered_by 为 null
- 单元测试（validators.test.ts 扩展）：reply 分支边界对——恰一通过、同真拒绝、同假拒绝、text 恰 32768 通过（LIMITS.TEXT_MAX）、by 恰 64 通过（BY_MAX）、by 65 拒绝
  </behavior>
  <action>
在 shared/src/index.ts 按 D-45/D-46/D-52/D-53 与 RESEARCH Pattern 1 新增三个接口，命名与字段集定稿如下（Q4 落点：ack 最小集恰 {v, type, wid}）：ReplyFrame 为 v=PROTOCOL_VERSION、type 字面量 reply、wid: string、selected_option?: string、text?: string、by?: string，注释注明 selected_option 与 text 恰提供其一、by 为自报展示名；AckFrame 为 v、type 字面量 ack、wid；AnsweredFrame 为 v、type 字面量 answered、wid、seq、answered 布尔（注释注明恒 true、字段保留给未来撤答扩展）、answered_by 为 string 或 null、answered_at、answered_content 为 string 或 null，注释注明独立帧是 SeqDedup 硬约束（D-17）而非偏好。ErrorCode 联合追加字面量 already_replied 与 not_found（D-42 区分两个域级拒绝；结构校验失败沿用既有 invalid_frame，与 sync 域坏帧语义一致）。新增导出常量 BY_MAX = 64（与 LIMITS 同级声明，注释注明 UTF-16 码元口径，与频道名/label 同口径）。ClientFrame 联合扩为 PingFrame | SyncFrame | ReplyFrame；ServerFrame 联合追加 AckFrame | AnsweredFrame。既有帧/字段/错误码一个都不动（D-07 只加不改）。
在 shared/src/validators.ts 的 validateInboundFrame 增加 frame.type 为 reply 的结构层分支（照 sync 分支纯函数模式）：wid 必须为非空 string；selected_option 与 text 恰提供其一——同真或同假即返回 invalidFrame()；text 存在时为 string 且长度不超过 LIMITS.TEXT_MAX；selected_option 存在时为 string 且长度不超过 LIMITS.TEXT_MAX（真正的成员白名单校验在 DO 语义层，因其需读库）；by 可缺省，存在时为 string 且长度不超过 BY_MAX。阈值全部 import 自 index.ts，遵守本文件"不出现裸数字阈值"禁令。白名单校验（selected_option 是否在原消息 options 内）不得写进本纯函数。
在 chat-room.ts 的 webSocketMessage switch 挂 reply 分支（per D-42/D-44/D-45/D-46/D-51/D-52/D-53）：结构校验失败（validateInboundFrame 拒绝）回 WsErrorFrame code 为 invalid_frame，不断连——照既有 sync 坏帧防御语义（chat-room.ts:631-640）；SELECT 该 wid 的 options/answered/callback_url 列，无行回 not_found 错误帧；answered 已置位回 already_replied 错误帧（两条 return 均不断连）；selected_option 存在时 JSON.parse(options) 数组 includes 白名单检查，不在白名单回 invalid_frame 错误帧；通过后在同步块零 await 完成 UPDATE messages SET answered=1, answered_by, answered_at, answered_content（Pitfall 5——竞态正确性唯一依赖 UPDATE 的同步性，签名/扇出/入队全部排在其后）；随后向回复者本人 ws.send AckFrame（恰 v/type/wid），再按 handlePublish 扇出遍历同款（chat-room.ts:506-516，含死连接 try/catch 收集后 close(1011)）向全部连接扇出 AnsweredFrame——seq 取该消息行 seq，answered_by 取 by 或 null（D-51/D-53），answered_content 取 selected_option ?? text（D-49 的 reply 字段同源）。attachment 演进：serializeAttachment 调用点加 displayName: by ?? null 增量字段（D-52——名字跨休眠存活，后续 reply 默认复用）。reply 分支本任务不触 callbacks 表与签名（callback_url 非空时暂不动作，04-02 挂入队——功能性缺口可后补，架构挂载点已在）。
reply-chain.test.ts 按 behavior 全套实现（directPublish 可信头直调 DO + nextFrame once 监听 + 双 WS 客户端，逐字沿用 retention-alarm.test.ts 基建与 crypto.randomUUID 派生唯一频道名隔离策略）。validators.test.ts 追加 reply 分支边界对。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/server exec vitest run test/reply-chain.test.ts test/validators.test.ts --max-workers=1 --no-isolate</automated>
  </verify>
  <acceptance_criteria>
- packages/shared/src/index.ts 导出 ReplyFrame/AckFrame/AnsweredFrame 三接口与 BY_MAX=64；ErrorCode 含 already_replied 与 not_found 字面量；ClientFrame 联合含 ReplyFrame、ServerFrame 联合含 AckFrame 与 AnsweredFrame
- reply-chain.test.ts 六组用例全绿（ack 三键断言、answered 四字段断言、already_replied/not_found/invalid_frame 三类错误帧断言、竞态恰一成功）
- validators.test.ts 边界对全绿（恰一/同真/同假/上限边界）
- test 命令退出码 0
  </acceptance_criteria>
  <done>双客户端回复端到端闭环集成测试全绿：reply → ack + answered 扇出 → 二次回复 already_replied → 竞态先到先得，全部提交。</done>
  <reversibility rating="costly">帧字段集与校验规则将进 golden fixtures 反例（D-46 口径）；Task 2 checkpoint 冻结前字段仍可零成本调整。</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: 帧形冻结裁决——三帧命名/字段集/错误码进 golden fixtures 前的用户确认</name>
  <files>（无文件产出——裁决记录进 SUMMARY）</files>
  <read_first>
- packages/shared/src/index.ts（Task 1 产出后的三帧定义与错误码现状）
- .planning/phases/04-reply-callback/04-CONTEXT.md 的 D-42~D-53 决策文
  </read_first>
  <decision>三帧帧形（命名/字段集/错误码/上限常量）是否按 Task 1 实现形态冻结进 golden fixtures（逐字节基线，此后变更即协议事件）</decision>
  <context>
D-45 已锁"WS 内 reply 帧 + 三帧类型"，但帧字面量命名、AnsweredFrame 是否携带恒 true 的 answered 布尔、ack 最小集、错误码 already_replied/not_found 与结构失败沿用 invalid_frame、BY_MAX=64 属规划定稿裁量——进 fixtures 前是最后调整点（Phase 1 01-02 冻结先例：one-way 门经用户之手关闭）。
  </context>
  <options>
    <option id="approve-freeze">
      <name>按 Task 1 形态冻结（reply/ack/answered 字面量 + ack 恰 {v,type,wid} + 两新错误码 + BY_MAX=64）</name>
      <pros>与 RESEARCH Pattern 1/CONTEXT D-42~D-53 完全一致；SDK（04-03）与 fixtures（Task 3）按此并行推进</pros>
      <cons>冻结后字段变更需走协议事件流程</cons>
    </option>
    <option id="adjust-first">
      <name>先调整再冻结（用户指出字段/命名/错误码修改项）</name>
      <pros>帧形完全按用户意图定型</pros>
      <cons>Task 3 与 04-03/04-04 依赖此定稿，需等待调整落实</cons>
    </option>
  </options>
  <resume-signal>用户回复"冻结"或"approve-freeze"即继续 Task 3；回复具体调整项则先改 index.ts/validators.ts/测试再冻结</resume-signal>
  <acceptance_criteria>
- SUMMARY 含用户裁决结论（冻结确认或调整落实记录），one-way 门经用户之手关闭
  </acceptance_criteria>
  <done>帧形经用户确认，golden fixtures 冻结获授权。</done>
</task>

<task type="auto">
  <name>Task 3: golden fixtures 冻结 + 逐字节契约测试扩展</name>
  <files>packages/shared/fixtures/reply-frame.positive.json, packages/shared/fixtures/reply-frame.negative.json, packages/shared/fixtures/answered-frame.positive.json, packages/shared/fixtures/ws-error-frame.json, packages/server/test/fixtures-contract.test.ts</files>
  <read_first>
- packages/server/test/fixtures-contract.test.ts — 既有逐字节契约断言模式（排序后 Object.keys 全键断言 + 严格相等纪律 + 反例驱动校验器闭环）
- packages/shared/fixtures/ws-error-frame.json — 正例数组结构与既有两例的 v/type/code/message 四键形态
- packages/shared/fixtures/sync-frame.negative.json — 反例数组的 _violation 元数据结构
- packages/shared/fixtures/history-frame.positive.json — 服务端发射帧的 fixture 形态（结构检查器锁定模式）
  </read_first>
  <action>
新增 reply-frame.positive.json（正例数组：恰 selected_option、恰 text、带 by、不带 by 至少四例）；新增 reply-frame.negative.json（{_violation, frame} 反例数组：同真、同假、wid 缺失、wid 非字符串、text 超限、by 超一字符等，每例 _violation 注明违例点与期望拒绝 code 为 invalid_frame）；新增 answered-frame.positive.json（服务端发射帧正例：answered_by 为 string 与为 null 两形态）；ws-error-frame.json 追加 already_replied 与 not_found 两例（照既有两例的四键形态，协议事件）。fixtures-contract.test.ts 静态 import 四个文件并扩展断言：reply 正例经 validateInboundFrame 全部通过、反例全部拒绝且 code 与 _violation 元数据逐例匹配（反例闭环驱动校验器）；answered 正例由结构检查器锁定（服务端发射帧无入站校验器，同 history 模式——排序后全键断言）；ws-error 两新例结构与既有例同构。遵守本文件"全键严格相等、禁止子集匹配式宽松断言"纪律。完成后跑 server 全量测试回归确认无既有契约破坏。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/server exec vitest run test/fixtures-contract.test.ts test/reply-chain.test.ts test/validators.test.ts --max-workers=1 --no-isolate</automated>
  </verify>
  <acceptance_criteria>
- packages/shared/fixtures/ 下存在 reply-frame.positive.json、reply-frame.negative.json、answered-frame.positive.json 三个新文件；ws-error-frame.json 含 code 为 already_replied 与 not_found 的两个条目
- fixtures-contract.test.ts 对四个文件的断言全绿（正例通过数 = 正例条目数，反例拒绝数 = 反例条目数）
- 全量 server 测试无回归（fixtures-contract/reply-chain/validators 三文件全绿）
  </acceptance_criteria>
  <done>三帧 golden fixtures 正反例冻结、逐字节契约测试全绿——协议演进按 D-07 路径闭环，后续四端实现有了契约基线。</done>
  <reversibility rating="one-way">golden fixtures 逐字节基线即冻结门（D-45 one-way）——紧随 Task 2 用户裁决之后，此后变更即协议事件。</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS 客户端 → DO | 已鉴权连接上的入站 reply 帧——载荷（wid/text/by）是任意外部输入 |
| DO → 全部 WS 连接 | answered 帧扇出——answered_by/answered_content 将在客户端渲染，是存储型 XSS 载体 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering(XSS) | answered 帧 answered_by/answered_content | high | mitigate | 服务端透传不转义（RPL-02 哑管道）；渲染侧唯一管道纪律：answered_content 经 renderMarkdown 消毒、answered_by 一律 textContent（D-53 明令）——04-03 SDK 事件与 04-04 测试页落实，E2E 攻击样本断言 |
| T-04-02 | DoS | webSocketMessage reply 分支 | medium | mitigate | 结构层恰一/长度校验（shared 纯函数）+ 域级白名单/存在性/已回复判定（DO 单线程）——非法帧回错误帧不断连（对齐宽容忽略坏帧语义），无内存累积面 |
| T-04-03 | Repudiation | answered 一次锁定（D-42/D-44） | medium | mitigate | UPDATE 在同步块零 await 完成（Pitfall 5），DO 单线程串行保证先到先得；竞态集成测试证明恰一成功 |
| T-04-04 | Elevation | 伪造他人 answered_by | low | accept | D-51 自报展示名是产品语义（零账号体系）；名字仅展示用、无权限语义，同名无害——EXT-04 升级真实身份时替换 |
</threat_model>

## Artifacts this phase produces（本计划新增）

- 帧类型：ReplyFrame（v/type:"reply"/wid/selected_option?/text?/by?）、AckFrame（v/type:"ack"/wid）、AnsweredFrame（v/type:"answered"/wid/seq/answered/answered_by/answered_at/answered_content）
- 错误码字面量：already_replied、not_found（进 ErrorCode 联合）
- 常量：BY_MAX = 64（shared/src/index.ts）
- validators：validateInboundFrame 的 reply 结构分支
- fixtures：reply-frame.positive.json、reply-frame.negative.json、answered-frame.positive.json（新增）、ws-error-frame.json（追加两例）
- chat-room.ts：webSocketMessage reply 分支、attachment displayName 字段
- 测试文件：packages/server/test/reply-chain.test.ts（新增）

<verification>
- 集成：reply-chain.test.ts 六组用例（含双客户端竞态恰一成功）
- 契约：fixtures-contract.test.ts 四文件扩展全绿（逐字节冻结）
- 单元：validators.test.ts reply 边界对
- 回归：pnpm test（server 全量）无既有用例破坏
</verification>

<success_criteria>
- 三帧 + 两错误码进 shared 单一事实源并经 golden fixtures 字节冻结（用户裁决记录在案）
- 双客户端端到端回复闭环可观察：A 回复 → A 收 ack、A/B 均收 answered → B 二次回复被拒且错误码区分"已回复"与"不存在"
- 竞态先到先得经测试证明（D-44）
- 既有 12 个 fixtures 与全部既有测试零破坏（D-07 只加不改）
</success_criteria>

<output>
Create `.planning/phases/04-reply-callback/04-01-SUMMARY.md` when done
</output>
