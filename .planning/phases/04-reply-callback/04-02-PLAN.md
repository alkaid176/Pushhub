---
phase: 04-reply-callback
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - packages/server/src/keys.ts
  - packages/server/src/admin.ts
  - packages/server/src/index.ts
  - packages/server/src/chat-room.ts
  - packages/shared/src/validators.ts
  - packages/server/test/callback-delivery.test.ts
  - packages/server/test/retention-alarm.test.ts
  - packages/server/test/admin-reset-kick.test.ts
  - packages/server/test/send-validation.test.ts
  - packages/server/test/validators.test.ts
autonomous: false
requirements: [RPL-03, RPL-04, KEY-06]
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "首次成功回复后，发送方 callback_url 收到恰好一次 POST（被拒回复绝不触发）：headers 为 PushHub-Message-Id / PushHub-Timestamp / PushHub-Signature，body 为 D-49 五字段 JSON（message_id/reply/replied_by/replied_at/channel_id），测试用 Node node:crypto.createHmac 重算 HMAC-SHA256 hex 比对一致（D-43/D-48/D-49；KEY-06 encoding 边界由 CONTEXT 决议解析：timestamp 为毫秒数字符串、hex 编码、常时比较长度前置）"
    - "重试投递每次生成新 timestamp + 新签名，而 callbacks.body 字节自入队起逐次不变——首次与重试投递的验签都通过（Pitfall 4）"
    - "回调目标持续失败：按 1s/2m/10m/30m 档位重试，总尝试恰 5 次（CALLBACK_MAX_ATTEMPTS）后 status='failed' 并落 final_failed_at 与 last_error——记录可经 API 查询（D-50；RPL-04 边界由 CONTEXT 决议解析）"
    - "保留清理与回调重试共存：alarm 重构为多事件单槽调度器后，D-08 每 24h 清理语义不漂移、不被重试 alarm 顺延或吞噬（Pitfall 1）"
    - "GET /api/callback-failures 以 Bearer Channel Key 鉴权（Q3 落点）：有效 key 返回 failed 行列表（wid/url/last_error/attempts/final_failed_at），无效 key 401 错误信封"
    - "频道创建响应携带 signingSecret（phsig_ 前缀 + 32 字符）；旧频道 reveal 时惰性补发；meta 无 secret 的频道回复后回调行直接 status='failed'、last_error 为 no signing secret——可见可循补发，不静默消失（D-47/Pitfall 8）"
    - "callback_url 非 http/https scheme 被 validateSendBody 以 invalid_body 拒绝（SSRF 面收窄，Pitfall 6）"
  artifacts:
    - packages/server/src/keys.ts — generateSigningSecret()、ChannelKeyInfo.signingSecret?、createChannel 写入
    - packages/server/src/admin.ts — signing-secret 段 reveal/reset 端点（CHANNELS_PATH_RE 白名单扩展）
    - packages/server/src/index.ts — /ws 转发携带 X-PH-Signing-Secret / X-PH-Channel-Id 内部头 + GET /api/callback-failures 公网路由
    - packages/server/src/chat-room.ts — meta 新行（signing_secret/channel_id/retention_due）、CREATE_CALLBACKS_DDL、scheduleNextAlarm()、alarm() 双职责重构、reply 尾部入队与即时首投、dispatchDueCallbacks、signCallback、DO 内部 /callback-failures 路由
    - packages/shared/src/validators.ts — validateSendBody 的 callback_url scheme 白名单
    - packages/server/test/callback-delivery.test.ts — 投递/重试/封顶/并存/路由鉴权集成测试
  key_links:
    - reply 处理尾部（04-01 挂载点）→ callbacks 入队（body 预序列化一次）→ 立即 dispatchDueCallbacks（Q5 单路径）→ scheduleNextAlarm 单点重排
    - Worker /ws 转发头 → DO meta 落盘 → dispatchDueCallbacks 读 meta.signing_secret 签名——secret 永远先于任何回调就位（回复必经 WS 连接）
    - Worker GET /api/callback-failures（Bearer Channel Key）→ ch: 预检 → DO 内部 /callback-failures 转发（X-PH-Verified，D-36 模式）
  prohibitions:
    - "回调 body 不得在重试时重新序列化——键序漂移导致字节变化、接收方验签失败；入队时 JSON.stringify 一次存 callbacks.body，投递永远发送该字符串"
    - "不得引入 Cloudflare Queues 或任何外部队列产品做回调重试（CLAUDE.md Do NOT use + 零成本约束）——callbacks 表 + DO alarm 是既定方案"
    - "timestamp 不得用秒——协议全域（created_at/answered_at）均为毫秒，秒口径会使容忍窗错 1000 倍（Q2 落点：毫秒定稿）"
    - "全部 setAlarm 调用不得散落各处——收敛到 scheduleNextAlarm() 单点（publish 判空播种点除外），否则单槽覆盖互相吞噬（Pitfall 1）"
  assumptions:
    - "signing secret 的 admin.html UI 集成本期不交付（Q1 落点，按 RESEARCH 建议裁决）：查/重置以 Admin API 端点交付（GET reveal / POST reset），尊重 D-58 管理页本期不动；测试页验签器的 secret 经创建响应或 API 获取后粘贴。flagged：若用户后续要求 UI，最小方案是测试页可选 Admin Key 小区块（未规划）"
    - "A2/A4（flagged）：cloudflare:test 的 fetchMock 能否拦截 DO 外呼 fetch 未证实——Task 2 第一步 spike；保底方案为测试内起本地 node:http 服务器作 callback_url（wrangler dev 下 DO 与测试同机可 fetch localhost）"
    - "生产遗留频道（0.1.12 前约 10 个）无 signingSecret：补发前回复链正常（answered 扇出不依赖 secret）、回调行落 failed 可查——migrate-on-touch 惰性补发，不写迁移脚本"

user_story: "**As a** 发送方脚本作者，**I want to** 在消息被人工确认的瞬间收到一条可验签、不可伪造、失败自动重试的 HTTP 回调，**so that** 我的自动化脚本能安全地在'通知→确认→续行'闭环里继续执行。"
---

<objective>
回调投递全链路：每频道 signing secret 的生成/存储/传递/重置（D-47），首答触发的 HMAC-SHA256 签名回调 POST（D-48/D-49），DO alarm 多事件单槽调度器重构驱动的指数退避重试与最终失败记录（D-50），以及 Channel Key 域的失败记录查询 API（D-58 服务端侧）。

Purpose: 这是 Phase 4 旗舰差异化能力的核心——SC2（签名回调）与 SC3（重试可查）的服务端全部语义。
Output: callbacks 表 + alarm 调度器重构 + 签名投递 + signing secret 全链 + /api/callback-failures，callback-delivery.test.ts 集成测试全绿。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reply-callback/04-CONTEXT.md
@.planning/phases/04-reply-callback/04-RESEARCH.md
@.planning/phases/04-reply-callback/04-PATTERNS.md
@.planning/phases/04-reply-callback/04-01-SUMMARY.md

（04-01 SUMMARY 仅在需要了解三帧/reply 分支落点时读取——本计划挂载其尾部）
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: signing secret 基建——生成/KV 演进/Admin 端点/WS 转发落 meta</name>
  <files>packages/server/src/keys.ts, packages/server/src/admin.ts, packages/server/src/index.ts, packages/server/src/chat-room.ts, packages/server/test/admin-reset-kick.test.ts</files>
  <read_first>
- packages/server/src/keys.ts — ChannelKeyInfo 现结构（keys.ts:40-44）、SendKeyInfo 可选字段演进先例（keys.ts:33-37）、generateRandomString 拒绝采样（keys.ts:88-99）、generateSendKey 前缀模式（keys.ts:107-109）、createChannel 的 ch: put（keys.ts:218-221）、resetChannelKey（keys.ts:458-486）
- packages/server/src/admin.ts — CHANNELS_PATH_RE 路由白名单（admin.ts:53-54）、checkAdminAuth 两段式常时比较（admin.ts:74-96）、routeAdminApi 前置鉴权（admin.ts:117-120）、sub 段分发链（admin.ts:173-194）、handleResetChannelKey 的 KV 写先 DO 转发后 + 201 完整返回结构（admin.ts:210-237）
- packages/server/src/index.ts — 内部头常量区（index.ts:26-31）、handleWebSocket 的 resolveChannelKey 与转发头（index.ts:90-105）
- packages/server/src/chat-room.ts — fetch() 内部路由分发（chat-room.ts:253-271）、handleKickAll 的 meta INSERT ON CONFLICT 落盘（chat-room.ts:313-320）
- packages/server/test/admin-reset-kick.test.ts — admin 端点测试模式（KV 写 + DO 转发尽力语义断言）
  </read_first>
  <behavior>
- Test 1（admin-reset-kick.test.ts 扩展）: 创建频道 → 201 响应 JSON 含 signingSecret 字段且以 phsig_ 开头、总长恰 phsig_ + 32 字符
- Test 2: GET signing-secret reveal → 200 返回完整 secret（与创建响应一致）；POST signing-secret reset → 200/201 返回新 secret 且与旧值不同，旧值再 reveal 已换新
- Test 3（migrate-on-touch）: 用 runInDurableObject 或直接构造旧格式 ch: 记录（无 signingSecret 字段）→ GET reveal → 返回新生成的 secret 且 KV 记录已被补写该字段
- Test 4（转发落 meta）: reset 后经 X-PH-Verified 连接 WS → runInDurableObject 读 meta 表 signing_secret 行 → 值为新 secret
  </behavior>
  <action>
keys.ts（per D-47）：新增 SIGNING_SECRET_PREFIX 常量 phsig_ 与 generateSigningSecret()（前缀 + generateRandomString(32)，对齐 generateSendKey 模式——约 190 bit 熵）；ChannelKeyInfo 增加可选字段 signingSecret?: string（照 SendKeyInfo 可选字段先例，注释注明旧值无此键天然合法）；createChannel 生成并写入 ch: 记录（三前缀写序不变）；resetChannelKey 重置 Channel Key 时保持 signingSecret 原值不变（密钥分级隔离——KEY-04 语义：任一级重置不影响其他级）。
admin.ts：CHANNELS_PATH_RE 的 sub 白名单加入 signing-secret 段，照既有 if (sub === ... && request.method === ...) 分发链新增两分支——GET 为 reveal（读 ch: 记录；signingSecret 缺省时 migrate-on-touch：generateSigningSecret + KV put 补写后返回），POST 为 reset（generateSigningSecret + KV put 写新值 → 照 handleResetChannelKey 结构先 KV 写、后向 DO 转发内部请求尽力更新 meta（try/catch 吞转发失败）→ 以 200/201 返回完整新 secret——密钥唯一完整返回点先例 D-13）。两分支天然被 routeAdminApi 的 checkAdminAuth 前置鉴权覆盖。测试页/发送方文档层面的掩码显示不适用（API 返回完整值给 Admin Key 持有者）。
index.ts：handleWebSocket 在既有转发头（VERIFIED_HEADER/CHANNEL_KEY_HEADER）旁追加 X-PH-Signing-Secret（resolveChannelKey 结果的 signingSecret 存在时设置）与 X-PH-Channel-Id（channelId）两个内部头——零额外 KV 读。
chat-room.ts：/ws 升级路径（校验通过处）把两个头落 meta 表：INSERT INTO meta (k, v) VALUES ... ON CONFLICT(k) DO UPDATE SET v = ?2，k 为 signing_secret 与 channel_id（照 handleKickAll 代际落盘同款）。正确性论证照 RESEARCH Pattern 5：回调仅在回复后发生、回复必经已鉴权 WS、该连接升级时 secret 已落 meta——secret 永远先于任何回调就位。
admin-reset-kick.test.ts 按 behavior 四组扩展。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/server exec vitest run test/admin-reset-kick.test.ts --max-workers=1 --no-isolate</automated>
  </verify>
  <acceptance_criteria>
- generateSigningSecret() 产出以 phsig_ 开头、总长 37 的字符串（前缀 6 + 32 字符）
- 创建频道 201 响应含 signingSecret；reveal/reset 端点行为四组测试全绿；旧格式记录 reveal 触发 KV 补写
- WS 连接后 meta 表存在 signing_secret 行且值与 KV 一致（Test 4 断言通过）
- reset 不改变 signingSecret 之外的重置语义（既有 admin-reset-kick 用例零回归）
  </acceptance_criteria>
  <done>每频道 signing secret 全链就位：创建生成下发 → KV 存储（normalize 兼容）→ Admin API 查/重置 → WS 转发落 DO meta——回调签名所需的密钥永远先于回调就位。</done>
  <reversibility rating="costly">KV ch: 值结构演进（normalize 兼容层既有先例）+ API 响应结构变更——发布后靠兼容层演进，避免破坏性变更。</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: 对外回调契约冻结裁决——三头/签名格式/D-49 body/重试档位/查询路由</name>
  <files>（无文件产出——裁决记录进 SUMMARY）</files>
  <read_first>
- .planning/phases/04-reply-callback/04-CONTEXT.md 的 D-43/D-48/D-49/D-50/D-58 决策文
- .planning/phases/04-reply-callback/04-RESEARCH.md 的 Pattern 3/4（投递次序与签名方案）
  </read_first>
  <decision>随本计划发布的对外契约全集是否定稿：签名三头 PushHub-Message-Id / PushHub-Timestamp / PushHub-Signature；signed payload 为 timestamp 毫秒数字符串 + "." + rawBody 的 HMAC-SHA256 hex；容忍窗 300000ms（接收方拒收超窗）；回调 body D-49 五字段 {message_id, reply, replied_by, replied_at, channel_id}；重试档位 1s/2m/10m/30m 封顶 5 次、每次重试新 timestamp 新签名；查询路由 GET /api/callback-failures（Bearer Channel Key 鉴权域）</decision>
  <context>
D-48/D-49 已锁方向（one-way——发布后发送方脚本按此验签），但毫秒口径（Q2）、鉴权域（Q3）、档位数值与封顶（D-50 量级已定数值裁量）、路由名是规划定稿项。发布即成对外契约，上线前是唯一零成本调整点。04-04 的 callback-receiver.mjs 与测试页验签器按此口径实现，需先定稿。
  </context>
  <options>
    <option id="approve-contract">
      <name>按上述参数定稿（毫秒/300000ms/五字段/1s-2m-10m-30m 封顶 5 次/Channel Key 域）</name>
      <pros>全部与 RESEARCH 建议、Stripe 心智、协议全域毫秒口径一致；04-04 按此并行</pros>
      <cons>发布后变更即对外破坏性变更</cons>
    </option>
    <option id="adjust-contract">
      <name>先调整再实现（用户指定档位/容忍窗/路由名/鉴权域修改项）</name>
      <pros>契约完全按用户意图定型</pros>
      <cons>Task 3/4 与 04-04 依赖此定稿，需等待调整落实</cons>
    </option>
  </options>
  <resume-signal>用户回复"定稿"或"approve-contract"即继续 Task 3；回复调整项则先更新本计划参数再实现</resume-signal>
  <acceptance_criteria>
- SUMMARY 含用户裁决结论（契约定稿或调整记录）——one-way 对外契约经用户之手关闭
  </acceptance_criteria>
  <done>对外回调契约全集经用户定稿，实现与 04-04 验签参考实现按同一口径推进。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: callbacks 表 + alarm 多事件调度器重构 + 签名投递与重试</name>
  <files>packages/server/src/chat-room.ts, packages/server/test/callback-delivery.test.ts, packages/server/test/retention-alarm.test.ts</files>
  <precondition>Task 1 已合入（meta.signing_secret 落盘路径存在）；A2 spike 前置——第一步验证 cloudflare:test fetchMock 可否拦截 DO 外呼 fetch，不可用则本任务测试改用本地 node:http 服务器作 callback_url（A4 同证）。</precondition>
  <read_first>
- packages/server/src/chat-room.ts — CREATE_RATE_SENDS_DDL 幂等 DDL 模式（chat-room.ts:87-93）、构造器 DDL 调用点（chat-room.ts:231-244）、handlePurge 的 deleteAll+deleteAlarm 与重建（chat-room.ts:357-371）、publish 判空播种点（chat-room.ts:522-524）、alarm() 现实现（chat-room.ts:713-728）、reply 分支（04-01 Task 1 产出）、meta 读写辅助
- packages/server/test/retention-alarm.test.ts — runDurableObjectAlarm 直调（:128,157）、runInDurableObject 内 SQL 种数据（:119-125）、既有保留清理断言（重构不得破坏）
- .planning/phases/04-reply-callback/04-RESEARCH.md 的 Pattern 2（alarm 多事件单槽官方模式）/Pattern 3（投递次序与 attempts 先行递增）/Pattern 4（HMAC 签名与三头）——本任务的实现蓝本
  </read_first>
  <behavior>
- Test 1（首投三头与签名交叉验证）: directPublish 带 options 与 callback_url（指向测试接收器）→ WS reply → 接收器收到恰一次 POST：PushHub-Message-Id 等于消息 wid、PushHub-Timestamp 为毫秒数字字符串、PushHub-Signature 与测试用 node:crypto.createHmac("sha256", secret).update(ts + "." + rawBody).digest("hex") 重算值一致；body 为 D-49 五字段 JSON（message_id/reply/replied_by/replied_at/channel_id）
- Test 2（body 字节冻结）: 同一 callbacks 行多次投递（种失败重试场景）→ 每次接收器收到的 rawBody 字节完全一致、timestamp/签名逐次不同
- Test 3（重试档位与封顶）: runInDurableObject 种 callbacks 行（status pending、next_attempt_at 已过期、attempts 为 n）→ runDurableObjectAlarm 直调 → 断言 attempts 递增、next_attempt_at 按档位 1s/2m/10m/30m 推进；attempts 达 5 时 status 为 failed 且 final_failed_at 非空、last_error 含失败摘要
- Test 4（无 secret 防静默消失）: meta 无 signing_secret 的频道回复带 callback_url 消息 → 回调行 status 为 failed、last_error 为 no signing secret
- Test 5（并存回归）: 种过期重试行 + 到期 retention_due → run alarm → 重试已分发且保留清理已执行、retention_due 已推进 +24h；再 run 第二次 alarm → getAlarm 为 min(下一重试, 新 retention_due) 量级（Pitfall 1 警戒线：不得仍是 +24h 常数）
- Test 6（D-43 恰首答一次）: 已 answered 消息的二次 reply 被拒（04-01 语义）→ callbacks 表该 wid 恰一行、接收器恰收到一次 POST
- Test 7（retention-alarm.test.ts 既有用例）: 全部保持绿（重构后 D-08 清理语义不漂移；如断言需按新调度器语义适配 getAlarm 值，仅调整断言值不删用例）
  </behavior>
  <action>
第一步 spike（前置）: 验证 cloudflare:test 的 fetchMock 可拦截 DO 外呼 fetch（A2）——可行则测试用它拦截断言三头；不可行则在测试内起本地 node:http 服务器作 callback_url（非标准端口，A4 同证），两种路线在 SUMMARY 记录取用结论。
chat-room.ts 常量区（照 RETENTION_* 模式）新增: CALLBACK_MAX_ATTEMPTS = 5、重试档位数组 [1_000, 120_000, 600_000, 1_800_000]（1s/2m/10m/30m，attempts 递增后取档）、FIRST 常量随档位数组首元素、SIGNATURE_TOLERANCE_MS = 300_000（Q2 落点：毫秒口径，供文档与接收方同口径引用；DO 侧投递自身不校验容忍窗——那是接收方职责）。
新建 CREATE_CALLBACKS_DDL（幂等 IF NOT EXISTS，列: wid TEXT PRIMARY KEY / url TEXT NOT NULL / body TEXT NOT NULL / attempts INTEGER NOT NULL DEFAULT 0 / next_attempt_at INTEGER NOT NULL / status TEXT NOT NULL DEFAULT pending / last_error TEXT / created_at INTEGER NOT NULL / final_failed_at INTEGER），挂进构造器 DDL 调用点与 handlePurge 重建处（与 CREATE_RATE_SENDS_DDL 两处既有调用点并列各加一行）。callbacks 随 deleteAll 一并清除语义正确（频道已删无需回调）。
signCallback(secret, timestampMs, body) 私有方法: Web Crypto importKey HMAC SHA-256 → sign 编码 ts + "." + body（点分隔消除拼接歧义，Stripe 同款）→ hex（[...new Uint8Array(mac)].map(b => b.toString(16).padStart(2, "0")).join("") 模式）。
reply 处理尾部（04-01 挂载点，per D-43/D-49/D-50/D-54 与 Q5 落点）: callback_url 非空时构造 body 对象 {message_id: wid, reply: selected_option ?? text, replied_by: by ?? null, replied_at, channel_id: meta.channel_id 读值}，JSON.stringify 恰一次；INSERT OR REPLACE INTO callbacks（wid 主键天然一消息一回调）attempts=0、next_attempt_at=now、status=pending、created_at=now；随后立即调用 dispatchDueCallbacks()（即时首投与到期重试同一条投递函数——单路径，竞态面最小）；最后 await scheduleNextAlarm()。
dispatchDueCallbacks(): SELECT status=pending AND next_attempt_at <= now 的行，逐条 await 处理——attempts 先行递增 UPDATE 并按新 attempts 值预写 next_attempt_at 档位（fetch 中途崩溃按已消耗计，at-least-once、接收方按 message_id 幂等兜底）；读 meta.signing_secret，为 null 时直接置 status=failed、final_failed_at=now、last_error 为 no signing secret（Pitfall 8）；否则生成当下 timestamp（String(Date.now()) 毫秒）+ signCallback 新签名 → fetch(url, POST, headers: content-type application/json + PushHub-Message-Id + PushHub-Timestamp + PushHub-Signature, body: 行内预序列化字符串)——resp.ok 即 status=delivered；非 2xx 记 last_error 为 HTTP 状态码摘要且 resp.body 有值则 cancel() 释放连接；异常 catch 记 last_error 为 String(e).slice(0, 200)；attempts >= CALLBACK_MAX_ATTEMPTS 时置 failed 与 final_failed_at。
alarm() 重构为多事件单槽调度器（Pitfall 1 根治）: try 内先 dispatchDueCallbacks()，再判断 Date.now() >= retention_due（meta 行，缺省视为到期立即补跑）则执行 D-08 原清理 SQL 并写 retention_due = now + RETENTION_INTERVAL_MS；catch 吞异常保节奏（官方推荐 + 既有注释语义）；finally 调 scheduleNextAlarm()。scheduleNextAlarm() 私有方法: 读 callbacks 中 pending 行 MIN(next_attempt_at) 与 meta.retention_due 取 min——无任何到期项则不 setAlarm；有则 setAlarm(max(next, now+1))。publish 判空播种点补一行 retention_due 缺省播种（now + RETENTION_INTERVAL_MS）。全部其余 setAlarm 调用收敛消失（唯一入口 scheduleNextAlarm + publish 判空播种）。retention_due 的读写走 meta 表（INSERT ON CONFLICT 同款）。
retention-alarm.test.ts 若因调度器语义需调整断言（如 getAlarm 具体值），按新语义更新断言值，不得删除任何既有用例。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/server exec vitest run test/callback-delivery.test.ts test/retention-alarm.test.ts --max-workers=1 --no-isolate</automated>
  </verify>
  <acceptance_criteria>
- chat-room.ts 存在 CREATE_CALLBACKS_DDL、scheduleNextAlarm、dispatchDueCallbacks、signCallback 四个符号；alarm() 尾部无字面 +24h 的 setAlarm（重排经 scheduleNextAlarm）
- callback-delivery.test.ts 七组用例全绿（三头/交叉验签/body 字节冻结/档位封顶/no-secret failed/并存回归/恰首答一次）
- Node createHmac 重算与 DO 产出 hex 逐字符一致（Test 1 断言）
- 既有 retention-alarm 用例全绿（可适配断言值、不可删除）
  </acceptance_criteria>
  <done>首答触发的签名回调可投递可重试可封顶可查询：接收方验签通过、重试字节不变、五次封顶落 failed、保留清理共存无漂移。</done>
  <reversibility rating="one-way">签名三头/算法/D-49 body 结构随实现发布即成对外契约（D-48/D-49）——已经 Task 2 checkpoint 用户定稿关闭此门。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 失败记录查询链路 + callback_url scheme 白名单</name>
  <files>packages/server/src/index.ts, packages/server/src/chat-room.ts, packages/shared/src/validators.ts, packages/server/test/callback-delivery.test.ts, packages/server/test/send-validation.test.ts, packages/server/test/validators.test.ts</files>
  <read_first>
- packages/server/src/index.ts — routeRequest 分发与 wsMatch 正则模式、handleSend 的 Bearer 鉴权模式（index.ts:56-65）
- packages/server/src/chat-room.ts — handleHistory 的同步游标查询 + JSON 200 响应模式（chat-room.ts:399-437）、fetch() 内部路由分发（chat-room.ts:253-271）
- packages/shared/src/validators.ts — validateSendBody 的 callback_url 现校验（只查长度）
- packages/server/test/send-validation.test.ts — send 侧校验矩阵测试模式
- packages/server/test/validators.test.ts — shared 校验器单测模式
  </read_first>
  <behavior>
- Test 1（鉴权域）: GET /api/callback-failures 无 Authorization → 401 错误信封；Bearer 无效 key → 401；Bearer 有效 Channel Key → 200 且 JSON 为 failed 行数组（种数据后断言 wid/url/last_error/attempts/final_failed_at 字段存在）
- Test 2（Send Key 不得跨域）: Bearer Send Key 调用 → 401（ch: 域预检不认 sk:）
- Test 3（scheme 白名单）: POST /api/send callback_url 为 http:// 与 https:// → 接受；file:///etc/passwd、ftp://x、javascript: 前缀 → 400 invalid_body
- Test 4（validators 单测）: validateSendBody 对 scheme 白名单的通过/拒绝边界对
  </behavior>
  <action>
index.ts 新增公网路由 GET /api/callback-failures（Q3 落点：Bearer **Channel Key** 鉴权——失败记录是频道域诊断数据，测试页天然持有 Channel Key）: Authorization 头解析 → resolveChannelKey（ch: 预检，无效即 401 错误信封 D-06）→ 照 D-36 内部转发模式构造 Request 到 INTERNAL_ORIGIN/callback-failures，携带 VERIFIED_HEADER 与频道定位头 → 透传 DO 响应。
chat-room.ts fetch() 内部分发追加 /callback-failures 分支: SELECT status='failed' 行按 final_failed_at 倒序 LIMIT 50，返回 JSON 200（字段 wid/url/last_error/attempts/final_failed_at/created_at，照 handleHistory 响应模式）。
shared/src/validators.ts 的 validateSendBody 增加 callback_url scheme 白名单: 存在且非空时必须以 http:// 或 https:// 开头，否则以既有 invalid_body code 拒绝（Pitfall 6 SSRF 面收窄——重试封顶 5 次已是放大器上界、响应体不读即 cancel 已在 Task 2；深防取舍文档化于 SUMMARY）。send-validation.test.ts 与 validators.test.ts 增边界例。既有 golden fixtures 无 file:// 类 callback_url 条目，零契约破坏。
callback-delivery.test.ts 追加路由鉴权 describe（behavior Test 1/2）。
  </action>
  <verify>
    <automated>pnpm --filter @pushhub/server exec vitest run test/callback-delivery.test.ts test/send-validation.test.ts test/validators.test.ts --max-workers=1 --no-isolate</automated>
  </verify>
  <acceptance_criteria>
- GET /api/callback-failures 路由存在且四组鉴权/形状测试全绿（无效 401、Send Key 跨域 401、有效 200 数组）
- validateSendBody 拒绝非 http/https scheme 的 callback_url（invalid_body），http/https 通过
- pnpm --filter @pushhub/server exec vitest run test/fixtures-contract.test.ts --max-workers=1 --no-isolate 保持绿（契约零破坏）
  </acceptance_criteria>
  <done>RPL-04 的"可查"闭环：测试页（04-04）将消费的查询 API 就位，鉴权域为 Channel Key；回调 fetch 目标面收窄到 http/https。</done>
  <reversibility rating="costly">/api/callback-failures 路由名与鉴权域在 Task 2 checkpoint 已随契约全集定稿；代码可重构但路由语义变更需对外协调。</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 发送方 → Worker /api/send | callback_url 是发送方完全可控的 fetch 目标（SSRF 面） |
| DO → 发送方 callback_url | 服务端替发送方外呼——伪造/重放/篡改回调的信任问题在此解决 |
| 测试页/发送方脚本 → Worker /api/callback-failures | 频道域诊断数据的读取边界 |
| Admin → signing secret 端点 | 密钥材料的读取/轮换边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Spoofing | 回调 POST（第三方伪造"确认"欺骗发送方） | high | mitigate | KEY-06 全套：三头 + HMAC-SHA256(secret, ts "." rawBody) + 接收方时间窗 5 分钟 + 常时比较；secret 与 Send Key 权限分离（D-47）——泄露不互伤 |
| T-04-06 | Repudiation/Replay | 回调重放 | medium | mitigate | timestamp 入签名（改 ts 即签名失效）+ 容忍窗拒收；重试新生成 ts/签名（Stripe 同构）——重放的旧 ts 在窗外被拒 |
| T-04-07 | Tampering | 回调 body 篡改 | medium | mitigate | 签名覆盖 rawBody 字节；callbacks.body 入队预序列化一次、重试字节不变（Pitfall 4） |
| T-04-08 | Information Disclosure/SSRF | callback_url 任意目标 | high | mitigate | send 侧 scheme 白名单仅 http/https（Task 3）+ 重试封顶 5 次（放大器上界）+ 响应体不读即 cancel；深防（私网 IP 黑名单）v1 不做、SUMMARY 记录取舍 |
| T-04-09 | Elevation | signing secret 泄露 | medium | mitigate | 独立生成/独立重置（POST reset 即轮换）、Admin Key 两段式常时比较前置鉴权、KV 低频写；secret 不进 WS 广播/回调 body |
| T-04-10 | DoS | 重试放大第三方 | medium | mitigate | 档位 1s/2m/10m/30m + CALLBACK_MAX_ATTEMPTS=5 硬封顶——每消息回调外呼上界 5 次 fetch + 5 次 alarm 唤醒 |
| T-04-11 | Information Disclosure | 验签时序侧信道 | low | mitigate | 接收方常时比较：Node node:crypto.timingSafeEqual 长度前置两段式（D-13 同款）；Workers 内部复用 crypto.subtle.timingSafeEqual |
| T-04-12 | DoS | /api/callback-failures 滥用 | low | accept | Channel Key 域鉴权 + LIMIT 50 固定上界；查询为读操作、DO SQLite 读额度 5M 行/天远超负载（Info 级 finding 先例——admin 域无限流已留 secure-phase 评估） |
</threat_model>

## Artifacts this phase produces（本计划新增）

- 常量: SIGNING_SECRET_PREFIX="phsig_"、CALLBACK_MAX_ATTEMPTS=5、重试档位数组 [1s,2m,10m,30m]、SIGNATURE_TOLERANCE_MS=300_000（chat-room.ts 常量区）
- keys.ts: generateSigningSecret()、ChannelKeyInfo.signingSecret? 可选字段
- admin.ts: signing-secret 段 GET reveal / POST reset 端点
- index.ts: X-PH-Signing-Secret / X-PH-Channel-Id 内部转发头、GET /api/callback-failures 公网路由
- chat-room.ts: meta 行 signing_secret/channel_id/retention_due、CREATE_CALLBACKS_DDL（callbacks 表）、scheduleNextAlarm()、dispatchDueCallbacks()、signCallback()、DO 内部 /callback-failures 路由、alarm() 双职责重构
- shared/validators.ts: callback_url scheme 白名单
- 测试文件: packages/server/test/callback-delivery.test.ts（新增）
- HTTP 契约: 回调 body D-49 五字段结构 + 三头 PushHub-Message-Id / PushHub-Timestamp / PushHub-Signature

<verification>
- 集成: callback-delivery.test.ts 全 describe（投递/重试/封顶/无 secret/并存/恰一次/鉴权域）
- 回归: retention-alarm.test.ts（D-08 清理语义零漂移）、admin-reset-kick.test.ts、send-validation.test.ts、fixtures-contract.test.ts
- 额外: 签名跨实现一致性（Node createHmac 重算比对 DO 产出）
</verification>

<success_criteria>
- 首答触发恰一次签名回调，Node 交叉验签通过，body 字节重试不变
- 5 次封顶 + failed 记录 + Channel Key 域可查（SC3 服务端语义完整）
- alarm 单槽双职责共存，保留清理 24h 节奏不漂移
- 每频道独立 secret：创建下发/可查/可重置/泄露隔离（SC2 + KEY-06 服务端侧完整）
</success_criteria>

<output>
Create `.planning/phases/04-reply-callback/04-02-SUMMARY.md` when done
</output>
