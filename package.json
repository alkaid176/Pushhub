{
  "name": "pushhub",
  "version": "0.1.1",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "test": "pnpm --filter @pushhub/server test",
    "deploy": "pnpm --filter @pushhub/server deploy"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "workerd"
    ]
  }
}
